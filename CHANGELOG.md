# Changelog

## v0.9.43

**동작은 달라지지 않습니다.** 디스커버리 문서가 광고하는 것을 서버가 실제로 지키는지 확인합니다 — 오늘 기준으로 지킵니다. 이미 v0.9.42를 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **디스커버리 문서를 실제 동작에 붙들어 둡니다.** 이 문서는 **같은 사실이 두 번 적힌 것**입니다 — 한 번은 RP가 그것만 보고 자기 설정을 맞추는 광고로, 또 한 번은 그 약속을 지켜야 하는 코드로. 둘을 묶어 두는 것이 없어서, 끝내지 않은 기능의 클레임이 목록에 남아 있거나 발급이 멈춘 뒤에도 광고는 계속될 수 있었습니다. 어느 쪽이든 **RP는 런타임에, 못 쓰는 토큰을 손에 들고** 알게 됩니다.
- 그래서 서버에 무엇을 제공하는지 물어본 뒤 **그것을 전부 그대로 요구합니다.** 광고된 스코프를 모두 요청하고, 광고된 클레임이 **실제로 도착해야** 하며, 폐기 엔드포인트가 광고하는 `none` 인증은 **비밀값 없는 공개 Client**로 직접 시험합니다. 폐기 뒤 그 Refresh Token이 정말 못 쓰게 됐는지까지 봅니다 — 하지 않은 폐기에 `200`을 내는 것이 이 엔드포인트가 가장 잘 숨길 수 있는 실패입니다.
- 계정은 **전부를 낼 수 있도록** 갖춥니다: 이메일 주소, Realm Role, Client Role. **자료가 없어서 빠진 클레임은 아무것도 증명하지 못하기** 때문입니다 — 실제로 픽스처가 덜 갖춰졌을 때 `email`·`email_verified`·`resource_access`가 빠져 보였고, 셋 다 정상이었습니다.
- 발급 쪽에서 `resource_access`를 들어내보고 테스트가 **빠진 클레임의 이름을 대는 것**을 확인했습니다. 스코프 쪽은 픽스처가 덜 갖춰졌을 때 실제 거절을 잡아냈습니다.

## v0.9.42

**토큰 탈취 신호가 흔적도 없이 사라질 수 있었습니다.** 재사용에 대한 대응(계열 폐기)이 실패하면, 감사 기록에 `REFRESH_TOKEN_REUSE`가 **한 줄도 남지 않고** 도둑의 토큰은 계속 동작했습니다.

### 보안

- **계열을 폐기하지 못한 재사용도 기록합니다.** 회전된 뒤에 다시 제시된 Refresh Token은 **이 서비스가 가진 가장 강한 "토큰이 탈취됐다" 신호**이고, 계열 폐기가 대응의 전부입니다 — 그것이 들고 있는 사람에게서 동작하는 토큰을 빼앗습니다.
- 그 폐기의 결과를 버리고 있어서, **실패해도 아무도 알 수 없었습니다.** 폐기를 거부시켜 재보니 — 호출자는 만료된 토큰과 똑같은 `invalid_grant`를 받고, 감사에는 **`REFRESH_TOKEN_REUSE`가 0건**이며(운영 가이드가 찾아보라고 안내하는 바로 그 항목입니다), 계열의 토큰 2개가 **살아 있었습니다.**
- 대응이 실패했을 때야말로 탐지를 **더** 알려야 합니다. 토큰이 아직 동작하고 있으니까요. 이제 `ErrFamilyNotRevoked`가 함께 실려 오고, 기록은 어느 쪽이든 남으며, 해당하면 `detail.family_revoked=false`가 붙고 서버 로그에 error로 남습니다.

## v0.9.41

**동작은 달라지지 않습니다.** Client가 자기 것에만 작용하는지 확인합니다 — 오늘 기준으로 그렇습니다. 이미 v0.9.40을 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **Client가 자기 것에만 작용하는지 확인합니다.** 이것을 판단하는 자리가 둘 있는데, **둘 다 되든 안 되든 같은 답을 냅니다** — 그래서 어느 쪽이 틀려도 조용합니다. 폐기는 폐기하지 않은 토큰에도 `200`을 내고(RFC 7009이 토큰의 존재 여부를 알리지 말라고 합니다), RP 로그아웃은 거절 대신 이 서비스의 로그인 화면으로 되돌립니다.
- 기존 확인은 `id_token_hint`로 온 **꺼진 Client**의 post-logout 주소를 덮습니다. **다른 Client가 등록한 주소**(등록되긴 했는데 그쪽 것이 아닌, 미묘한 경우)와 **아무도 등록하지 않은 주소**는 없었고, **한 Client가 발급받은 적 없는 토큰을 폐기하는 경우**도 없었습니다.
- 각 쌍은 **자기 경우를 먼저 실행합니다** — 그래야 "전부 거절하는 서비스"에서 통과하지 않습니다. 폐기의 소유권 검사를 들어내보고 앞쪽 절반이 실제로 잡는 것을 확인했습니다.

## v0.9.40

**API 키 이름을 조금 길게 적으면 데이터베이스가 자기 스키마를 설명했습니다.** 열 이름·폭·엔진이 계정 있는 누구에게나 그대로 나갔습니다.

### 보안

- **거절이 데이터베이스를 설명하지 않습니다.** 개인 API 키를 만들 때 이름이 몇 글자 길면 `"create personal API key: ERROR: value too long for type character varying(120) (SQLSTATE 22001)"`가 돌아왔습니다 — **열 이름, 그 폭, 그리고 엔진 종류**입니다.
- `writeStoreError`는 정확히 그것을 응답에 닿지 않게 하려고 있고 주석에도 그렇게 적혀 있는데, 이 핸들러는 그것을 지나지 않고 `err.Error()`로 답했습니다 — 막아둔 것을 도로 넣은 셈입니다. 이제 그 매핑을 지나고, 저장소가 사람을 위해 쓴 문장 하나는 센티널을 달아 `500`이 아니라 `400`으로 남습니다.
- 확인은 **이름을 받는 모든 경로**에 너무 긴 이름을 보냅니다 — 새어 나간 하나가 아니라 부류를 봅니다. 그리고 각 거절이 **문장을 담고 있는지**도 확인합니다. 아무 말도 하지 않는 것은 너무 많이 말하는 것만큼이나 쓸모없습니다.

## v0.9.39

**승인 요청을 거절할 때 무엇이 문제인지 말하지 않았습니다.** 개인 화면에서는 이미 결정된 요청에 `403 "conflict"`, 없는 요청에 `403 "not found"`가 나갔고, 관리자 화면에서는 **다른 Realm 관리자의 거절이 `500`** 으로 나갔습니다.

### 사용성

- **거절이 무엇인지 말합니다.** 승인 결정은 여러 이유로 거절될 수 있고 각각 거절당한 사람에게 다른 뜻인데, 두 핸들러 모두 어느 쪽인지 말하지 않았습니다.
- **개인 경로**는 저장소 오류가 뭐라고 하든 그대로 `403`에 실었습니다 — 이미 결정된 요청에 `"conflict"`, 없는 요청에 `"not found"`, 그리고 거르는 것이 없었으므로 **데이터베이스 실패는 그 자신의 문구 그대로**. `writeStoreError`가 응답에 그것이 닿지 않게 하려고 존재하는데 이 경로가 그것을 우회했습니다. 요청 생성도 한 단계 아래에서 같은 모양이었습니다.
- **관리자 경로는 반대로 틀렸습니다.** 그 매핑을 쓰긴 했는데 매핑이 "거절"을 몰라서, **다른 Realm의 관리자가 거절당하면 `500`** 과 함께 이 서비스가 실패했다고 말했습니다 — 열 길이를 넘긴 값과 같은 모양입니다.
- 뿌리는 하나였습니다: **거절에 알아볼 센티널이 없었습니다.** `ErrForbidden`이 그것을 주고, 권한 거절 네 개와 잘못된 입력 두 개가 문장을 갖고, 세 핸들러 모두 하나의 매핑을 지납니다. `insufficient_permission`은 문서가 이미 열거하는 코드입니다.

## v0.9.38

**동작은 달라지지 않습니다.** 일반 사용자가 디렉터리 전체를 읽는 API 키를 스스로 만들 수 없는지 확인합니다 — 오늘 기준으로 만들 수 없습니다. 이미 v0.9.37을 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **일반 계정이 `admin:read` 키를 만들 수 없는지 확인합니다.** 개인 API 키의 `admin:read`는 **Realm의 모든 계정과 모든 Client를 REST와 MCP로 읽습니다.** 요청한 사람이 그럴 자격이 있는지는 키를 만드는 순간의 **플래그 하나**가 정하는데, 그것을 확인하는 것이 없었습니다 — 뒤집히거나 사라져도 누구나 그 키를 만들 수 있게 되고 **테스트는 계속 통과합니다.** 플래그를 항상 참으로 만들어 실제로 그렇게 되는 것을 확인했습니다.
- 거절이 의미 없어지는 두 경우를 **먼저** 확인합니다 — 관리자가 같은 범위를 요청하면 성공해야 하고(아니면 "아무도 키를 못 만든다"는 뜻일 뿐), 일반 계정이 자격 있는 범위는 받아야 합니다(아니면 "endpoint가 고장났다"는 뜻일 뿐).

## v0.9.37

**동작은 달라지지 않습니다.** 인가 코드가 가리키던 것이 사라진 뒤에는 그 코드가 토큰으로 바뀌지 않는지 확인합니다 — 오늘 기준으로 그렇습니다. 이미 v0.9.36을 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **코드가 발급된 뒤 그 근거가 사라지면 거절되는지 확인합니다.** 인가 코드는 **쓰이기 직전에 발급되는 자격증명**이고, 그 사이에 근거가 사라질 수 있습니다 — Client가 꺼지거나, Realm이 정지되거나, 다른 화면에서 세션이 종료되거나. 셋 다 누군가가 내린 결정이고, 그 전에 발급된 코드를 받아주면 **그 결정이 막으려던 토큰을 그대로 내주게 됩니다.**
- 비활성 Realm·Client에 대한 기존 확인은 userinfo·introspection·revocation에 있었습니다. **코드 교환은 빠져 있었고**, 그것이 기존 토큰을 읽는 대신 **새 토큰을 만들어내는** 경로입니다.
- **아무것도 바꾸지 않은 교환을 먼저 실행하고 성공해야 합니다.** 이게 없으면 통과하면서 아무것도 증명하지 못합니다 — 이 검사의 첫 판이 교환에서 PKCE를 빠뜨려 **네 경우 모두 그 이유로 거절**됐고, 경계가 지켜지는 것처럼 보였습니다.

## v0.9.36

**서명 키가 없는 Realm의 키 화면을 열면 화면이 비어버렸습니다.** 빈 목록이 `[]`가 아니라 `null`로 나가서, 콘솔이 그것을 `.length`로 읽다가 죽습니다.

### 사용성

- **빈 목록은 빈 목록으로 답합니다.** Go는 아무것도 담기지 않은 슬라이스를 `null`로 직렬화하는데, 콘솔은 이 값을 그대로 `.length`와 `.map`에 넣습니다. `null`이 오면 빈 표가 그려지는 게 아니라 **예외가 나고 화면이 사라집니다.**
- 서명 키 화면이 정확히 그랬습니다. 서명 키를 만들지 못한 Realm은 **부분 생성으로 기록되고 운영자는 그것을 바로잡으러 이 화면으로 안내받는데**, 도착하는 순간 화면이 죽었습니다. Realm 목록도 같은 모양이었습니다(Realm이 하나도 없어야 닿는 경우입니다).
- **`realm_access.roles`도 마찬가지입니다.** 이건 화면이 아니라 **모든 RP가 읽는 클레임**이고, 거기에 무엇이 들어 있는지 묻는 쪽은 `null`에서 "없음"이 아니라 오류를 만납니다. 계정은 만들어질 때 Realm의 `user` Role을 갖게 되므로, 이건 Role이 전부 회수된 계정의 경우입니다.
- 깨진 하나가 아니라 **모든 목록을 확인합니다**(오늘 13개) — 다음에 추가될 목록이 바로 이 검사가 있는 이유이기 때문입니다.

## v0.9.35

**동작은 달라지지 않습니다.** 거절된 로그인이 어떤 이유였는지 말하지 않는지 확인합니다 — 오늘 기준으로 그렇습니다. 이미 v0.9.34를 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **거절된 로그인이 이유를 흘리지 않는지 확인합니다.** 없는 계정, 틀린 비밀번호, 비활성 계정, 잠긴 계정이 모두 같은 `401`과 같은 문장으로 답합니다. 이걸 확인하는 것이 없었는데, 저장소는 **처음 보는 이름에 대해서도 더미 해시를 검증합니다** — 빠른 답이 "그런 사람 없음"을 뜻하지 않도록 일부러 그렇게 합니다. **테스트 없는 방어는 조용히 사라질 수 있는 방어입니다.**
- 성공하는 경우도 함께 확인해 "전부 거절해도 통과"하는 검사가 되지 않게 했고, 잠긴 계정에 올바른 비밀번호를 넣으면 무엇이 문제인지 알려주는지도 봅니다 — 계속 동작해야 하는 절반입니다.
- **시간 비교는 HTTP가 아니라 저장소에서 합니다.** 요청 하나가 어느 쪽이든 60ms쯤 들어서, 드러나야 할 해싱 시간과 같은 자릿수입니다 — 이 검사의 첫 판이 거기 있었고 **방어를 제거해도 통과했습니다.** 저장소에서 보면 없는 이름이 50ms에서 0.5ms로 떨어지므로, 기준을 넉넉히 잡아도 잡힙니다.

## v0.9.34

**동작은 달라지지 않습니다.** 한 Realm에서 발급된 토큰이 다른 Realm에서는 통하지 않는지 확인합니다 — 오늘 기준으로 그렇습니다. 이미 v0.9.33을 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **토큰이 발급된 Realm 밖에서는 통하지 않는지 확인합니다.** 토큰은 그것을 발급한 Realm의 것입니다. 관리 API의 경계는 경로마다 확인하고 있었지만, **한 테넌트의 RP가 다른 테넌트의 Access Token으로 무엇을 할 수 있는지**는 확인하는 것이 없었습니다 — 이 제품이 존재하는 이유인 바로 그 경계입니다.
- 두 답을 **같은 토큰 하나로** 얻습니다. 그래서 전부 거절해도 통과하는 검사가 될 수 없습니다 — 발급한 Realm은 userinfo에서 받아들이고 introspection에서 활성이라고 답해야 하며, **같은 호흡에** 다른 Realm은 거절해야 합니다. 거절 확인을 발급 Realm 쪽으로 돌려보면 실패하는 것으로 그 절반이 의미 있음을 확인했습니다.
- 오늘은 지켜집니다 — 다른 Realm의 userinfo는 `401`, introspection은 오류가 아니라 `active:false`(RFC 7662가 요구하는 형태)입니다.

## v0.9.33

**눈으로 구별할 수 없는 계정·Role·Client를 만들 수 있었습니다.** 감사 기록이 행위자를 이름으로 가리키고, 콘솔이 관리자에게 이름으로 고르게 하는 제품에서 이건 그 둘을 동시에 무너뜨립니다.

### 보안

- **같은 글자의 두 가지 표기가 이제 같은 이름입니다.** 유니코드는 같은 글자를 쓰는 방법이 여럿입니다 — `é`가 한 글자일 수도, `e` + 결합 부호일 수도 있습니다. 이름을 유일하게 만들라고 있는 인덱스 뒤에서 이 둘은 **다른 행**이었고, 그래서 콘솔·감사 기록·이름을 보여주는 모든 RP에서 **완전히 같아 보이는 계정 두 개**가 존재할 수 있었습니다. 이제 한 표기로 합성하므로 두 번째는 "이미 쓰는 이름"이 됩니다.
- **보이는 대로 저장되지 않는 문자를 거절합니다.** `admin` 사이에 제로폭 결합자를 넣으면 화면에는 `admin`으로 보이고, 오른쪽-왼쪽 오버라이드는 뒤 글자를 뒤집어 보여주며, 개행은 **한 줄에 한 항목을 쓰는 로그에서 줄을 위조**합니다. 제어 문자와 서식 문자는 이유를 적은 문장과 함께 거절합니다.
- 사용자 이름·표시 이름, Role과 Client Role 이름, `client_id`와 Client 이름에 **함께** 적용했습니다 — 관리자가 권한을 부여할 때 고르는 목록에 똑같아 보이는 Role이 있는 것은 똑같아 보이는 계정과 같은 결함입니다.

### Upgrade notes

검사는 새로 쓸 때만 적용됩니다. 이미 저장된 이름은 그대로 두므로 기존 계정은 계속 로그인합니다. 이전에 만들어진 이런 이름이 있는지 확인하려면 감사 화면에서 행위자 이름을 살펴보세요. `golang.org/x/text`가 간접에서 직접 의존성으로 바뀌었을 뿐, 모듈 그래프와 버전은 그대로입니다.

## v0.9.32

**너무 긴 값을 보내면 서비스 장애로 답했습니다.** 이름을 몇 글자 길게 붙여 넣으면 `500`과 "요청을 처리하지 못했습니다"가 돌아왔습니다 — 고칠 수 있는 것을 말해주지 않고, 모니터링에는 **서비스가 실패한 것으로** 잡혔습니다.

### 사용성

- **열에 들어가지 않는 값은 400으로, 무엇을 고쳐야 하는지와 함께 답합니다.** 값이 열 길이를 넘는 것은 **호출자가 고칠 일**입니다. 그런데 `500`으로 답하면 사람에게는 이 서비스가 고장 났다고 말하고, 콘솔에는 보여줄 것을 주지 않으며, 지켜보는 대시보드에는 **서비스가 오류를 내고 있다**고 알립니다 — 폼 한 칸 때문에요.
- 열 길이는 스키마가 정합니다. 그래서 **각 길이를 Go에 다시 적지 않고**(적으면 실제로 결정하는 값과 어긋날 수 있는 두 번째 사본이 됩니다) 모든 쓰기가 이미 오류를 보고하는 한 곳에서 처리합니다. 사용자·Client·Realm·Role·API Key·Federation 전부 한 번에 덮입니다.
- 거절은 새 오류 코드를 만들지 않고 `invalid_input`을 씁니다 — OpenAPI 문서가 열거하는 코드 집합은 그대로입니다.

## v0.9.31

**동작은 달라지지 않습니다.** 콘솔의 API·MCP 화면이 표시하는 프로토콜 개정 번호가 서버가 실제로 말하는 것과 같은지 확인합니다 — 오늘 기준으로 둘 다 일치합니다. 이미 v0.9.30을 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **연동 화면이 이 서비스가 말하는 개정 번호를 표시하는지 확인합니다.** 이 화면은 연동하는 사람에게 어느 개정을 기준으로 만들지 알려줍니다. 두 숫자 모두 이 서비스 자신의 것이고(MCP 개정은 여기 상수, OpenAPI는 이 핸들러가 내보내는 문서), 화면은 **각각의 사본을 따로** 들고 있습니다.
- 사본이 뒤처지면 **서버가 더 이상 말하지 않는 개정을 기준으로 만들게 되고**, 확인하러 갈 자리가 바로 그 화면입니다. 어느 쪽을 옮겨도 어느 것인지 말하며 실패합니다.

## v0.9.30

**빈 데이터베이스에 인스턴스를 여럿 동시에 띄우면 하나만 살아남았습니다.** 8개를 함께 띄우면 **7개가 기동에 실패**합니다.

### 운영

- **함께 시작한 인스턴스가 모두 부트스트랩을 통과합니다.** 부트스트랩은 `Serializable`로 도는데, 빈 데이터베이스를 만난 인스턴스들은 모두 "Realm이 없다"를 읽고 모두 하나를 쓰려 합니다. 이긴 하나를 빼고 전부 직렬화 충돌로 거절되고, 기동 경로는 그것을 `os.Exit(1)`로 바꿉니다.
- 재시작하면 Realm이 이미 있어 성공하므로 **결국은 자가 치유됩니다.** 하지만 배포의 **첫 롤아웃에서 컨테이너들이 죽고** 로그에 "service bootstrap failed"가 남습니다 — 이 제품은 다중 Pod 배포를 문서로 안내하는데, 첫 설치가 그렇게 보여서는 안 됩니다.
- 직렬화 충돌은 **"다시 하라"는 뜻인 유일한 오류**이므로 돌려주는 대신 재시도합니다. 멱등성은 그대로이고, 이제 확인도 합니다 — 동시 기동 8개 뒤에 Realm 하나, 관리자 하나, 그리고 **정확히 하나의 인스턴스만** 자신이 만들었다고 보고합니다.

## v0.9.29

**주석이 코드에 없는 성질을 주장하고 있었습니다.** 읽을 수 없는 해시가 해싱 슬롯을 잡고 있었고, 그 동안 진짜 로그인이 뒤에서 기다렸습니다.

### 보안

- **읽을 수 없는 해시가 해싱 슬롯을 가져가지 않습니다.** `VerifyContext`의 주석은 *"형식이 깨진 해시는 슬롯을 잡기 전에 거절된다"*고 말했지만, 실제로는 **슬롯을 먼저 잡고 그 안에서 형식을 읽었습니다.** 이 슬롯 제한은 로그인이 몰릴 때 Argon2의 메모리 사용이 요청 수에 비례해 늘어나는 것을 막으려고 있는 것이고, 읽을 수 없는 해시는 애초에 비교까지 가지 못합니다 — 그것을 알아내려고 슬롯을 붙잡으면 **실제 로그인이 쓸 수 있었을 슬롯 하나를 아무것도 아닌 데 씁니다.**
- 형식을 읽는 비용은 사실상 없고 디코더도 이미 분리돼 있어, 코드가 주석이 줄곧 주장해온 대로 동작하게 했습니다.

## v0.9.28

**해싱 비용을 올려도 기존 비밀번호는 영원히 옛 비용에 머물렀습니다.** 비밀번호를 바꾸지 않은 사람은 계정을 만든 날의 비용을 그대로 유지했고, 아무것도 그 사실을 알려주지 않았습니다.

### 보안

- **로그인에 성공하면 오래된 비밀번호 해시가 현재 비용으로 올라갑니다.** Argon2id 비용(`DefaultParams`)이 변수인 이유는 하드웨어가 싸질수록 올리기 위해서입니다. 그런데 올려도 **그 이후에 설정된 비밀번호만** 보호받았습니다. Client Secret은 같은 이유로 최초 인증 성공 시 스스로 승격하는데, 사용자 비밀번호에는 그 경로가 없었습니다.
- 로그인 성공은 **평문이 손에 있는 유일한 순간**이라 여기서 다시 해싱합니다. 그 사이에 비밀번호가 바뀌었다면 덮어쓰지 않도록 조건을 걸었고, `password_changed_at`은 건드리지 않습니다 — 비밀번호가 바뀐 것이 아니라 **저장 형식**만 바뀌었기 때문입니다.
- 이 실패는 의도적으로 무시합니다. 이 저장소가 다른 곳에서 없애온 "조용한 최선 노력"과는 다릅니다 — **되돌려진 것이 없고**, 저장된 해시는 방금 전과 정확히 같은 강도이며, 사람이나 운영자가 할 일이 없고, 다음 로그인이 다시 시도합니다.
- `Verify`와 이 검사가 **같은 디코더 하나**를 씁니다. 같은 형식을 두 번 적지 않습니다.

## v0.9.27

**대시보드를 열 때마다 잠긴 계정 3건을 세려고 사용자 5만 행을 읽었습니다.**

### 성능

- **잠긴 계정 조회에 인덱스가 생겼습니다.** 대시보드는 열릴 때마다 잠긴 계정 수를 세고, 관리자가 그 숫자를 따라가면 사용자 화면이 같은 조건으로 목록을 냅니다. 둘 다 "잠금이 아직 안 풀린 계정"을 묻는데 그런 계정은 거의 없고, 그 조건에 인덱스가 없었습니다 — **5만 행을 읽어 3건을 셌습니다**(비용 1645 → 8.15).
- 인덱스 조건은 "잠금 값이 설정되어 있는가"입니다. "아직 잠겨 있는가"는 시계와의 비교라 인덱스 조건에 들어갈 수 없기 때문입니다. 잠금은 드물고 일시적이므로, Realm에 계정이 몇이든 이 인덱스는 몇 행에 머뭅니다.

### Upgrade notes

기동 시 인덱스가 하나 추가됩니다. 기존 데이터는 바뀌지 않으며 되돌릴 때 이전 이미지도 그대로 동작합니다.

## v0.9.26

**사용자 역할 편집 화면이 열릴 때마다 Federation 배정 표를 통째로 읽었습니다.** 역할 하나마다 한 번씩, 저장할 때 또 한 번씩입니다.

### 성능

- **"이 역할이 디렉터리에서 온 것인가"를 인덱스로 답합니다.** 이 질문은 사용자와 역할로 묻고 **공급자는 대지 않습니다** — 어차피 답은 하나이고 호출자가 들고 있는 것은 사람이지 공급자가 아니기 때문입니다. 그런데 이 표의 유일한 인덱스는 공급자로 시작하는 기본 키라, 두 질문 모두 그것을 쓸 수 없었습니다.
- 둘 다 관리자가 **누군가의 역할을 편집하는 화면**에서 돕니다 — 나열된 역할마다 한 번씩(디렉터리가 주는 것을 표시하려고), 저장할 때 역할마다 다시 한 번씩(로컬로 준 행 중 무엇을 지울지 정하려고). 연동 계정 1만 명이 역할 3개씩 가진 상태라면 그때마다 **3만 행**을 읽었고, **"아니오"라고 답하는 경우는 그것을 증명하려고 전부 읽어야** 했습니다.
- 측정은 그 "아니오" 경우로 했습니다 — 비용 760에서 8로, 전수 스캔이 Index Only Scan으로 바뀝니다.

### Upgrade notes

기동 시 인덱스가 하나 추가됩니다. 기존 데이터는 바뀌지 않으며 되돌릴 때 이전 이미지도 그대로 동작합니다.

## v0.9.25

**세션 화면이 Realm의 세션 표를 통째로 읽고 있었습니다.** 500건을 보여주려고 5만 건을 읽어 정렬했고, 기다리는 시간이 **답이 아니라 Realm 크기에 비례해** 늘었습니다.

### 성능

- **콘솔 세션 목록이 필요한 만큼만 읽습니다.** 이 화면은 Realm의 세션을 최근 사용 순으로 500건 가져오는데, 그 조합(`realm_id`, `last_access`)에 인덱스가 없었습니다. 5만 건 규모에서 실행 계획은 **전체를 읽고 정렬해 500건을 남기는** 것이었습니다(cost 4080). 인덱스를 넣으면 인덱스로 500행만 만집니다(cost 0.69).
- **사고 대응 중에 여는 화면**입니다 — 누가 접속해 있는지 보고 세션을 끊으러 가는 자리라, 배포가 바빠질수록 느려져서는 안 되는 화면입니다.
- v0.9.24의 확인이 이 목록까지 함께 봅니다. 여전히 **인덱스 이름이 아니라 실행 계획**을 묻습니다.

### Upgrade notes

기동 시 인덱스가 하나 추가됩니다. 기존 데이터는 바뀌지 않으며 되돌릴 때 이전 이미지도 그대로 동작합니다.

## v0.9.24

**계정을 잠그는 그 순간이 가장 느렸습니다.** 누군가의 세션을 모든 곳에서 끝내는 작업이 배포 전체의 Refresh Token을 **전부 읽고** 있었습니다.

### 성능

- **비상 정지가 Refresh Token 표를 전수 스캔하지 않습니다.** 세션을 모든 곳에서 끝내는 작업은 `user_id`로 Refresh Token을 고르는데, 그 열에 인덱스가 없었습니다. 토큰 6만 건이 계정 200개에 흩어진 상태에서 한 계정의 300건을 찾으려고 **나머지 5만 9천 7백 건을 읽고 그 동안 잠금을 잡았습니다.**
- 이 작업이 곧 비상 정지입니다 — 관리자가 계정을 비활성화할 때, 비밀번호가 유출됐다고 믿어 바꿀 때, 디렉터리 동기화가 계정을 무더기로 비활성화할 때 계정마다 한 번씩 돕니다. **빠르고 확실해야 하는 순간인데, 훑는 표는 토큰이 발급될 때마다 커지는 바로 그 표**였습니다.
- Token 갱신이 실패한 뒤 되돌리기가 "이 토큰에 후속 토큰이 있는가"를 `parent_id`로 묻는 것도 전수 스캔이었습니다. 회전된 후속 토큰만 부모를 가지므로 부분 인덱스로 만들었습니다.
- 확인은 **인덱스 이름이 아니라 실행 계획**을 봅니다. 있지만 선택되지 않는 인덱스는 이름 검사만 통과시키고, 막으려던 스캔은 그대로 일어나기 때문입니다.

### Upgrade notes

기동 시 인덱스 두 개가 추가됩니다. 기존 데이터는 바뀌지 않으며 되돌릴 때 이전 이미지도 그대로 동작합니다.

## v0.9.23

**README가 Digest Key 보관 규칙을 불완전하게 말하고 있었습니다.** 그대로 따르면 이전 키를 지운 순간 **Confidential Client 인증이 한꺼번에 끊깁니다.**

### 문서

- **Digest Key 보관 규칙의 모든 서술이 Client Secret을 함께 말합니다.** 운영 가이드는 이것을 정확히 적고 있습니다 — 세 항목 중 마지막이 시점을 정하며, **Client Secret은 만료되지 않기 때문**입니다. README는 같은 규칙을 한 줄로 줄이면서 그 항목을 뺐습니다.
- **그 항목이 빠진 서술은 규칙의 축약본이 아니라 다른 규칙입니다.** 문자 그대로 따르면 운영자는 Session·Token 수명이 지나기를 기다린 뒤 이전 Digest Key를 제거하고, 그 시점 이전에 만들어진 Secret을 가진 **모든 Confidential Client가 Token·Introspection·Revocation endpoint에서 동시에 인증에 실패합니다** — 다이제스트는 재작성되지 않고, 검증은 설정에 남아 있는 키만 시도하기 때문입니다.
- Digest Key를 얼마나 보관해야 하는지 말하는 모든 문단을(이어지는 목록까지 함께) 읽어 Client Secret을 말하지 않으면 실패하는 검사를 넣었습니다.

## v0.9.22

**동작은 달라지지 않습니다.** v0.9.21이 만든 두 번째 응답 형태를 API 명세가 설명하지 않고 있었습니다. 이미 v0.9.21을 쓰고 계시다면 서두를 이유가 없습니다.

### 문서

- **비활성화가 절반만 적용됐을 때의 응답 형태를 OpenAPI 문서가 설명합니다.** v0.9.21에서 사용자 변경은 200 응답을 두 가지 갖게 됐습니다 — 계정 그대로이거나, 세션 정리가 부족했을 때 `sessions_ended`와 `message`가 더해진 형태입니다. 공급자 endpoint들은 같은 것을 이미 선언하고 있었고, 이것만 빠져 있었습니다.
- 이 확인은 **실제 응답을 그 endpoint 자신의 200 스키마와** 대조하며, 스키마가 참조하는 `$ref`를 따라갑니다. 문서 전체에서 이름을 찾는 방식으로 처음 썼더니 두 이름이 **다른 스키마에 이미 있어서**, 이 endpoint가 아무 말도 하지 않는 채로 통과했습니다.

## v0.9.21

**계정 비활성화가 절반만 걸린 채 "정상"으로 기록될 수 있었습니다.** 계정은 꺼졌는데 열려 있던 세션은 살아 있고, 요청은 **감사 기록 없이 500**을 답했습니다. 그리고 다시 시도하면 **종료를 건너뛴 채 성공이라고 기록했습니다.**

### 보안

- **비활성화의 나머지 절반이 실패하면 그렇게 말합니다.** 비활성화는 행을 먼저 쓰고 세션을 나중에 끝냅니다. 두 번째 절반이 실패하면 계정은 꺼졌고 세션은 살아 있는데, 요청은 감사 기록 한 줄 없이 500을 답했습니다 — **일어난 일을 일어나지 않은 일로 설명하는 것**이고, 공급자 쪽에서 `ErrUsersNotSignedOut`이 바로 이것을 피하려고 존재합니다.
- **재시도가 더 나빴습니다.** 세션 정리가 "활성 → 비활성" 전환에만 걸려 있어서, 두 번째 시도는 계정이 이미 꺼진 것을 보고 정리를 건너뛴 뒤 **깨끗한 SUCCESS를 기록**했습니다. 관리자가 자기 작업을 확인하는 바로 그 방법이, 일어나지 않은 정지를 확인해 준 셈입니다.
- 이제 **결과가 비활성인 모든 수정이 정리를 수행하므로 재시도가 실제로 재시도합니다.** 정리가 부족하면 계정을 그 사실과 함께 돌려주고, 감사에는 `PARTIAL`과 `users_signed_out=false`가 남으며, 콘솔은 정지가 끝났다고 알리는 대신 경고를 띄웁니다 — 세션 화면이 Refresh Token 폐기 실패에 쓰는 것과 같은 방식입니다.

## v0.9.20

**감사 기록이 이 서비스가 스스로 만든 일을 "토큰 도난"으로 적을 수 있었습니다.** Token 갱신에서 회전을 되돌리지 못하면, RP의 다음 시도가 재사용으로 읽혀 계열이 폐기되고 `REFRESH_TOKEN_REUSE`가 남습니다 — 원인을 잘못 가리키는 기록입니다.

### 보안

- **회전을 되돌리지 못한 갱신을 기록합니다.** Refresh 교환은 새 토큰 쌍에 서명하기 **전에** 호출자의 토큰을 회전시킵니다. 서명이 실패하면 회전을 되돌리는데, **되돌리기까지 실패하면** 호출자는 이 서비스가 이미 소진된 것으로 여기는 토큰을 그대로 들고 있게 됩니다. 그 다음 시도는 재사용으로 읽히고, 계열이 폐기되며, 감사에는 `REFRESH_TOKEN_REUSE`가 남습니다 — **이 서비스가 가진 가장 강한 "토큰이 탈취됐다"는 신호이고, 운영 가이드가 바로 그것을 찾아보라고 안내하는 항목입니다.**
- 즉 기록은 **이 서비스가 스스로 만든 일을 도난이라고 말하고**, 실제로 무슨 일이 있었는지 적힌 줄은 로그에만 있어서 그 기록을 읽을 무렵에는 이미 사라져 있었습니다.
- 이제 그 갱신이 `TOKEN_REFRESH` `result=PARTIAL`, `detail.rotation_rolled_back=false`로 남습니다. 운영 가이드의 `REFRESH_TOKEN_REUSE` 항목도 **먼저 이것부터 확인하라**고 안내합니다.

## v0.9.19

**로그아웃 알림이 RP에 닿지 못하는 다른 네 가지 경우도 기록에 남습니다.** v0.9.18은 "시도했지만 실패한 전달"만 남겼습니다.

### 보안

- **알림이 나가지 못하는 모든 경우를 감사에 남깁니다.** 같은 논거가 그대로 적용되는데도 로그 한 줄씩만 남던 자리가 넷 있었습니다 — 큐가 가득 차서 **아무 시도도 하지 못한 경우**, Realm을 읽지 못해 대상조차 찾지 못한 경우, 대상 조회가 실패한 경우, 특정 Client의 로그아웃 토큰에 서명하지 못한 경우입니다. 어느 쪽이든 결과는 같습니다 — **사용자가 닫았다고 믿는 세션이 어딘가에 열려 있습니다.**
- 첫 번째가 가장 알려야 할 경우입니다. 요청이 **한 건도** 나가지 않았기 때문입니다. 이 기록은 별도 goroutine에서 남깁니다 — `SessionRevoked`는 로그아웃 응답을 기다리게 하지 않아야 하므로 즉시 반환해야 하고, 종료 시 `Wait`가 그 goroutine까지 기다리므로 **드롭됐다는 기록이 드롭되지는 않습니다.**

## v0.9.18

**RP에 닿지 못한 백채널 로그아웃이 감사 기록에 남습니다.** 지금까지는 로그와 지표에만 남아서, 사용자가 닫았다고 믿는 세션이 **RP에서는 열려 있는 채로** 아무 기록 없이 지나갔습니다.

### 보안

- **전달되지 못한 백채널 로그아웃을 감사 기록에 남깁니다.** 콘솔은 세션 행이 폐기되는 즉시 "종료됨"이라고 말하고, RP로 가는 알림은 **그 뒤에** 나갑니다. 그래서 전달 실패는 운영자가 보는 어느 자리에도 나타나지 않았습니다. 남는 것은 **사용자가 닫았다고 믿는, RP에서는 여전히 열려 있는 세션**이고, "정말 모든 곳에서 끝났는가"는 로그가 이미 순환된 한참 뒤에 묻는 질문입니다. 감사 이벤트는 보존 기간 동안 유지되며, LDAP 동기화 결과를 로그가 아니라 감사에 남기는 것과 같은 이유입니다.
- **실패만 남깁니다.** 성공한 전달은 평범한 경우이고, 전부 적으면 읽을 가치가 있는 것이 묻힙니다.
- 행위자는 **이 서비스**입니다 — 사용자는 로그아웃을 요청했고, 부족했던 것은 전달입니다. 사용자를 행위자로 적으면 기록이 `users` 행에 매이게 되는데, 몇 년 뒤의 질문에 답하려고 남기는 이벤트가 그것에 의존해서는 안 됩니다. 처음 만든 판이 실제로 그렇게 했다가 외래 키에서 실패했고, 그 사실을 알리는 것은 로그 한 줄뿐이었습니다.

## v0.9.17

**동작은 달라지지 않습니다.** Realm 정책 한계가 네 곳에 적혀 있는데 세 곳만 서로 대조하고 있었습니다 — 빠진 하나가 **관리자가 값을 고를 때 실제로 마주하는 화면**이었습니다. 오늘 기준으로 일곱 개 필드 전부 일치합니다. 이미 v0.9.16을 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **콘솔의 입력 한계를 서버가 강제하는 값과 대조합니다.** 이 숫자들은 네 곳에 적혀 있습니다 — 검증 테이블, 마이그레이션의 CHECK 제약, 운영 가이드, 그리고 콘솔 Realm 화면의 숫자 입력. 앞의 셋은 이미 서로 대조하고 있었는데, **콘솔만 빠져 있었습니다.**
- 한쪽에서 한계를 올리고 폼을 잊으면, 폼이 **서버가 받아줄 값을 거부하거나 받지 않을 값을 권합니다.** 그리고 그 거절은 저장을 누른 뒤에야 오고, 낡은 쪽이 폼 자신의 한계였다는 단서는 어디에도 없습니다. 이제 한 곳만 바꾸면 어느 필드인지 말하며 실패합니다.
- 유휴 만료는 하한이 다릅니다 — `0`이 끄기이므로 폼은 그것을 받아야 합니다. 그 예외만 명시하고 나머지는 그대로 비교합니다.
- 테이블 주석이 "세 곳"이라 말하던 것도 고쳤습니다. 그 주석 자체가 같은 문제의 축소판이었습니다.

## v0.9.16

**Role을 삭제하면 감사 기록이 어떤 Role이었는지 말합니다.** 지금까지는 방금 지워진 행의 ID만 남겨서, **그 ID로는 아무것도 되찾을 수 없었습니다.**

### 보안

- **Role 삭제 기록이 이름과 영향받은 인원을 남깁니다.** Role을 지우면 행이 사라지는데, 감사 이벤트는 그 행의 ID로만 대상을 가리키고 다른 것은 아무것도 담지 않았습니다. 삭제가 반영되는 순간부터 그 ID는 무엇도 가리키지 못하므로, **감사 기록이 존재하는 이유인 바로 그 질문 — "어떤 Role이 삭제됐는가" — 에 답할 수 없었습니다.** 연쇄 삭제로 권한을 잃은 사람들에 대해서도 한 마디가 없었습니다.
- 이 저장소의 다른 삭제들은 이미 무엇을 지웠는지 남깁니다 — Federation 삭제는 공급자 이름과 연결 해제된 계정 수를, API Key 폐기는 행을 남겨두어 ID가 계속 풀립니다. 이 둘만 예외였습니다.
- Realm Role과 Client Role을 **함께** 고쳤습니다. 하나만 고치면 "삭제할 때 무엇을 남길 가치가 있는가"에 대해 둘이 서로 다른 답을 갖게 됩니다. 배정 삭제는 연쇄에 맡기지 않고 같은 트랜잭션 안에서 명시적으로 수행하며, 그래야 인원 수가 추정이 아니라 정확한 값이 됩니다.

## v0.9.15

**동작은 달라지지 않습니다.** Realm 경계를 **경로 하나하나에 대해** 확인합니다 — 오늘 기준으로 전부 지켜지고 있습니다. 고친 것은 없습니다. 이미 v0.9.14를 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **Realm이 경로에 담긴 모든 라우트가 그 경계를 지키는지 확인합니다.** 각 라우트는 호출자가 경로에 적힌 Realm에 작용해도 되는지를 스스로 판단합니다. 하나가 잊으면 **한 Realm의 관리자가 다른 테넌트의 사용자 목록을 받습니다** — 이 제품이 존재하는 이유인 그 경계인데, 경로별로 확인하는 것이 없었습니다.
- 기대치를 적어두지 않고 **동작에서 뽑습니다.** 같은 세션이 같은 라우트를 두 번 묻습니다 — 한 번은 자기 Realm, 한 번은 남의 Realm. 자기 Realm에 작용하는 라우트는 남의 Realm에서 반드시 거절되어야 하며, 그 수가 몇 개였는지도 함께 남깁니다(오늘 33개 중 15개).
- "거절되지 않았는가"로 셌다면 **거의 아무것도 확인하지 못했을 것입니다.** 이 경로들 대부분은 하위 자원 ID도 함께 받는데, 없는 ID는 어느 Realm을 적든 404라서 18개는 경계가 아니라 ID 때문에 통과했을 것입니다. 자기 Realm에서 그 라우트가 실제로 무엇을 하는지 먼저 물어 그 18개를 제외합니다.

## v0.9.14

**동작은 달라지지 않습니다.** 상태를 바꾸는 모든 API 경로가 CSRF 미들웨어 뒤에 있는지 확인합니다 — **오늘 기준으로 33개 전부 그렇습니다.** 고친 것은 없고, 그 답이 조용히 바뀌지 않게 합니다. 이미 v0.9.13을 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **CSRF 보호가 라우트를 어디에 등록했는지에 달려 있는데, 그것을 확인하는 검사가 없었습니다.** 토큰 검사는 브라우저 세션을 푸는 미들웨어 한 곳에서 이뤄지므로 핸들러가 잊을 자리는 없습니다. 하지만 **그 그룹 밖에 등록된 라우트는 미들웨어에 닿지도 않고**, 아무도 그 사실을 말해주지 않았습니다.
- 이제 `/api` 아래에서 무언가를 바꾸는 모든 메서드에 대해, 유효한 세션 쿠키를 들고 토큰 없이 보낸 요청을 거절하는지 확인합니다. 보호 그룹 밖에 변경 라우트를 하나 등록해보면 그 이름과 함께 실패합니다. 로그인은 정의상 예외이고(아직 지킬 세션이 없습니다), 로그아웃은 예외가 아니라 포함됩니다. `/api` 밖은 Bearer 토큰 표면이라 다른 사이트가 빌려 쓸 주변 자격증명이 없습니다.

## v0.9.13

**동작은 달라지지 않습니다.** 배포가 정상인지 확인하는 스모크 테스트가 **콘솔은 한 번도 열어보지 않았습니다** — 콘솔이 완전히 빈 이미지도 "통과"라고 답했습니다. 이미 v0.9.12를 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **스모크 테스트가 자기가 정상이라고 말하는 콘솔을 실제로 엽니다.** 스크립트의 모든 검사가 콘솔을 지나쳐 API로 향했고, 그래서 **관리자가 가장 먼저 여는 화면이 유일하게 아무도 보지 않는 화면**이었습니다. 콘솔 빌드를 돌리지 않고 만든 이미지는 문서 한 장만 싣고(`webui/dist`는 `index.html`을 빼고 무시됩니다) 그 문서가 가리키는 스크립트 없이도 멀쩡히 응답합니다. 바로 그 상태에 대고 돌려보니 스크립트는 그대로 `ReSSO smoke test passed`를 찍었습니다.
- 이제 문서에게 어느 번들을 쓰는지 묻고(해시는 빌드마다 바뀝니다) 그것을 가져옵니다. 없으면 그 이름으로 실패하고, JavaScript가 아닌 것으로 답해도 실패합니다 — 뒤쪽은 v0.9.8 이전에 catch-all이 브라우저에게 문서 자체를 200으로 건네주던 모습입니다.

## v0.9.12

**설정 파일이 아무 일도 하지 않는 값을 권하지 않습니다.** `.env`에 `LISTEN_ADDRESS`를 적어도 compose 배포에는 전달되지 않았고, 오류도 나지 않아 **바꿨다고 믿게 됩니다.**

### 운영

- **`.env.example`이 권하는 설정은 실제로 배포에 닿습니다.** README는 `.env.example`을 복사해 `.env`로 쓰라고 안내하므로 거기 적힌 이름은 전부 "설정해도 된다"는 초대입니다. `LISTEN_ADDRESS`는 그 안에 있었지만 어느 compose 파일도 컨테이너로 넘기지 않았습니다 — 값은 받아들여지고, 아무 말도 없고, 배포는 그대로 `:8080`에 바인딩합니다.
- 전달하는 것이 답은 아니었습니다. **컨테이너 안에서 바인딩 주소는 정답이 하나**이고, 이걸 움직이게 두면 게시 포트가 함께 맞춰야 할 두 번째 값이 됩니다. 운영자가 실제로 바꾸고 싶은 것은 호스트가 여는 포트이므로 그것을 `PUBLISHED_PORT`로 제공하고, `LISTEN_ADDRESS`는 적용되는 자리 — 바이너리를 직접 실행할 때 — 에 문서화했습니다.
- `.env.example`이 어느 compose 파일도 읽지 않는 이름을 권하면 그 문장으로 실패하는 가드를 넣었습니다.

### Upgrade notes

기존 `.env`에 `LISTEN_ADDRESS`를 적어두셨다면 지금까지도 효력이 없었으므로 동작은 달라지지 않습니다. 호스트 포트를 바꾸려면 `PUBLISHED_PORT`를 쓰세요.

## v0.9.11

**동작은 달라지지 않습니다.** 테스트가 어떤 PostgreSQL에서 도는지가 세 곳에 따로 적혀 있었고 서로 달랐습니다. 이미 v0.9.10을 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **모든 검사가 같은 PostgreSQL에서 돕니다.** 테스트가 쓰는 데이터베이스는 세 곳에 이름이 적혀 있습니다 — 로컬 서비스를 띄우는 스크립트, 릴리즈 워크플로의 service 블록, README의 수동 절차. 스크립트만 새 메이저(17)로 옮겨가고 나머지 둘은 처음 적힌 16에 남아 있었습니다. **모든 병합은 17에서 통과하는데, 이미지가 나가기 직전 마지막 관문인 백엔드 전체 스위트는 16에서 돌았습니다** — 둘 사이에 차이가 있는 변경은 손 쓸 시간이 없는 자리에서 처음 만나게 됩니다. README를 따라간 개발자는 또 다른 자리에서 돌렸습니다.
- 세 곳이 같은 이미지를 가리키도록 맞추고, 다시 어긋나면 그 문장으로 실패하는 가드를 넣었습니다. 어느 메이저가 옳은지는 결정할 문제지만, 셋을 동시에 두는 것은 결정이 아닙니다.

## v0.9.10

**오프라인 설치 안내대로 따라 하면 인터넷을 찾지 않습니다.** 두 compose 파일이 아홉 릴리즈 전 이미지(`resso:v0.2.1`)를 손으로 고정한 채였고, 릴리즈 아카이브는 자기가 빌드한 버전으로 저장됩니다. 이미지를 로드한 뒤 compose를 올리면 **없는 태그를 찾다가 네트워크로 나갔습니다** — 오프라인 설치가 할 수 없는 단 하나의 일입니다.

### 운영

- **`compose.offline.yaml`과 `compose.maintenance.yaml`이 이 릴리즈의 이미지를 가리킵니다.** README의 시작 절차는 "새 `resso:vX.Y.Z` 이미지를 로드"한 뒤 `compose.offline.yaml`을 올리라고 안내하는데, 그 파일은 `resso:v0.2.1`을 찾고 있었습니다. 태그는 이제 이 릴리즈를 기본값으로 갖는 변수(`RESSO_VERSION`)라, 방금 `docker load`한 아카이브가 안내 그대로 실행되고, 롤백할 때만 다른 값을 지정하면 됩니다.
- **정비용 파일이 더 나빴습니다.** 콘솔에서 잠긴 사람이 실행하라고 안내받는 파일이라, **다른 길이 없는 바로 그 순간에** 실패합니다. 게다가 그 안내가 부르는 `admin recover` 명령은 `v0.2.1`에는 존재하지도 않았습니다.
- **다시 어긋나지 않도록 대조합니다.** 두 기본값이 CHANGELOG의 최신 릴리즈와 일치하는지 확인합니다 — 손으로 고정해 둔 것이 이렇게 표류했으므로, 이제 릴리즈가 Makefile·콘솔 package 버전을 옮기듯 이 값도 함께 옮겨야 합니다.

## v0.9.9

**MCP로 동기화 상태를 물으면 실패한 이유와 비활성화된 계정 수가 함께 옵니다.** 지금까지는 "실패했다"까지만 답하고, 그 실행이 무엇을 했는지는 알려주지 않았습니다.

### 운영

- **`resso_list_user_federations`가 기록 전체를 돌려줍니다.** 이 도구는 돌려줄 필드를 하나씩 손으로 베껴 담았고, 그 사본이 기록보다 뒤처졌습니다. 동기화를 확인하러 온 에이전트는 `last_sync_status`만 받고 **왜 실패했는지(`last_sync_error`)는 듣지 못했으며**, 그 실행이 비활성화한 계정 수(`last_sync_disabled`)도 알 수 없었습니다 — `DISABLE` 정책에서 그 사람들의 세션을 끝내는, 관리자가 바로 이 필드들을 보러 오는 숫자입니다. 매핑이 가리키는 없는 Role(`last_sync_unknown_roles`)과 그룹을 가진 사용자 수(`last_sync_group_memberships`)도 마찬가지였습니다.
- 사본은 아무것도 감추고 있지 않았습니다. **기록에는 감출 자격증명이 없습니다** — bind 비밀번호는 불리언(`bind_credential_set`)이고, 같은 `admin:read` 뒤의 REST 목록은 이미 기록 전체를 콘솔에 넘깁니다. 그래서 이 목록은 **뒤처지는 것 말고는 할 수 있는 게 없는 중복**이었고, 실제로 뒤처졌습니다. 이제 REST와 같은 것을 돌려주므로 다시 어긋날 자리가 없습니다.

## v0.9.8

**콘솔이 빈 화면으로 뜨면 그 이유가 드러납니다.** 번들 파일이 없을 때 브라우저는 200 OK로 HTML을 받아 스크립트로 실행하려다 실패했고, 네트워크 탭에는 모든 요청이 성공으로 찍혔습니다.

### 운영

- **없는 자산 요청에 콘솔 문서를 돌려주지 않습니다.** 콘솔은 catch-all로 서비스됩니다 — 번들에 없는 경로는 `/admin/users` 같은 딥링크가 브라우저 라우터에 닿도록 `index.html`로 되돌려줍니다. 그런데 이 규칙이 **해시가 붙은 자산 파일에도 그대로 적용되어**, 스크립트를 요청한 브라우저가 HTML을 200으로 받았습니다. 파싱은 실패하고 화면은 비어 있는데, **사람이 들여다볼 유일한 자리인 네트워크 탭은 전부 성공이라고 말합니다.** 이제 파일 이름 꼴(경로에 `.`이 있는)의 요청이 없으면 404로 답합니다. 콘솔 자신의 경로는 그대로 문서를 받습니다.
- `index.html`이 자기가 가리키는 번들보다 오래 사는 상황은 가정이 아닙니다 — 배포 직후 이전 문서를 들고 있는 캐시나 프록시가 사라진 해시를 요청하고, `webui/dist/*`는 `index.html`만 빼고 무시되므로 콘솔을 빌드하지 않고 만든 바이너리는 문서만 싣습니다.

## v0.9.7

**검색이 제 몫을 하도록 다듬었습니다.** 사용자·Client 목록에서 입력한 검색어가 주소창에 남지 않아 걸러낸 화면을 공유할 수 없었고, 세션 검색은 아무것도 찾지 못했을 때 **Realm에 세션이 없다고** 답했습니다.

### 사용성

- **사용자·Client 목록의 검색어가 주소에 남습니다.** 지금까지 주소창의 `?q=`는 명령 팔레트가 넣어줄 때만 채워졌고, 직접 입력한 검색어는 화면 안에만 있었습니다. "잠긴 계정" 필터는 이미 주소에 남아 대시보드가 곧장 링크하는데, 정작 **검색 결과는 옆자리에 보여줄 방법이 없었습니다.** 이제 입력이 멎으면(0.3초) 검색어가 주소에 반영되어, 걸러낸 목록 그대로 링크가 됩니다.
- **팔레트가 건네는 검색어가 항상 목록에 적용됩니다.** 팔레트로 `alice`를 열어둔 채 검색창에 다른 말을 입력한 뒤, 팔레트에서 다시 `alice`를 고르면 **입력한 말이 그대로 남아 있었습니다** — 주소창의 `?q=alice`가 처음부터 끝까지 그대로여서 화면이 볼 때 아무것도 바뀌지 않았기 때문입니다. 검색어가 주소를 따라 움직이게 되면서 이 자리가 함께 닫혔습니다. v0.6.1에서 "히스토리와 디바운스가 얽힌 더 큰 변경"이라며 남겨둔 한계였고, 그 뒤 감사 화면 행위자 필터에서 검증한 방식을 그대로 적용했습니다.
- **세션 검색이 아무것도 찾지 못하면 그렇게 말합니다.** 지금까지는 **"세션이 없습니다"**라고 답했습니다 — Realm에 열린 세션이 하나도 없다는 뜻으로 읽히고, 실제로는 검색어가 빗나갔을 뿐인데 운영자를 엉뚱한 곳으로 보냅니다. v0.7.2에서 세션 검색을 서버로 옮기면서 돌아온 행이 곧 전체 답이 됐는데, "조건에 맞는 세션이 없습니다" 분기는 화면 안에서 거르던 시절의 조건을 그대로 두어 **닿을 수 없게 됐습니다.** 감사 화면은 같은 자리를 이미 옳게 구분하고 있었습니다.

## v0.9.6

**동작은 달라지지 않습니다.** 앞으로 조용히 어긋날 수 있는 세 자리를 막고, 아무 일도 하지 않던 분기를 지웠습니다. 이미 v0.9.5를 쓰고 계시다면 서두를 이유가 없습니다.

### 개발

- **문서가 안내하는 복구 명령을 코드가 아니라 코드의 사본과 대조하던 가드**를 고쳤습니다. 이 검사의 존재 이유는 "문서가 시키는 명령을 바이너리가 받지 않는" 상황을 막는 것인데, 정작 대조 대상이 **테스트 안에 옮겨 적은 목록**이었습니다. 명령 이름을 바꾸면 소스·문서·그 목록이 서로 일치한 채 통과하고, **잠겨서 안내를 따르는 운영자만 거절당합니다.** 이제 실제 dispatch에서 읽으며, 명령 이름을 바꿔보면 그 문장으로 실패합니다 — 바꾸기 전에는 아무것도 실패하지 않았습니다. 다른 가드들은 이미 소스나 살아 있는 라우터를 읽고 있어 이 하나뿐이었습니다.
- **Go와 Node 버전이 빌드 곳곳에서 어긋나지 않도록** 확인합니다. `go.mod`가 Go 버전을 정하고 두 워크플로가 거기서 해석하는데, Dockerfile은 자기 베이스 이미지를 따로 적습니다. 표준 라이브러리 수정 때문에 toolchain을 올리고 Dockerfile을 잊으면 **모든 테스트는 새 Go에서 돌고 배포되는 바이너리만 옛 Go로 빌드됩니다.** v0.9.5에서 고친 `ARG VERSION` 표류가 정확히 이 방식으로 일어났습니다.
- `adminCreateUser`에서 아무 일도 하지 않던 분기를 제거했습니다 — `ErrInvalidInput`을 검사해 부르는 함수와 그 다음 줄에서 부르는 함수가 같았습니다.

## v0.9.5

`docker build .`로 만든 이미지가 **다섯 릴리즈 전 버전이라고 답하고** 있었습니다.

### 운영

- **`docker build .`로 만든 이미지가 다섯 릴리즈 전 버전이라고 답하던 문제**를 수정했습니다. Dockerfile의 `ARG VERSION` 기본값이 `v0.4.1-dev`에 멈춰 있었습니다. `make image`와 릴리즈 워크플로는 값을 명시적으로 넘기므로 배포본은 옳았지만, 인자 없이 빌드하면 이미지 라벨(`org.opencontainers.image.version`)과 `/api/v1/meta`가 **v0.4.1-dev**라고 말합니다. 이미지를 디스크에 담아 기계 사이로 옮기는 오프라인 설치에서 그 라벨은 **내가 무엇을 들고 있는지 알려주는 유일한 표시**입니다.
- 기본값을 표류할 수 없는 자리표(`v0.0.0-dev`)로 바꿨습니다 — 실제 번호를 적어두면 다음 릴리즈가 나가는 순간 낡습니다. 그리고 기본값이 **릴리즈처럼 보이지 않는지**를 CHANGELOG의 릴리즈 목록과 대조해 확인합니다. 누군가 다시 실제 번호를 넣으면 그 문장으로 실패합니다.

### 개발

- `adminCreateUser`에서 아무 일도 하지 않던 분기를 제거했습니다. `ErrInvalidInput`을 검사해 `writeStoreError`를 부르고 그 다음 줄에서 나머지에 대해 같은 호출을 했는데, `writeStoreError`가 이미 구분하므로 응답이 달라진 적이 없습니다. 전수로 훑어 이 하나뿐이었습니다.

## v0.9.4

**Realm은 만들어졌는데 감사 기록이 한 줄도 남지 않을 수 있었습니다** — 테넌트가 언제 생겼는지 1년치 기록이 답하지 못합니다.

### 보안

- **Realm은 만들어졌는데 감사 기록이 한 줄도 남지 않을 수 있던 문제**를 수정했습니다. Realm 생성은 쓰기 두 번입니다 — Realm, 그리고 그 첫 서명 키. 두 번째가 실패하면 핸들러가 500을 답하고 **기록하는 줄에 닿기 전에 빠져나갔습니다.** 첫 번째 쓰기 시점부터 Realm은 존재하므로, 감사 기록에는 **"이 테넌트가 언제 생겼는가"에 대한 답이 없고** 유일한 흔적은 30일 보관하는 서버 로그입니다(감사는 1년입니다).
- 이 서비스가 이미 한 번 고친 모양입니다 — 관리자가 Session을 강제 종료했을 때 감사 기록이 남지 않던 문제와 같습니다. 이제 `PARTIAL`로 기록하며 어느 쪽이 실패했는지(`signing_key: not_created`)를 함께 남깁니다. 응답은 그대로 "Realm은 생성되었으나 서명 키를 만들지 못했습니다"입니다.
- 같은 모양이 다른 핸들러에도 있는지 전수로 훑었습니다 — **이 하나뿐입니다.** 나머지의 이른 반환은 모두 변경 자체가 실패한 경우(기록할 것이 없음)이거나, 이미 부분 실패를 구분해 기록하는 경로였습니다.

## v0.9.3

운영자가 **경보로 옮겨 적는** 지표 이름이 실제로 존재하는지 확인합니다.

### 개발

- **운영 문서가 경보로 안내하는 지표 이름에 가드가 없던 것**을 채웠습니다. README의 지표 표는 양방향으로 검사되고 있었지만, 운영자가 실제로 **경보로 옮겨 적는** 이름은 운영 문서의 대응 절에 있습니다. 오늘은 그 여덟 개가 모두 README에도 있어 이름을 바꾸면 기존 검사가 실패하지만, 그것을 고친 사람이 **운영 문서까지 고칠 것이라는 기대**에 기대고 있었습니다. 기대를 대신하는 것이 가드의 일입니다 — 없는 이름으로 만든 경보는 영영 울리지 않으면서 설정된 것처럼 보입니다.
- 이제 운영 문서가 이름 붙인 지표도 실제로 `/metrics`가 내보내는지 확인합니다. 감사 이벤트 표와 같이 **한 방향만** 봅니다 — 그 절은 목록이 아니라 볼 만한 것의 선별이기 때문입니다.

## v0.9.2

문서가 **열거한** Realm 정지의 약속을 HTTP 수준에서 확인합니다 — 사양상 거절하지 않아야 하는 두 endpoint까지 그 의미로 구분해서.

### 개발

- **Realm 정지가 무엇을 멈추는지에 대한 문서의 약속에 가드가 없던 것**을 채웠습니다. 운영 문서는 Discovery·JWKS·인가·Token·userinfo·Introspection·Revocation이 모두 거절한다고 **열거**하는데, 그것을 HTTP 수준에서 확인하는 테스트가 없었습니다. 실제 구현은 한 함수(`realmFromPath`)가 결정하고 프로토콜 핸들러가 모두 그것을 지나므로 구조는 옳았지만, **나중에 추가되는 경로가 Realm을 자기 방식으로 조회하면** 문서가 계속 약속하는 동안 정지된 테넌트를 서비스하게 됩니다. 이제 정지 후 모든 경로를 실제로 호출해 확인하고, 그 함수에서 확인을 빼면 네 곳이 실패합니다.
- 다만 **Introspection과 Revocation은 거절하지 않는 것이 맞습니다.** RFC 7662는 알 수 없는 Token에 `{"active": false}`를, RFC 7009는 200을 답하도록 하는데, 두 endpoint가 "무엇이 존재하는가"를 묻는 통로가 되지 않게 하기 위해서입니다. **정지된 Realm은 아무것도 없는 Realm과 똑같이 보여야 합니다.** 테스트는 이 둘을 예외로 빼는 대신 그 의미를 직접 확인합니다 — 정지 확인을 우회시키면 Introspection이 401을 답해 Realm의 존재를 드러내고, 테스트가 그것을 잡습니다.

## v0.9.1

서명 키를 잃은 Realm이 **Token이 잘못됐다고 답해**, 잠깐의 장애가 전원 로그아웃이 될 수 있었습니다.

### 보안

- **서명 키가 없는 Realm이 "Token이 잘못됐다"고 답하던 문제**를 수정했습니다. 서명할 키가 없으면 토큰을 발급할 수 없는데, 그 실패가 다른 원인들과 뭉쳐 **`invalid_grant`**로 나갔습니다 — 게다가 문구는 "authorization session is no longer active"였습니다. 세션은 멀쩡한데 말입니다.
- 이것이 위험한 이유는 **RP가 `invalid_grant`에 반응하는 방식** 때문입니다. 표준적인 처리는 그 Refresh Token을 **버리고** 사용자를 다시 로그인시키는 것인데, 로그인 역시 키가 없으면 안 됩니다. 그래서 키를 잃은 Realm은 복구된 뒤에 **살아남을 수 있었던 세션들이 이미 버려진 상태**로 돌아옵니다. 잠깐의 장애가 전원 로그아웃이 됩니다. 대시보드에 `ACTIVE 키 누락` 준비 상태 행이 있다는 것은 이 상태가 실제로 일어난다는 뜻입니다.
- 이제 `500 server_error`와 "이 Realm에 활성 서명 키가 없어 Token을 발급할 수 없습니다"로 답합니다. Refresh Token도 소모되지 않습니다.
- **Refresh Token 재사용 기록에 계정 이름이 비어 있던 문제**도 수정했습니다. 회전된 Token이 다시 제시되는 것은 이 서비스가 가진 **토큰 탈취의 가장 강한 신호**이고, 운영 문서는 감사 화면에서 이 이벤트를 찾으라고 안내합니다. 그런데 그 기록만 행위자 칸이 비어 있어 **화면에 계정이 나오지 않고, 계정으로 좁히면 영영 나오지 않습니다.** 계정은 바로 몇 줄 위에서 이미 조회해 둔 것이었습니다.

## v0.9.0

**비활성화한 계정의 API 키가 목록에서는 계속 `활성`으로** 보였습니다. 비상 정지 뒤에 확인하는 화면이 반대로 말하고 있었습니다.

### 보안

- **비활성화한 계정의 API 키가 목록에서는 계속 `활성`으로 보이던 문제**를 수정했습니다. "이 키가 아직 유효한가"를 두 곳이 답하는데 질문이 서로 달랐습니다 — 목록은 **폐기·만료**만 보고, 인증은 거기에 **계정 활성·Realm 활성**까지 요구합니다. 계정 비활성화는 이 서비스의 비상 정지이고, 운영자가 그 뒤에 확인하는 화면이 **키가 아직 살아 있다고** 말하고 있었습니다. 실제로는 이미 멈춘 뒤인데 말입니다.
- 세션이 이미 쓰고 있던 방식대로, 하나의 술어를 세 곳이 공유합니다. 목록만 짧은 질문으로 되돌리면 테스트가 실패합니다.
- **왜 멈췄는지도 서버가 말합니다.** 화면은 가진 필드로 이유를 추측하고 있었는데, 그러면 계정 비활성으로 멈춘 키를 **`만료`라고 부릅니다** — 갱신해도 동작하지 않을 키를 갱신하러 보내는 셈입니다. 이제 `폐기`·`만료`·`계정 비활성`·`Realm 정지`를 구분하며, 만료 판정은 **그 시각을 쓴 시계**(데이터베이스)가 합니다. 브라우저 시계로 계산하지 않습니다.

## v0.8.8

같은 사실을 읽는 세 곳 중 **실행을 시작해도 되는지 실제로 결정하는 한 곳**만 테스트가 없었습니다.

### 개발

- **중단된 동기화가 다시 시작될 수 있는지를 확인하는 테스트가 없던 것**을 채웠습니다. "동기화가 돌고 있는가"는 30분 stale 창을 기준으로 판정하고, 그 창을 읽는 곳이 **세 군데**입니다 — 목록에 실리는 값, 시작 전 검사, 그리고 **실제로 잠금을 얻는 UPDATE**. 앞의 둘은 테스트가 지키고 있었지만, **실행을 시작해도 되는지를 실제로 결정하는 세 번째는 지켜지지 않았습니다.**
- 거기서만 창이 빠지면 콘솔은 "돌고 있지 않다"며 버튼을 열어 주고 서버는 요청을 거부합니다 — 그 테스트가 존재하는 이유인 불일치가, 반대 방향으로 되살아납니다. 이제 버려진 실행을 실제로 이어받을 수 있는지까지 확인하며, 잠금 쪽에서만 창을 제거하면 실패합니다.

## v0.8.7

릴리즈 관문의 검사 하나가 **계속 초록이면서 확인하는 것만 사라져** 있었습니다 — v0.8.0에서 응답 모양을 바꾸며 제가 만든 것입니다.

### 개발

- **릴리즈 관문의 검사 하나가 제 손으로 약해져 있던 것**을 되돌렸습니다. 스모크 테스트는 MCP 사용자 검색이 "결과를 냈는지"를 `structuredContent | length > 0`으로 확인했습니다. v0.8.0에서 그 도구의 응답을 배열에서 객체로 바꾸면서, jq의 `length`가 **객체에서는 키 개수**가 되어 **0건이어도 통과**하게 됐습니다. 검사는 계속 초록이었고 확인하던 것만 사라졌습니다.
- 이제 `items`의 개수를 보고, `matched`가 돌려준 수 이상인지와 `truncated`가 실려 있는지도 확인합니다. 잘린 답이 잘렸다고 말하지 않는 것이 v0.8.0에서 고친 결함이므로, 관문이 그것까지 지키게 했습니다. 옛 검사는 0건 응답에서 통과하고 새 검사는 실패하는 것을 직접 확인했습니다.
- 스모크 테스트를 로컬에서 실제로 돌려 통과를 확인했습니다 — 이 스크립트는 릴리즈에서만 실행되므로, 여기 손을 대면 그것으로 검증해야 합니다.

## v0.8.6

`연결 테스트`는 **속성을 하나도 요청하지 않으면서** 성공을 알리고 있었습니다. `mail`을 `maill`로 적어도 통과하고, 모든 사용자가 이메일 없이 들어옵니다.

### 운영

- **`연결 테스트`가 무엇도 확인하지 않으면서 성공을 알리던 문제**를 수정했습니다. 이 검사는 bind하고 Users DN이 존재하는지만 보았고, **속성은 하나도 요청하지 않았습니다**(검색이 `1.1`로 "속성 없음"을 요청). 그래서 `mail`을 `maill`로 적어도 검사는 통과하고, 동기화는 성공하고, 모든 사용자가 **이메일 없이** 들어옵니다. 속성 이름의 *형식*은 저장할 때 이미 검사하지만, 그 이름이 **이 디렉터리에 실제로 있는지**는 어디에서도 보지 않았습니다.
- 이제 표본 5명을 실제로 읽어, 설정한 각 속성이 몇 명에게서 값을 냈는지 함께 돌려줍니다. 전부 비어 있는 속성이 있으면 화면이 이름을 지목하고, 조건에 맞는 사용자를 한 명도 찾지 못하면 그 사실을 알립니다(Users DN·검색 범위·Object Class·필터를 확인하도록). 모두 값이 있으면 그렇게 말합니다 — 그래야 경고가 떴을 때 의미가 있습니다.
- 사용자 목록을 읽지 못하더라도 **bind와 DN이 확인됐다는 사실은 그대로 보고합니다.** 그 둘을 한꺼번에 실패로 만들면 이미 확인된 것까지 가려집니다.

## v0.8.5

Role 이름이 전부 맞는데도 아무도 Role을 받지 못하는 경우가 있습니다 — **디렉터리가 그룹 소속을 아예 돌려주지 않을 때**입니다. 이제 그 둘을 구분해 알립니다.

### 운영

- **매핑이 옳은데도 아무 Role이 부여되지 않는 경우를 구분할 수 없던 문제**를 수정했습니다. v0.8.4는 매핑이 *없는 Role*을 가리키는 경우를 지목합니다. 그런데 Role 이름이 전부 맞아도 아무도 받지 못하는 다른 원인이 있습니다 — **디렉터리가 그룹 소속을 아예 돌려주지 않는 경우**입니다. `memberOf` 속성 이름이 틀렸거나, 그 속성을 만들어 주는 overlay가 꺼져 있을 때입니다(OpenLDAP은 기본으로 켜지지 않습니다). 어느 쪽이든 동기화는 성공으로 끝나고, 운영자는 이미 맞는 Role 이름을 다시 확인하며 시간을 씁니다.
- 이제 실행마다 **그룹 소속을 가진 사용자가 몇 명이었는지** 세어 요약·감사 기록·공급자 행에 남깁니다. 매핑이 설정돼 있는데 그 수가 0이면 화면이 그 사실과 확인할 곳을 알려줍니다. 소속이 하나라도 있었다면 아무 말도 하지 않으므로, 문구가 뜨면 그것이 원인입니다.
- 이 서비스의 테스트 디렉터리는 memberof overlay를 켜 두면서도 **그룹 소속이 실제로 넘어오는지 확인하는 테스트가 없었습니다.** 이제 실제 그룹을 만들어 소속이 도착하고 매핑된 Role이 실제로 부여되는 것까지 확인합니다.

## v0.8.4

Group → Role 매핑이 **없는 Role을 가리켜도 동기화는 성공으로 끝났습니다.** 그 그룹 사람들은 Role을 받지 못하고, 어느 규칙이 죽었는지 아무 데도 나오지 않았습니다.

### 운영

- **Group → Role 매핑이 없는 Role을 가리켜도 아무도 모르던 문제**를 수정했습니다. 매핑은 Role 이름을 **자유 입력**으로 받고 적힌 그대로 씁니다. 이 Realm에 없는 이름 — 오타이거나, 매핑을 만든 뒤 삭제된 Role — 은 조회에서 빠질 뿐이라 **동기화는 성공으로 끝나고 그 그룹 사람들은 Role을 받지 못합니다.** 규칙은 설정에 그대로 남아 있고, 어느 규칙이 죽었는지 아무 데도 나오지 않으니 아무도 고치지 않습니다.
- 실행마다 해석되지 않는 이름을 모아 요약·감사 기록·공급자 행에 남기고, 화면에서 경고로 보여줍니다. 사용자별이 아니라 **실행당 한 번** 확인합니다 — 매핑은 공급자의 설정이지 누군가의 속성이 아닙니다. 매핑이 모두 해석되면 아무 말도 하지 않으므로, 문구가 뜨면 그것은 실제 문제입니다.

### 개발

- 콘솔 테스트 중 가장 느리던 **LDAP 공급자 폼 테스트를 15.2초에서 0.4초로** 줄였습니다. 긴 값을 한 글자씩 타이핑하고 있었는데, 확인하려는 성질은 "긴 값이 받아들여지고 유지되는가"이지 "100번의 키 입력마다 다시 그려지는가"가 아닙니다. 붙여넣기로 바꿨습니다 — 연결 URL이나 DN을 다루는 실제 방식이기도 합니다. 이 테스트는 예전에 한 번 CI 안정화 대상이었고, 부하가 걸린 러너에서 자기 타임아웃(15초)마저 넘고 있었습니다.

## v0.8.3

동기화가 **몇 명의 세션을 끊었는지**를 정작 그 결과를 확인하라고 안내한 화면에서 볼 수 없었습니다.

### 운영

- **동기화가 몇 개의 계정을 비활성화했는지를 정작 그 화면에서 볼 수 없던 문제**를 수정했습니다. `DISABLE` 정책의 실행은 디렉터리에서 사라진 계정을 비활성화하고 **그 사람들의 세션을 끊습니다** — 한 번의 동기화에서 결과가 가장 큰 숫자입니다. 그런데 공급자 행에 남는 것은 추가·갱신·실패뿐이었고, 콘솔은 운영자를 바로 그 필드들로 안내합니다. 결국 **확인하라고 보낸 자리에 확인해야 할 것이 없었습니다.**
- 이 숫자가 사라졌던 것은 아닙니다 — 감사 이벤트에는 남고, 그 코드의 주석이 이유까지 적어두고 있습니다. 다만 동기화는 비동기로 돌아 요약을 받을 호출자가 없고(예약 실행은 더욱), 화면이 읽는 곳은 공급자 행입니다. 이제 `last_sync_disabled`로 함께 저장되고 화면의 `추가 · 갱신 · 실패` 옆에 **비활성화**로 표시됩니다.
- v0.5.0에서 고친 결함 — `DISABLE` 정책이 조직 전체를 비활성화할 수 있던 문제 — 이 다시 일어난다면 운영자가 가장 먼저 봐야 할 숫자가 이것입니다.

### Upgrade notes

- 마이그레이션 `008_federation_disabled_count.sql`이 `user_federations`에 컬럼 하나를 더합니다(기본값 0). 기존 공급자는 다음 동기화부터 값이 채워집니다.

## v0.8.2

이 저장소가 프런트엔드에서 이미 해결해 둔 함정이 백엔드 도구에 그대로 남아 있었습니다 — **확인 명령이 무엇을 설치하라는 말 없이 `command not found`로 끝나는 것.**

### 개발

- **`make lint`이 도구가 없으면 `command not found`로 끝나던 문제**를 고쳤습니다. 이 Makefile은 이미 같은 함정을 프런트엔드에서 다뤘습니다 — `web/node_modules` 타깃의 주석이 *"신선한 clone이 'vitest: not found'를 만난다"*고 적고 있습니다. 백엔드 쪽 두 도구(`golangci-lint`, `govulncheck`)는 그대로 남아 있었고, 둘 다 `GOBIN`에 설치되는데 그 경로는 기본 PATH에 없습니다. 이제 없는 것을 **한 번에 이름으로 알리고** 설치 명령과 PATH 설정을 함께 보여주며, 실행할 때도 `GOBIN`을 앞에 붙입니다.
- **로컬 linter 버전이 CI와 다를 수 있던 문제**도 함께 막았습니다. 버전은 `ci.yaml`에만 적혀 있었으므로, 다른 버전을 깐 사람은 `make lint`가 통과하고 푸시가 실패하며 **로컬 바이너리가 한 번도 언급한 적 없는 규칙 이름**을 받게 됩니다. Makefile이 같은 버전을 명시하고, 두 파일이 어긋나면 테스트가 실패합니다.

## v0.8.1

동작이 바뀌지는 않습니다. **검증 없이 나간 변경에 테스트를 붙이고, 어긋나 있던 기록을 바로잡습니다.**

### 개발

- 감사 화면의 결과·행위자 필터를 URL로 옮긴 변경이 **잘림 수정 커밋에 섞여 테스트도 기재도 없이 v0.8.0으로 나갔습니다.** 동작 자체는 옳았지만 그대로 두면 다음 사람이 근거 없이 물려받게 됩니다. 링크로 도착했을 때 걸리는지, 입력한 값이 URL에 실리는지, `초기화`가 셋을 함께 지우는지를 확인하는 테스트를 붙였고, 변경 직전 버전에서는 그중 셋이 실패합니다.
- v0.8.0의 릴리즈 노트에도 그 변경을 추가했습니다 — 무엇이 나갔는지에 대한 기록이 틀려 있었습니다.

## v0.8.0

에이전트가 받는 답에도 같은 문제가 있었습니다 — **잘린 목록이 잘렸다고 말하지 않는 것.** 사람은 화면이 어색하면 의심하지만, 에이전트는 받은 것을 전부라고 읽고 그대로 사람에게 옮깁니다.

### 사용성

- **MCP `resso_search_users`가 20건에서 잘리면서 아무 말도 하지 않던 문제**를 수정했습니다. 이 표면의 다른 도구들(`resso_list_realms`, `resso_list_clients`, `resso_list_user_federations`)은 **찾은 것을 전부** 돌려주므로, 배열을 받은 에이전트가 그것을 전체 답으로 취급하는 것은 합리적입니다. 그래서 "김씨 성을 가진 사람이 몇 명인가"라는 질문에 **300명인데 20명이라고** 답하게 됩니다. 도구 설명에도 상한이 적혀 있지 않았습니다.
- 이제 `items`와 함께 조건에 맞는 전체 수 `matched`, 잘렸는지 `truncated`, 상한 `limit`을 돌려줍니다. 개수는 목록과 **같은 필터**로 셉니다. 도구 설명도 그렇게 적습니다.
- **감사 화면의 필터가 모두 URL에 실립니다.** 이전 릴리즈에서 이벤트 유형만 URL로 옮겼는데, 결과와 행위자는 로컬 상태로 남아 있었습니다. 그래서 걸러진 감사 화면의 주소를 사고 대응 중에 공유하면 **받는 사람에게는 절반만 걸린 다른 목록**이 열립니다. 셋 다 URL에서 읽고, `초기화`는 한 번의 쓰기로 셋을 함께 지웁니다 — 따로 지우면 URL에서 서로 경합해 하나가 남을 수 있습니다.

- **잘렸는지를 화면이 세지 않고 서버가 답합니다.** 세션과 승인 목록은 "받은 행 수가 상한과 같으면 잘린 것"이라고 추측했는데, 이러면 **정확히 상한만큼 있는 Realm에서 숨긴 것이 없는데도 있다고 말합니다.** 늑대를 부르는 알림은 곧 읽히지 않게 됩니다. 이제 두 목록 모두 상한보다 한 건 더 읽어보고 `truncated`로 사실을 돌려줍니다.
- 이 추측은 v0.7.2·v0.7.3에서 제가 넣은 것이고, 500건을 그려서 확인하던 테스트가 **CI에서 5초 시간 초과로 릴리즈를 막았습니다.** 서버가 답하게 바꾸니 그 테스트는 행 두 개로 충분해졌고, 두 화면의 테스트 실행 시간이 5.3초에서 1.3초가 됐습니다.

### Upgrade notes

- **`resso_search_users`의 응답 모양이 바뀝니다.** 이전에는 사용자 배열이었고 이제는 `{items, matched, returned, truncated, limit}` 객체입니다. 이 도구의 결과를 배열로 직접 파싱하는 에이전트나 스크립트가 있다면 `items`를 읽도록 고쳐야 합니다. 다른 MCP 도구의 응답은 그대로입니다.

## v0.7.3

승인 목록이 **잘렸다는 사실을 말하지 않고 있었습니다.** 생략되는 처리 이력은 감사 기록에 남아 있으므로, 그쪽을 가리키되 링크가 걸린 채로 열리게 했습니다.

### 사용성

- **승인 목록이 잘렸다는 사실을 말하지 않던 문제**를 수정했습니다. 목록은 500건에서 잘리고 대기 중인 요청이 먼저 오므로, 생략되는 것은 **오래된 처리 이력**입니다. 아무 말 없이 잘린 목록은 전체 이력처럼 보이고, "반년 전 이 요청을 누가 승인했나"를 찾는 사람은 없는 것으로 결론짓게 됩니다.
- 다만 그 결정들은 **사라진 것이 아닙니다** — `APPROVAL_DECISION` 감사 이벤트로 1년치가 남아 있고 감사 화면은 페이지네이션과 필터를 갖추고 있습니다. 그래서 알림이 그쪽을 가리킵니다.
- 그러려면 그 링크가 **걸린 채로 열려야** 합니다. 감사 화면의 이벤트 유형 필터는 URL이 아니라 로컬 상태였으므로, 링크를 걸어도 필터 없는 전체 목록이 열렸습니다 — 이 릴리즈들에서 반복해 고쳐온 바로 그 모양입니다. 이제 URL에서 읽으므로 링크가 그 유형에 맞춰 열리고, 걸러진 감사 화면을 공유하거나 북마크할 수도 있습니다.

## v0.7.2

세션 검색이 **열려 있는 세션을 "없다"고 답하고 있었습니다.** 화면이 가져온 500건 안에서만 찾았기 때문입니다.

### 사용성

- **세션 검색이 "없다"고 답하는데 실제로는 열려 있던 문제**를 수정했습니다. 세션 화면은 최근 사용 순으로 **500건만** 가져오고, 검색은 **그 500건 안에서만** 이뤄졌습니다. 그래서 501번째 세션을 사용자명이나 IP로 찾으면 화면은 `검색 조건에 맞는 세션이 없습니다`라고 답합니다 — 세션은 열려 있는데 말입니다. 이 화면의 주석은 그 검색을 넣은 이유를 이렇게 적고 있습니다: *"이 사용자가 어떤 세션을 열어두었는지'를 답하려면 Realm 전체를 스크롤해야 했다"*. 정확히 그 질문에 틀린 답을 하고 있었습니다.
- 검색을 서버로 옮겼습니다. 이제 **상한은 보여주는 양을 제한할 뿐 찾을 수 있는 범위를 제한하지 않습니다.** 대소문자를 가리지 않고, 사용자명과 IP 모두를 봅니다. 입력은 디바운스합니다 — 이제 요청이 나가기 때문입니다.
- **잘렸다는 사실도 말합니다.** 500건에 닿으면 그렇게 알리고, 검색으로 그 범위 밖도 찾을 수 있다고 안내합니다. 아무 말 없이 잘린 목록은 전체 목록처럼 보입니다. 서버 로그 화면이 이미 같은 방식으로 알리고 있었고, 이 화면만 조용했습니다.

## v0.7.1

앞 릴리즈에서 대시보드 행을 하나씩 따라가다 마지막 하나가 남아 있었습니다 — **콘솔에서 고칠 수 없는 항목이 고칠 수 있는 것처럼 보이던 문제**입니다.

### 사용성

- **`서버·데이터베이스 시각 차이` 행이 `시각 동기화 확인` 버튼으로 감사 이벤트 화면을 열던 문제**를 수정했습니다. 감사 화면은 시각 동기화를 확인할 수 없습니다. 게다가 이 값은 **대시보드를 열 때만 측정되고** 로그나 지표로 남지 않으므로, 콘솔 안에는 링크할 곳이 애초에 없습니다. 두 시계를 맞추는 일은 호스트에서 하는 것이고 운영 문서가 그렇게 적고 있습니다.
- 다른 행들과 똑같이 생긴 버튼이 도움이 되지 않는 곳으로 데려가는 것은 **버튼이 없는 것보다 나쁩니다.** 이제 이 행은 버튼 대신 실제 조치를 적습니다 — "두 호스트가 같은 시각 소스를 쓰도록 맞추세요".
- 대시보드에는 테스트가 없었습니다. 이제 준비 상태 행이 **문제를 알릴 때 그것을 보여주는 화면으로 연결하는지**, 그런 화면이 없을 때는 조치를 적는지, 정상일 때는 아무것도 권하지 않는지를 확인합니다.

## v0.7.0

대시보드가 "7일 내 만료 API 키 N개"라고 세면서 **그것이 누구 키인지 알 방법이 없었습니다.** 관리자용 API 키 화면을 추가해, 세는 곳과 보는 곳이 같은 술어를 공유하도록 했습니다.

### 사용성

- **대시보드가 "7일 내 만료 API 키 N개"라고 세면서, 그것이 누구 키인지 알 방법이 없던 문제**를 수정했습니다. 그 행의 버튼은 **감사 이벤트**로 보냈는데, 감사 로그는 키에 *무슨 일이 있었는지*를 기록할 뿐 **곧 만료될 키 목록이 아닙니다.** API 키를 보여주는 화면은 `개인 API 키` 하나뿐이고 그것은 **자기 키만** 보여줍니다. 그래서 관리자는 숫자를 읽고도 소유자에게 회전을 요청할 수 없었고, 키는 예고 없이 만료되어 연동을 멈춥니다.
- 이 파일의 주석은 이미 원칙을 적어두고 있었습니다 — "각 준비 상태 행은 그것을 고칠 수 있는 화면으로 연결한다". 이 행만 그것을 어기고 있었습니다.
- 관리자용 `API 키` 화면을 추가했습니다. Realm 구성원이 발급한 키를 **소유자·이름·Prefix·범위·마지막 사용·만료·상태**로 보여주며, 대시보드에서 오면 만료 임박 키만 걸러진 채로 열립니다. **읽기 전용이고 metadata뿐입니다** — Secret은 읽을 수 있는 형태로 저장되지 않으므로 여기에도 나타나지 않습니다.
- 대시보드가 세는 술어와 목록이 고르는 술어는 **같은 상수 하나**입니다. 어느 한쪽에 복사본을 만들면 각자 자기 테스트는 통과하면서 서로 다른 말을 하게 되므로, 두 응답을 서로 비교하는 테스트로 잠갔습니다 — 목록만 30일 창으로 바꾸면 `대시보드는 1개, 링크된 화면은 2개`로 실패합니다.

## v0.6.5

대시보드가 "잠긴 사용자 N명"이라고 말하면서 **그 수를 확인할 방법이 없었습니다.** 링크는 필터 없는 목록으로 보냈고, 그 화면의 필터는 이미 불러온 페이지 안에서만 걸렸습니다.

### 사용성

- **대시보드가 "잠긴 사용자 N명"이라고 말하면서, 그 수를 확인할 방법이 없던 문제**를 수정했습니다. 대시보드에는 `잠긴 사용자 보기` 버튼이 있는데 보내는 곳은 **필터가 전혀 걸리지 않은** 사용자 목록이었습니다. 게다가 그 화면의 상태 필터는 **이미 불러온 페이지 안에서만** 걸렀습니다 — 잠긴 계정이 사용자명 순으로 뒤쪽 페이지에 있으면 필터를 켜도 나오지 않습니다. 3명이라고 적혀 있는데 화면은 0명을 보여주고, 운영자는 페이지를 넘겨가며 찾게 됩니다.
- 필터가 페이지 안에만 걸렸던 이유는 **페이저의 수를 정직하게 유지하려는 것**이었습니다. 실제로는 그 반대가 됐습니다 — 목록은 걸러지고 개수는 걸러지지 않아, 3건을 보여주면서 `1–50 / 1200`이라고 적었습니다. 이제 서버가 목록과 개수에 **같은 술어**를 적용합니다. 하나의 상수를 공유하므로 둘이 갈라질 수 없습니다.
- 상태는 이제 URL에 실립니다. Realm이 URL에 있는 것과 같은 이유입니다 — 대시보드가 곧바로 그 화면을 열 수 있고, 걸러진 목록을 공유하거나 북마크할 수 있습니다. 알 수 없는 값은 필터가 없는 것으로 취급하므로, 오래된 링크가 오류 화면을 만들지 않습니다. 정렬 컬럼이 이미 쓰는 방식과 같습니다.
- 만료된 잠금은 잠금이 아닙니다. `locked_until`을 **그 값을 쓴 시계**(데이터베이스)와 비교하므로, 이미 풀린 계정이 목록에 남지 않습니다.

## v0.6.4

감사 항목에서 그 Trace의 로그로 넘어가는 동작이 **30일치 로그를 전부 훑고** 있었습니다. 콘솔은 자기가 들고 있는 것이 Trace라는 사실을 이미 알면서 서버에 전하지 않았을 뿐입니다.

### 운영

- **감사 항목에서 그 Trace의 로그로 넘어가는 동작이 30일치 로그를 전부 훑던 문제**를 고쳤습니다. 감사 상세에는 "이 Trace의 로그 보기" 버튼이 있고, 넘어간 화면은 그 값을 **자유 검색어**로 취급했습니다. Trace ID는 정확한 식별자인데 선행 Wildcard로 찾으니 색인이 도울 수 없고, 로그 미러는 **모든 요청의 30일치**를 담습니다. 40만 행에서 **163ms 전체 순차 스캔**이었고 행 수에 정비례합니다.
- 콘솔은 자기가 들고 있는 것이 Trace라는 사실을 이미 알고 있었습니다 — URL의 `trace` 파라미터가 현재 검색어와 같은 동안에만 유지하도록 이미 되어 있었으니까요. 그 사실을 서버에 전하지 않고 버리고 있었을 뿐입니다. 이제 그동안은 `trace=`로 정확 일치를 요청하고, 검색어를 고치는 순간 자유 검색으로 돌아갑니다. **화면은 달라지지 않습니다.**
- 정확 일치는 빠르기만 한 것이 아니라 **맞는 답**이기도 합니다. 자유 검색은 그 식별자를 *언급만 한* 줄과, 그것을 접두사로 갖는 **다른** Trace까지 함께 가져왔습니다 — 버튼이 약속한 것이 아닙니다.
- `trace_id`에 부분 색인을 추가했습니다. 대부분의 줄은 Trace를 갖지 않으므로 그것까지 색인하면 아무도 찾지 않을 항목에 비용을 냅니다. 40만 행 기준 **163ms가 0.08ms**가 됩니다. 트라이그램 색인으로도 2.2ms까지는 되지만 색인이 51MB로 이 부분 색인(23MB)의 두 배가 넘어, 오프라인 어플라이언스에는 맞지 않는 거래였습니다.

### Upgrade notes

- 마이그레이션 `007_system_log_trace_index.sql`이 로그 미러에 부분 색인을 하나 만듭니다. 기동 중에 적용되므로 서비스 중 쓰기를 막지 않고 기동 시간에만 더해집니다.

## v0.6.3

사고 조사에서 가장 먼저 하는 질의 — **감사 기록을 특정 계정으로 좁히기** — 가 365일치 테이블을 전부 훑고 있던 것을 고쳤습니다. 이미 있던 검색 색인 장치를 감사 쪽만 쓰지 못하는 형태였습니다.

### 운영

- **감사 기록을 특정 계정으로 좁히는 일이 전체 테이블 스캔이던 문제**를 고쳤습니다. 행위자 필터는 선행 Wildcard(`ILIKE '%…%'`)라 B-tree 색인이 도울 수 없고, 감사 테이블은 **365일치 로그인 기록**을 담습니다. 40만 행에서 항목이 다섯 건뿐인 행위자를 찾는 데 **40만 행을 전부 훑어 34ms**가 걸렸고, 이 비용은 행 수에 정비례합니다. 사고가 나면 가장 먼저 하는 일이 바로 이 질의입니다.
- 이 서비스에는 이미 같은 형태를 위한 장치가 있었습니다 — 사용자 검색용 `pg_trgm` 색인입니다. 다만 그 색인들은 `lower(컬럼)` **표현식** 색인이고 사용자 검색 질의도 `lower(컬럼) LIKE`로 맞춰져 있는 반면, 감사 쪽만 `ILIKE`를 써서 **같은 장치의 혜택을 받을 수 없는 형태**였습니다. 질의를 `lower(actor_name) LIKE lower(...)`로 맞추고 같은 자리에 색인을 추가하니 **34ms가 0.1ms**가 됩니다. 개수 질의도 같은 조건을 쓰므로 함께 빨라집니다.
- 흔한 행위자로 거를 때는 플래너가 그대로 기본 키 역순 스캔을 고릅니다 — 맞는 행이 촘촘해 그쪽이 싸기 때문입니다. 검색어가 비었을 때도 달라지는 것이 없음을 확인했습니다.
- 확장이 없는 데이터베이스는 이전과 똑같이 동작합니다. `pg_trgm`은 여전히 ReSSO가 직접 설치하지 않으며, 설치돼 있으면 다음 기동에 색인이 만들어집니다. README와 운영 문서가 이 확장을 "사용자 검색용"으로만 설명하고 있어 함께 고쳤습니다 — 설치 여부를 판단하는 근거가 달라졌기 때문입니다.

## v0.6.2

v0.6.1이 감사 목록의 정렬을 시계에서 `id`로 옮기면서, **그 정렬을 받쳐줄 인덱스가 없는 상태로** 나갔습니다. 걸러진 목록이 느려지는 회귀라 곧바로 잡았습니다.

### 운영

- **감사 목록의 정렬을 `id`로 옮기면서 걸러진 목록이 느려지던 것**을 함께 고쳤습니다. 기존 복합 인덱스는 `(realm_id, event_type, occurred_at DESC)`로 **이전 정렬을 위해** 만들어진 것이라, 새 정렬에서는 쓰이지 못하고 기본 키를 역순으로 훑으며 맞지 않는 행을 버리는 계획이 선택됐습니다. 40만 행에서 재보면 **0.5%만 맞는 필터가 0.09ms에서 1.57ms로** 늘었고, 이 차이는 테이블이 커지고 찾는 값이 드물수록 벌어집니다. `(realm_id, event_type, id DESC)` 인덱스를 추가하니 **0.12ms**가 되고 index-only scan이라 정렬 단계 자체가 없습니다. 필터 없는 기본 화면도 1.68ms에서 **0.06ms**로, 원래보다 빨라졌습니다.
- 이전 정렬을 위한 복합 인덱스는 이제 아무도 쓰지 않으므로 제거했습니다 — 남겨두면 감사 기록을 쓸 때마다 유지 비용만 냅니다. `occurred_at` 단일 인덱스는 **365일 보존 삭제가 쓰므로 유지**합니다.

### Upgrade notes

- 마이그레이션 `006_audit_order_index.sql`이 감사 테이블에 인덱스를 하나 만들고 하나를 지웁니다. 마이그레이션은 **서비스가 요청을 받기 전** 기동 중에 적용되므로 운영 중 쓰기를 막지는 않고, 기동 시간에 더해집니다. 40만 행 기준 **282ms, 인덱스 23MB**였습니다. 감사 테이블이 훨씬 크다면 그에 비례해 늘어납니다.

## v0.6.1

두 건 모두 **기록과 화면이 스스로 약속한 것을 지키지 않던 경우**입니다. 감사 목록은 "무엇이 언제 일어났는가"를 답하면서 순서를 서버 시계에 맡기고 있었고, 빠른 검색은 고른 항목에서 화면이 열린다고 해놓고 두 번째부터는 그러지 않았습니다.

### 보안

- **감사 목록이 시계에 따라 순서를 바꾸던 문제**를 수정했습니다. 목록은 `occurred_at`을 1차 정렬 키로 쓰고 `id`는 동점일 때만 썼습니다. 그런데 `occurred_at`은 서버의 벽시계(`now()`)이고, **시계는 뒤로 갈 수 있습니다** — 한동안 오프라인이던 장비의 NTP 보정, 재개된 가상 머신이 그렇습니다. ReSSO는 이미 대시보드에서 시계 오차를 보고할 만큼 이것을 운영 현실로 다루면서, 정작 **"무엇이 언제 일어났는가"를 답하는 기록의 순서를 그 시계에 맡기고** 있었습니다. 시계가 5분 뒤로 조정되면 그 뒤에 일어난 일이 이전 사건들 **아래에** 놓이고, 사고를 재구성하는 사람은 순서를 거꾸로 읽게 됩니다. `id`는 기록 시점에 부여되는 `bigserial`이고 `WriteAudit`은 `occurred_at`을 직접 넣는 일이 없으므로, 시계가 정상일 때 두 순서는 같고 **비정상일 때는 `id`만 옳습니다.** 이제 `id` 순으로 정렬합니다. 표시되는 시각은 그대로이고, 순서를 정하는 것이 아닐 뿐입니다.
- 이 결함은 **간헐적으로 실패하던 비밀번호 변경 감사 테스트의 유일하게 남은 설명**이기도 합니다. 그 테스트의 주석은 "잘못된 항목을 읽는 경우"를 동점 처리로 배제해 두었는데, **동점일 필요가 없었습니다.** 응답과 감사 결과는 같은 변수에서 나오므로 두 번째 항목이 `SUCCESS`일 수 없고, 항목이 둘 다 있는데도 `SUCCESS`를 읽었다면 목록이 **더 오래된 행을 돌려준 것**입니다. 주석을 사실에 맞게 고쳤습니다.

### 사용성

- **빠른 검색이 두 번째부터는 아무 일도 하지 않던 문제**를 수정했습니다. 명령 팔레트는 고른 항목의 검색어를 `?q=`로 함께 보내며, 서버 코드의 주석도 "도착한 화면이 그 항목에서 열리도록"이라고 적고 있습니다. 그런데 사용자·Client 목록은 그 값을 **마운트 시 한 번**, 그것도 라우터가 아니라 `window.location`에서 읽었습니다. 팔레트는 **지금 보고 있는 화면 위에서도** 열리므로 같은 경로로 이동하면 아무것도 다시 마운트되지 않습니다. 그래서 두 번째 검색부터는 **검색창·요청·주소창이 서로 다른 것을 가리킵니다** — 주소창은 `q=bob`인데 화면은 `alice`의 결과를 그대로 보여줍니다. 목록에서 사람을 찾다가 곧바로 다른 사람을 찾는 것은 이 화면에서 가장 흔한 동작입니다. 이제 라우터에서 읽고 값이 바뀔 때마다 반영합니다.
- 반영은 효과가 아니라 렌더 중에 합니다. 이 화면이 Realm 변경에 이미 쓰던 방식으로, 브라우저가 그리기 전에 조정되므로 **이전 검색어로 걸러진 화면이 한 프레임 스치는 일이 없습니다.**

## v0.6.0

이 릴리즈는 **꺼야 할 것이 실제로 꺼지는가**를 묻는 데서 시작해, 대부분의 수정이 거기서 나왔습니다. 계정을 비활성화하고, Realm을 정지시키고, 로그아웃하고, 비밀번호를 바꾸는 일들이 하나같이 성공을 보고하면서 해야 할 일의 일부를 하지 않고 있었습니다. 운영 중인 설치라면 **계정 비활성화**와 **Realm 정지** 두 항목을 먼저 읽으시길 권합니다.

### 보안

- **Session을 끝낼 때 Refresh Token 폐기에 실패해도 성공이라고 답하던 문제**를 수정했습니다. Session 종료는 두 번의 쓰기입니다 — 브라우저 쿠키를 멈추는 Session 레코드와, **RP를 계속 살아 있게 하는 Refresh Token**. 두 번째 실패가 버려지고 성공이 반환됐으므로, 측정해 보면 **Session은 폐기됐고(`revoked: true`) 그 Refresh Token은 여전히 유효합니다.** 본인은 로그아웃된 것처럼 보이는데 Refresh Token을 쥔 RP는 계속 Access Token을 발급받습니다. 더 나쁜 것은, v0.5.0에서 로그아웃·비밀번호 변경에 붙인 `PARTIAL` 보고가 **바로 이 경우를 위해 만들어졌는데 저장소가 성공을 반환하니 발동할 수 없었다**는 점입니다. 콘솔 로그아웃, RP-Initiated Logout, 관리자의 Session 강제 종료, "다른 세션 모두 종료", 비밀번호 변경·재설정이 전부 이 경로입니다.
- 이제 실패가 호출자에게 전달되어 감사 기록이 `PARTIAL`이 되고 사유가 남습니다. **RP 통지는 실패해도 그대로 보냅니다** — 로그아웃하라고 알리는 것은 성공한 절반이고, 그것만으로 세션을 끊는 RP도 있기 때문입니다.
- 같은 검사에서 **`EndSessionsOfDisabledUsers`는 이미 이 오류를 반환하고 있었습니다.** 계정 비활성화 작업에서 고쳐진 함수만 맞게 되어 있었고 나머지 셋이 그대로였습니다.
- 어느 쪽이 실패했는지가 **기록에 쓸 수 있는 문장을 결정합니다.** 종료된 Session을 두고 `session_revoked: false`라고 적으면, 읽는 사람은 이미 없는 Session을 끊으러 가고 **살아 있는 Refresh Token은 언급조차 되지 않습니다.** 저장소가 두 경우를 구분하는 오류(`ErrRefreshTokensNotRevoked`)를 돌려주고, 기록에는 `session_revoked: true`와 `refresh_tokens_revoked: false`가 함께 남습니다. 비밀번호 변경 쪽도 같은 구분을 씁니다.
- **관리자가 Session을 강제 종료했을 때 감사 기록이 아예 남지 않을 수 있던 문제**를 수정했습니다. 이 핸들러만 저장소를 직접 호출하고 오류를 그대로 응답으로 돌려보냈는데, 위 수정으로 저장소가 **폐기하지 못한 Refresh Token을 보고하기 시작하자** — Session은 실제로 종료됐는데도 — 관리자에게는 500이 나가고 **감사 기록은 한 줄도 남지 않았습니다.** 강제 종료는 "그 세션을 끊었다"는 사실을 남기는 유일한 기록입니다. 이제 다른 모든 로그아웃 경로가 쓰는 헬퍼를 지나며 `PARTIAL`로 기록됩니다.
- **본인이 자기 Session을 종료할 때도 같은 문제**가 있었습니다. 게다가 그것이 **지금 쓰고 있는 브라우저의 Session**이면, 오류가 반환되면서 쿠키가 지워지지 않아 **이미 동작하지 않는 Session의 쿠키를 그대로 들고 있게** 됩니다. 다음 요청이 아무 설명 없이 실패합니다. 이제 Session이 실제로 끝났으면 쿠키를 지우고 `PARTIAL`로 기록합니다.
- 비밀번호 변경·재설정의 부분 실패 응답이 **어느 쪽이 부족했는지**를 구분합니다. 세션은 끝났고 그 세션의 Refresh Token만 남은 경우에 "세션을 종료하지 못했습니다"라고 답하면, **비밀번호가 노출됐다고 판단해 바꾼 사람을 이미 없는 세션을 찾으러 보내면서** 정작 살아남은 것은 말하지 않게 됩니다. `other_sessions_ended`와 `refresh_tokens_revoked`를 함께 싣고 문구도 경우에 맞게 나눴습니다. 콘솔은 응답에 본문이 있으면 그 문구를 그대로 보여줍니다 — 특정 항목 하나만 보면 다른 한 경우를 놓칩니다.
- Session 하나를 끝내는 두 endpoint의 응답은 **`session_ended`와 `refresh_tokens_revoked`**로 답합니다. 비밀번호 변경 쪽이 쓰는 `other_sessions_ended`를 그대로 쓰면 **이 요청과 상관없는 것을 말하게 됩니다** — 여기에는 "다른 세션"이 없고, 끝난 이 세션과 거기서 발급된 Refresh Token만 있습니다. OpenAPI 문서와 콘솔에도 반영했습니다.
- **관리자 Session 화면이 Refresh Token을 폐기하지 못한 경우에도 조용히 닫히던 문제**를 수정했습니다. 확인 창은 "이 SSO 세션과 연결된 Refresh Token을 즉시 폐기합니다"라고 **약속하는데**, 실패할 수 있는 것이 정확히 그 절반입니다. 아무 말 없이 닫히면 관리자는 약속이 지켜졌다고 믿고, 그 Token을 쥔 RP는 계속 동작합니다. 내 세션 화면도 같이 고쳤습니다.
- **LDAP 공급자를 비활성화하거나 연결 해제해도 RP에는 아무 통지가 가지 않던 문제**를 수정했습니다. 계정 비활성화 항목은 비활성화가 "콘솔·LDAP 정리·공급자 연결 해제 세 곳"에서 일어나며 이제 한 경로를 지난다고 적었는데, **실제로 그 경로를 지나는 것은 앞의 둘뿐이었습니다.** 공급자 쪽 두 경로는 폐기 SQL을 각자 직접 쓰고 있었고, 측정해 보면 **Session은 폐기되고 Refresh Token도 폐기되지만 통지받은 RP는 0개**입니다. ReSSO에서는 로그아웃시키고 **그 사람이 ReSSO로 로그인한 모든 곳에서는 그대로 남겨두는** 것으로, 계정 비활성화에서 고친 것과 정확히 같은 결함입니다. 이제 둘 다 같은 호출을 지나므로 통지가 나가고, 폐기에 실패하면 성공으로 보고하지 않습니다.
- 연결 해제는 순서를 바꿔야 했습니다. 폐기가 트랜잭션 안에 있으면 커밋 뒤에는 알릴 대상이 남지 않으므로, **계정을 해제·비활성화하고 커밋한 다음** 로그아웃을 보냅니다. 커밋이 끝난 순간부터 그 계정들은 이미 비활성이고 비활성 계정의 Session은 어디서나 거절되므로, 그 사이에 열려 있는 것은 없습니다. `UpdateUser`가 계정을 비활성화할 때 쓰는 순서와 같습니다.
- 위 통지 수정으로 로그아웃이 **변경이 끝난 뒤에** 일어나므로, 로그아웃만 실패하는 경우가 생깁니다. 그것을 요청 실패로 답하면 **실제로 일어난 변경을 일어나지 않은 것처럼** 말하게 되고(공급자는 이미 비활성화됐거나 삭제됐습니다) 그 사실이 감사에도 남지 않습니다. 이제 변경은 성공으로 답하되 감사에 `PARTIAL`과 사유가 남고, 삭제는 로그아웃이 끝나지 못한 사실을 응답에 함께 싣습니다.
- 이 부분 실패는 **응답에도 실립니다.** 감사 기록만으로는 그 변경을 하고 있는 관리자가 볼 일이 없습니다. 비활성화 응답은 공급자 항목을 그대로 담은 채 `users_signed_out`과 안내 문구를 덧붙이고, 삭제는 204 대신 같은 내용을 담은 200을 돌려줍니다. 콘솔이 두 경우 모두 경고로 보여줍니다.
- **공급자 삭제가 어떤 이유로 실패하든 "가져온 사용자가 남아 있어 삭제할 수 없습니다"라고 답하던 문제**를 수정했습니다. 그 문구가 맞는 경우는 충돌 하나뿐인데 모든 실패에 같은 답을 돌려줬고, **연결된 사용자를 정리하라는 것은 되돌릴 수 없는 조치**입니다 — 진단이 틀린 채로 그 길로 안내하고 있었습니다. 이제 충돌만 409이고 나머지는 실제 오류로 답합니다.
- OpenAPI 문서가 **부분 실패로 끝날 수 있는 여섯 operation의 200 응답 형태를 모두 기술**합니다. 지금까지 이 단계가 매번 마지막이었습니다 — 저장소가 부족했던 부분을 알리게 하고, 핸들러를 고치고, 응답에 싣고, 화면에 띄운 다음에야 문서였습니다. 204만 약속하는 문서로 만든 클라이언트에는 "계정이 아직 다른 곳에 로그인되어 있다"는 문장을 받을 자리가 없습니다. 테스트가 여섯 경로 각각에 대해 200과 그 스키마가 실제로 정의되어 있는지 확인합니다.
- **Token 폐기에 실패해도 성공이라고 답하던 문제**를 수정했습니다. RFC 7009은 **모르는 Token에도 200**을 돌려주도록 정하고 있는데, 폐기하지 못한 경우가 전부 그 답으로 처리됐습니다. 실제로 측정해 보면 호출자는 **200**을 받고, 감사 기록은 **`SUCCESS` / `revoked: none`** — 모르는 Token을 줬을 때와 구분되지 않는 값 — 이며, 바로 뒤에 같은 Token을 introspection하면 **`active: true`**입니다. Token이 새어 나갔다고 판단했을 때 누르는 것이 폐기이므로, **되지 않았는데 됐다고 답하는 것**은 이 endpoint에서 나올 수 있는 최악의 답입니다. 사양도 이 경우를 따로 정하고 있습니다(§2.2.1): **503**을 돌려주고 호출자는 Token이 아직 살아 있다고 가정해 재시도합니다.
- 조회·폐기·기록 세 단계 중 **어디에서 실패했는지**가 로그와 감사 상세(`error`)에 남고, 감사 결과는 `FAILURE`입니다. **모르는 Token은 그대로 200**입니다 — 사양이 요구하는 동작이고, 그 두 가지를 구분하는 것이 이 수정의 전부입니다.
- 같은 수정에서 **Token 검증이 "유효하지 않음"과 "판단할 수 없음"을 구분**하게 했습니다. 폐기 목록을 읽지 못하면 지금까지는 서명이 멀쩡한 Token도 그냥 무효로 취급됐는데, 거절하는 쪽(userinfo·introspection)에서는 그것이 안전한 방향이지만 **상태를 바꾸라고 요청받은 쪽에서는 "할 일 없음"으로 읽혀** 위의 침묵을 만들었습니다.
- **재시작이 관리자의 결정을 되돌리던 문제**를 수정했습니다. Bootstrap은 멱등이라고 적혀 있고 실제로 기존 관리자의 비밀번호를 재설정하지 않습니다 — 재시작이 비밀번호 초기화 수단이 되지 않도록. 그런데 `platform_admin`과 `enabled`는 **매 기동마다 다시 적용**했습니다. 이름을 지은 관리자들을 만든 뒤 Bootstrap 계정을 비활성화하는 것은 평범한 운영인데, 다음 재시작에 **원래 비밀번호 그대로 되살아나고** 아무 기록도 남지 않았습니다. 권한을 회수해도 같은 방식으로 돌아왔습니다.
- 같은 한 줄 때문에 **`BOOTSTRAP_ADMIN`에 기존 일반 사용자의 이름을 넣으면 다음 기동에 그 사용자가 플랫폼 관리자가 되었습니다.** 설정 값 하나로 권한이 올라가는 경로입니다.
- 이제 권한은 **계정을 만들 때 함께** 부여하고 그 뒤로는 다시 적용하지 않습니다. 마지막 관리자가 잠겼을 때 되돌아가는 길은 `admin recover`입니다 — 의도적으로 실행해야 하고, 감사에 남으며, 복구한 계정의 Session과 API 키를 폐기합니다. **재시작으로 관리자 접근을 되살리던 동작에 의존하고 있었다면 그 경로로 바꿔야 합니다.**
- **Public Client가 수정 한 번으로 PKCE 요구를 잃을 수 있던 문제**를 수정했습니다. 생성 시에는 Type이 `public`이면 무조건 켜지만 **수정에는 같은 규칙이 없어서**, 평범한 편집으로 꺼질 수 있었습니다. Secret이 없는 Client에게 PKCE는 인가 코드를 **요청한 당사자에게 묶는 유일한 수단**입니다 — 없으면 리다이렉트에서 새어 나온 코드(브라우저 기록, 같은 custom scheme을 등록한 다른 앱, referrer)를 가진 누구나 교환할 수 있습니다. 호환성 문서는 "Public Client 강제"라고 적고 있었고, 첫 편집까지만 사실이었습니다. Confidential Client는 Secret으로 인증하므로 그대로 선택할 수 있습니다.
- 콘솔은 Public Client에서 이 스위치를 끌 수 없게 하고 이유를 함께 보여줍니다. 서버가 무시할 선택지를 내미는 것은 안 내미는 것보다 나쁩니다.
- **`prompt`을 하나의 값으로 비교해서 `prompt=login consent`가 조용히 무시되던 문제**를 수정했습니다. 사양상 `prompt`은 공백으로 구분된 **목록**이고 여러 SDK가 `login`을 다른 값과 함께 보냅니다. 문자열 전체를 `"login"`과 비교하니 일치하지 않아 **기존 Session을 재사용해 코드를 발급했습니다** — 민감한 동작 앞에서 재인증을 요구한 RP가 한 시간 된 Session에서 만든 코드를 받고, 응답에는 그 사실을 알 방법이 없습니다. v0.5.0에서 `max_age`와 `id_token_hint`를 고친 것과 같은 종류의 침묵입니다. 이제 목록으로 읽습니다.
- 화면이 없는 `consent`·`select_account`는 그대로 무시하되, **함께 왔다고 해서 `login`·`none` 처리가 사라지지는 않습니다.** 서로 모순되는 `none`과 `login`을 함께 요구하면 `invalid_request`로 거절합니다 — 하나는 어떤 화면도 띄우지 말라 하고 다른 하나는 로그인 화면을 요구하므로, 먼저 검사되는 쪽만 답하면 나머지 절반이 조용히 사라집니다.
- **시스템 로그의 비밀값 가림이 이 서비스가 자기 비밀을 부르는 이름들을 빠뜨리고 있던 문제**를 수정했습니다. `password`·`secret`·`token`·`authorization`만 찾고 있어서, LDAP 비밀번호를 저장할 때 쓰는 이름인 `bind_credential`과 사람들에게 발급하는 `api_key`로 기록되는 속성은 **그대로 남았습니다.** 이 로그는 30일 보관되고 플랫폼 관리자가 콘솔에서 읽습니다. 지금 그런 이름으로 로그를 남기는 곳은 없지만, 가림 장치가 존재하는 이유는 **내일 누군가 추가할 한 줄** 때문입니다. `credential`과 `key`를 더했고 판단은 가리는 쪽으로 기울였습니다 — 잘못 가린 값은 운영자를 잠깐 갸웃하게 할 뿐이지만, 잘못 남긴 값은 되돌릴 수 없습니다.
- 같은 곳에서 **묶음(group) 속성의 하위 항목이 상위 이름을 잃던 문제**도 고쳤습니다. `slog.Group("a", "value")`와 `slog.Group("b", "value")`가 둘 다 `value`에 기록되어 **뒤의 것이 앞의 것을 조용히 덮었습니다** — 받아들인 기록의 일부를 버리고 있었던 셈입니다. 이제 전체 경로로 남으며, 그 덕에 상위가 평범한 이름이어도 하위의 민감한 이름이 경로로 걸립니다.
- **계정 비활성화가 실제로는 아무 세션도 끝내지 않던 문제**를 수정했습니다. 모든 세션 조회가 `users.enabled`로 걸러지기 때문에 쿠키는 곧바로 통하지 않게 되고, 그래서 로그아웃된 것처럼 보였습니다. 그러나 Session 레코드도 Refresh Token도 살아 있었고 RP에는 아무 통지도 가지 않았습니다. 결과는 두 가지입니다. 자체 세션을 유지하는 애플리케이션에서는 그 사람이 **계속 로그인 상태로 남고**, 조사나 정직이 끝나 계정을 **다시 활성화하면 비활성화 시점에 열려 있던 세션이 전부 그대로 돌아옵니다** — 애초에 비활성화의 이유였던 그 세션까지 포함해서. 이제 비활성화는 Session과 Refresh Token을 폐기하고 Back-Channel Logout을 전송하며, 재활성화해도 이전 Session은 복구되지 않습니다. 콘솔은 저장 전에 이 사실을 경고로 알립니다.
- 같은 수정으로 **LDAP `DISABLE` 정책이 Refresh Token을 남겨두고 RP에 알리지 않던 문제**도 함께 해결됐습니다. 이 정책은 콘솔과 문서 모두 "비활성화 및 세션 종료"라고 설명하지만 실제로는 Session 레코드만 SQL로 폐기했고, 그래서 퇴사자가 연동 애플리케이션에서는 계속 로그인 상태였습니다. 비활성화는 콘솔·LDAP 정리·공급자 연결 해제 세 곳에서 일어나고 각자 "세션 종료"의 의미가 달랐는데, 이제 한 경로를 지납니다. 이번 정리에서 사라진 계정만 대상으로 하므로 예전에 떠난 사람에게 로그아웃이 반복 전송되지 않습니다.
- **Token introspection이 비활성화된 계정을 계속 `active`로 보고하던 문제**를 수정했습니다. 계정을 비활성화하면 userinfo도, Refresh 교환도, SSO Session도 즉시 거절하는데 introspection만 Access Token이 스스로 만료할 때까지 살아 있다고 답했습니다. 하필 introspection은 Resource Server가 **"이 Token이 지금도 유효한가"를 실시간으로 묻는** endpoint이고 — 코드 주석이 그 사용 패턴을 명시적으로 권장합니다 — 그래서 현재 답을 주도록 만들어진 유일한 경로가 낡은 답을 주고 있었습니다. 운영자가 계정을 막았다고 믿는 바로 그 지점에서 그 사람이 계속 일할 수 있습니다. 이제 사람에게 발급된 Token(세션 식별자를 가진 Token)은 주체가 여전히 활성 계정일 때만 `active`입니다. Client 자신의 Token은 사람을 가리키지 않으므로 영향받지 않습니다.
- **Realm을 정지시켜도 이미 들어와 있는 사람은 계속 쓸 수 있던 문제**를 수정했습니다. 새 로그인만 막혔고, 열려 있는 콘솔 세션도 개인 API 키도 그대로 동작했습니다. API 키는 세션보다 오래 살아남으므로 "전원 로그아웃시키고 기다린다"로 대체할 수도 없었고, 같은 키로 **MCP를 통해 디렉터리를 계속 읽을 수 있었습니다.** 세션 판정(`sessionIsLive`)과 API 키 인증이 Realm 상태를 보도록 했습니다 — 세션 쪽은 이미 Realm을 조인하고 있었으므로 한 단어가 모든 경로를 덮습니다.
- **자신이 로그인한 Realm은 비활성화할 수 없습니다.** 위 수정과 같은 변경에 포함해야 하는 항목입니다. 정지가 세션과 API 키까지 미치게 되면, 자기가 속한 Realm에 적용하는 순간 요청을 보내는 세션과 되돌릴 수 있는 모든 자격 증명이 함께 끊겨 데이터베이스를 직접 고치는 것 말고는 복구 수단이 없어집니다. 지금까지는 아무 방지 장치도 없었고 `master`도 예외가 아니었습니다. 다른 Realm 정지는 그대로 가능합니다.
- Realm 정지는 **폐기가 아니라 차단**입니다. 다시 활성화하면 만료되지 않은 세션과 API 키가 그대로 다시 동작합니다. 계정 비활성화가 세션을 영구 종료하는 것과 의도적으로 다르며 — 테넌트 정지는 보통 일시적이고 특정 개인의 세션이 원인인 상황이 아닙니다 — 콘솔과 문서에 그렇게 적었고 테스트가 확인합니다.
- **Realm이나 Client를 비활성화해도 일부 endpoint가 계속 응답하던 문제**를 수정했습니다. Realm을 조회하는 8개 endpoint 중 Discovery·JWKS·인가·Token만 `enabled`를 확인했고, userinfo·Introspection·Revocation·RP-Initiated Logout은 같은 헬퍼를 쓰면서 확인하지 않았습니다. 그래서 **정지시킨 테넌트가 사용자의 claim을 계속 내주고 Resource Server에게 Token이 유효하다고 계속 답했습니다.** Client도 마찬가지로 Token endpoint만 확인했고, Introspection과 Revocation은 확인하지 않았습니다. Introspection은 Resource Server 패턴을 위해 **같은 Realm의 모든 Confidential Client에게 열려 있으므로**, 폐기했거나 침해된 연동을 비활성화해도 그 Client의 Secret은 남의 Token을 검증하고 내용(`sub`·`username`·`scope`·`azp`)을 읽어내는 데 그대로 쓸 수 있었습니다. 두 검사를 각 호출부가 아니라 헬퍼에서 강제하도록 옮겼습니다 — 8개 중 4개, 3개 중 1개만 기억하고 있었다는 것이 호출부에 두는 방식의 결과입니다.
- RP-Initiated Logout이 Client를 두 가지 방법으로 찾으면서 **한쪽만 `enabled`를 확인하던 문제**를 함께 수정했습니다. `client_id`로 지정하면 거절됐지만 그 Client의 ID Token을 `id_token_hint`으로 제시하면 통과했고, 이 Client가 결정하는 것은 `post_logout_redirect_uri`를 허용할지 여부입니다.
- 비활성화된 Client가 이미 발급받은 Access Token은 서명 만료까지 유효합니다. 이번 수정은 Client의 **자격 증명**을 막는 것이고, 발급된 Token을 소급해서 무효화하지는 않습니다. Refresh 교환은 Token endpoint에서 이미 거절되므로 갱신되지는 않습니다.
- **로그아웃이 세션을 폐기하지 못해도 성공이라고 답하던 문제**를 수정했습니다. 콘솔 로그아웃과 RP-Initiated Logout 양쪽 모두 폐기 오류를 버렸습니다. 브라우저 쿠키는 어느 쪽이든 지워지므로 **본인은 로그아웃된 것처럼 보이지만** 세션은 살아 있고, 그 세션에 묶인 Refresh Token을 가진 RP는 계속 갱신하며, **Back-Channel Logout도 전혀 나가지 않습니다** — 그것을 보내는 주체가 폐기 동작 자체이기 때문입니다. 감사 기록은 `SUCCESS`였습니다. 로그아웃은 사용자가 가진 유일한 "지금 멈춰라"인데 아무것도 멈추지 않았습니다. 이제 실패가 로그에 남고 감사 기록이 `PARTIAL`과 세션 ID·사유를 담습니다.
- 로그아웃 응답은 **일부러 오류로 바꾸지 않았습니다.** 그 시점에 쿠키는 이미 지워졌고 사용자가 달리 할 수 있는 일이 없으므로, 거절하면 행동할 수 없는 실패 화면만 남기고 로그아웃 상태는 나아지지 않습니다. RP-Initiated Logout은 응답이 리다이렉트라 본문을 실을 자리도 없습니다. 대신 감사 기록이 세션을 지목하므로 관리자가 콘솔에서 직접 종료하고 RP 쪽을 점검할 수 있습니다.
- 로그인 요청이 이미 사용된 경우 방금 만든 Session을 되돌리는 경로도 실패 시 로그를 남깁니다. 바로 다음 줄에서 쿠키를 지우므로, 실패하면 주인이 볼 수 없는 살아 있는 Session이 남습니다.
- **비밀번호 변경·재설정이 세션 종료에 실패해도 성공이라고 답하던 문제**를 수정했습니다. 비밀번호가 바뀐 뒤 다른 세션을 폐기하는 것은 두 번째 단계이고 따로 실패할 수 있는데, 두 endpoint 모두 그 오류를 **버렸습니다** — 응답은 `204`, 감사 기록은 `SUCCESS`, 로그는 한 줄도 없었습니다. 비밀번호가 노출됐다고 판단해 바꾸는 사람에게, 또는 침해된 계정을 재설정하는 관리자에게 다른 세션이 끊겼다고 알려주면서 실제로는 살아 있었습니다. 콘솔 화면이 "비밀번호를 변경하면 현재 브라우저를 제외한 모든 SSO 세션이 종료됩니다"라고 약속하는 바로 그 동작입니다. 이제 실패하면 로그에 남고, 감사 기록이 `PARTIAL`과 실패 사유를 담으며, 응답이 `200`에 `other_sessions_ended: false`와 안내 문구를 실어 보냅니다. 비밀번호는 실제로 바뀌었으므로 요청을 거절하지는 않습니다 — 무엇이 안 됐는지 알리는 것이 핵심입니다.
- 감사 결과 값으로 **`PARTIAL`**이 추가됐습니다. 요청은 요구받은 일을 했고 그에 딸린 동작이 실패한 경우입니다. **`result=FAILURE`로만 알림을 거는 감시가 있다면 이 항목을 놓칩니다.** 콘솔 감사 화면의 결과 필터와 색상에 반영했습니다(주황).
- **로그인 요청 제한이 동작할 수 없는 설정에서도 "허용"이라고 답하던 문제**를 수정했습니다. 세 진입점이 각자 0값 결정에서 `Allowed`를 계산해 같은 설정에 **서로 다른 답**을 냈고, 특히 창(window)이 `0`이면 — 제한이 아예 꺼진다는 뜻인데 — 셋 다 허용이었습니다. 값이 설정이 아니라 코드의 리터럴에서 오므로 이 상태는 코드의 실수이고, 비밀번호 해싱 앞의 제한이 조용히 사라진 채 도는 것보다 요청을 실패시켜 드러내는 편이 맞습니다.
- MCP로 사람과 Client의 기록을 읽는 호출을 감사에 남깁니다. v0.5.0 릴리즈 노트에 영향 여부를 확인하는 방법도 함께 적었습니다 — LDAP 쪽은 확인할 수 있고, MCP 과거 호출은 기록이 없어 확인할 수 없다는 점을 포함해서. 이 endpoint는 그동안 **아무것도 기록하지 않았습니다** — 감사 이벤트도, 로그 한 줄도. v0.5.0의 `mcp:read` 디렉터리 노출을 공지받은 운영자가 "우리 디렉터리가 읽혔는가"를 확인할 방법이 없었고, 앞으로 에이전트가 사람들의 레코드를 읽어도 흔적이 남지 않습니다. 장기 사용 API 키로 대신 동작하는 에이전트는 감사 의무가 겨냥하는 바로 그 접근입니다. `MCP_TOOL_CALL`이 행위자·도구·결과를 남기며, **거절된 시도도 함께** 기록합니다 — 누군가 시도했다는 신호이기 때문입니다. 서비스 상태 조회는 무해하고 에이전트가 주기적으로 부를 수 있어 제외했습니다. 1년 보존을 그것으로 채우면 정작 찾아야 할 항목이 묻힙니다.

### 안정성

- **계정을 만들 때 기본 Role 부여가 실패해도 성공이라고 답하던 문제**를 수정했습니다. 계정 INSERT 다음에 `user` Role을 넣는 문장이 결과를 버리고 있어서, 그 문장이 실패하면 **콘솔에서는 멀쩡해 보이지만 `realm_access`에 `user` Role이 없는 계정**이 남습니다. 그 Role을 확인하는 RP는 전부 이 사람을 거절하는데 어디에도 이유가 적히지 않습니다. 이제 계정과 Role은 한 트랜잭션에서 **둘 다 만들어지거나 둘 다 만들어지지 않습니다** — 처음부터 망가진 계정을 만드는 것보다 관리자가 실패를 보고 다시 시도하는 편이 낫습니다.
- **개인 API 키 회전이 절반만 끝날 수 있던 문제**를 수정했습니다. 회전은 새 키를 만들고 이전 키를 폐기하는 두 번의 쓰기인데 서로 떨어져 있어서, **폐기가 실패하면 새 키만 남았습니다.** 호출자에게는 오류가 가므로 **새 Secret은 아무에게도 전달되지 않고**, 계정에는 같은 이름의 키가 둘 — 여전히 동작하는 원래 키와, 누구도 쓸 수 없는 키 — 남습니다. 다시 시도할 때마다 하나씩 늘어납니다. 실제로 폐기만 막아 보면 `agent` 하나가 `agent, agent`가 됩니다. 이제 한 트랜잭션 안에서 **둘 다 되거나 둘 다 안 됩니다.**
- 같은 변경으로 **서명하는 키와 공개하는 집합이 어긋날 여지**가 생겨 함께 막았습니다. 두 값은 각각 다른 질의에서 오는데, 이전에는 활성 키가 `RETIRED`가 되는 일이 없어 어긋날 수 없었습니다. 만료를 `retire_at`이 결정하게 되면서 **활성인데 만료 시각이 지난 행**이 이론상 가능해졌고, 그 조합은 **JWKS에 없는 키로 Token을 서명**합니다 — 모든 RP가 거절하는데, 키가 없어서가 아니라 서명이 틀린 것처럼 보입니다. 이제 그 상태에서는 서명도 JWKS 제공도 거절하고 어떤 키인지 로그로 지목합니다. 거절이 나은 실패입니다.
- **회전한 서명 키가 만료 시각이 지나도 계속 신뢰되던 문제**를 수정했습니다. 회전은 이전 키를 얼마나 더 받아줄지 정해 `retire_at`에 적고 콘솔도 그 시각을 표시하는데, **읽는 쪽은 아무도 그 값을 보지 않았습니다.** 판단은 상태 컬럼이 했고 그 컬럼을 바꾸는 것은 **매시간 도는 보존 정리뿐**이어서, 만료된 키가 최대 한 시간 더 JWKS에 남고 검증에 통과하며 콘솔에는 **이미 지나간 만료 시각 옆에서 `PASSIVE`로** 표시됐습니다. 키가 샜을지 모를 때 관리자가 누르는 것이 회전이고, 그때 약속되는 시간 범위를 **오래된 행을 지우는 작업에 맡겨둘 일은 아닙니다.** 이제 `retire_at`이 직접 결정하며, 보존 정리는 상태 컬럼에 기록을 남길 뿐 결정하지 않습니다. 캐시가 그 순간을 넘겨 서비스할 수 있는 시간은 한 시간이 아니라 캐시 수명(30초)으로 줄었습니다.
- 문서에도 유지 시간을 "일정 시간" 대신 **2시간**으로 명시했습니다. 이제 실제로 그 시각에 효력을 잃으므로 숫자를 적을 수 있습니다.
- **서명 키를 회전하면 다른 인스턴스가 새 키로 서명된 Token을 최대 30초 동안 거절하던 문제**를 수정했습니다. 키 캐시는 한 방향으로만 검토되어 있었습니다 — 방금 회전된 키로 계속 서명하는 인스턴스는 안전합니다(이전 키가 2시간 동안 PASSIVE로 남아 계속 받아들여지므로). **반대 방향이 문제였습니다.** 회전은 그 작업을 수행한 인스턴스의 캐시만 비우므로, 나머지 인스턴스는 **새 식별자가 없는 키 집합을 그대로 들고** 첫 번째 인스턴스가 이미 발급하고 있는 Token을 `token signing key is unavailable`로 거절했습니다. introspection·userinfo·Refresh 교환·`id_token_hint`이 모두 이 경로입니다.
- 더 오래 남는 쪽은 **JWKS**입니다. 같은 낡은 집합이 그 인스턴스의 JWKS로 나갔고, 모르는 식별자를 만난 RP가 하는 일이 바로 JWKS를 다시 받아오는 것이므로 — 하필 그 30초 안에 받아온 RP는 **새 키가 빠진 집합을 자기 캐시 수명만큼** 붙들게 됩니다. 원인이 된 창보다 훨씬 오래갑니다. 이제 처음 보는 식별자는 캐시를 한 번 다시 읽어 확인하며, 그 갱신이 JWKS가 내보내는 집합도 함께 새로 만듭니다.
- 이 재조회에는 **1초의 하한**을 뒀습니다. 식별자는 호출자가 제시한 Token에서 오므로, 하한이 없으면 아무 값이나 지어내는 것만으로 요청마다 질의를 하나씩 만들 수 있습니다. 하한이 없을 때 지어낸 식별자 100개가 커넥션 획득 200회를 발생시키는 것을 테스트가 확인합니다.
- **LDAP 동기화가 실패한 사용자를 숫자로만 보고하던 문제**를 수정했습니다. 이유는 발생하는 자리에서 이미 알고 있는데 — 이름이 로컬 계정과 겹친다, 디렉터리가 주는 주소를 이 Realm이 저장할 수 없다 — 그대로 버려졌고, 운영자에게는 `1 LDAP users could not be synchronized`만 남았습니다. **누가 실패했는지도, 무엇을 고쳐야 하는지도 알 수 없습니다.** 이제 사용자 이름과 사유를 최대 5명까지 기록하며 — 하나의 설정 실수는 보통 같은 문장을 반복하므로 몇 개면 충분합니다 — 그보다 많으면 남은 수를 함께 적어 **잘린 목록이 전체 목록처럼 읽히지 않게** 했습니다. 콘솔의 "최근 동기화 오류", 공급자 레코드, 감사 이벤트(`detail.failures`) 세 곳 모두에 담깁니다.
- 이것이 한 명의 문제로 끝나지 않는 이유는 **어떤 실패든 `DISABLE` 정리를 건너뛰기 때문**입니다. 그 규칙 자체는 옳습니다 — 실패한 사용자가 곧 동기화 시각이 갱신되지 않은 사용자이므로, 그대로 쓸어내면 디렉터리에 멀쩡히 있는 사람을 비활성화합니다. 문제는 그 결과를 **아무도 말해주지 않는다**는 점이었습니다. 관리자가 연동 전에 만들어둔 로컬 계정 하나와 이름이 겹치면, 그 뒤로 퇴사자 계정이 계속 활성 상태로 남는데 화면에 뜨는 것은 숫자 하나뿐입니다. 이제 실패 문구가 "정리를 실행하지 않았고 디렉터리에서 사라진 계정은 활성 상태로 남았다"는 사실을 함께 알립니다.
- **Back-Channel Logout의 재시도가 실제로는 끝까지 돌 수 없던 문제**를 수정했습니다. 재시도 정책은 세 번(2초·8초 간격)을 약속하는데, **한 번의 시도에 주는 시간과 전체 절차에 주는 시간이 같은 숫자 하나**였습니다. 그래서 빠르게 거절하는 RP에는 두 번만 시도하고 **마지막 8초는 이미 배제된 재시도를 기다리는 데** 썼고, 연결은 받아들이면서 응답하지 않는 RP — **재시작 중인 서버의 평범한 모습이자 재시도를 만든 이유 그 자체** — 에는 첫 시도가 예산을 전부 소진해 **재시도가 한 번도 일어나지 않았습니다.** 기존 테스트는 대기 시간을 1밀리초로 줄여 실행했으므로 이 결함이 보이지 않았습니다. 이제 시도마다 자기 마감을 갖고, 전체 예산은 재시도 정책에서 계산합니다 — 숫자를 따로 적어두면 정책이 바뀔 때 다시 어긋납니다.
- 이 변경으로 **로그아웃 응답이 느려지지는 않습니다.** 전달은 원래 백그라운드이며, 빠르게 실패하는 RP는 이전과 똑같이 10초 만에 끝납니다. 늘어난 예산은 시도 자체가 느릴 때만 쓰입니다. 종료 시에는 여전히 손에 든 시도만 마치고 다음 재시도를 시작하지 않으므로, 종료가 기다려야 하는 시간은 **한 번의 시도**이고 이는 종료 대기 시간과 같습니다.
- Realm과 대상 Client를 조회하는 데이터베이스 질의에는 시도 한 번 분량의 시간만 줍니다. 응답이 느린 데이터베이스가 RP들이 기다리는 예산을 먼저 써버리지 않도록 분리했습니다.
- 자기 Session을 종료할 때의 소유권 확인을 **폐기 문장 자체가** 하도록 바꿨습니다. 이전에는 호출자의 Session을 최대 500건 불러와 식별자를 찾는 방식이었고, 그게 맞았던 이유는 **그 숫자가 화면에 보이는 100건보다 크기 때문**이었습니다 — 사람이 볼 수 있는 Session은 늘 검사가 읽은 범위 안에 있었던 셈입니다. 소유권을 두 상한값 사이의 암묵적 관계로 결정할 일은 아니고, 데이터베이스에 묻는 비용은 조건 하나입니다. 남의 Session과 없는 Session은 똑같이 "찾을 수 없음"으로 답하므로 존재 여부를 알려주지 않습니다.

- 선택적인 사용자 검색 색인 세 개도 **첫 실패에서 멈추지 않고 전부 시도합니다.** 보존 정리에서 고친 것과 같은 모양입니다 — 앞의 하나가 실패하면 뒤의 둘은 만들어지지 않은 채, 호출자는 어느 쪽이든 한 줄만 기록하므로 이메일이나 표시 이름으로 검색할 때 계속 전체 테이블을 훑으면서도 **셋 중 어느 것이 없는지 알 수 없었습니다.** 이제 실패한 색인을 이름으로 지목해 함께 보고합니다.

- **Refresh Token 회전 유예 판정이 두 시계를 섞어 쓰던 문제**를 수정했습니다. `rotated_at`은 PostgreSQL이 `now()`로 기록하는데, 유예 창(30초) 안인지는 **ReSSO 프로세스의 시계**로 계산했습니다. 프로세스 시계가 데이터베이스보다 30초 넘게 앞서 있으면 유예 창 안의 **정상적인 재시도가 전부 재사용으로 판정됩니다** — Token 계열 전체가 폐기되고, 사용자는 해당 RP에서 로그아웃되며, 감사에는 일어나지 않은 `REFRESH_TOKEN_REUSE` 사건이 남습니다. Token 만료 판정도 같은 문제였습니다. 두 판정 모두 데이터베이스가 하도록 옮겨, 이미 데이터베이스에 묻는 `InspectRefreshToken`·세션 판정과 답이 일치합니다.
- **보존 정리(retention sweep)가 첫 실패에서 멈추던 문제**를 수정했습니다. 9개 문장을 순서대로 실행하면서 하나라도 실패하면 즉시 반환했으므로, **한 문장이 그 뒤 모든 문장의 운명을 결정했습니다.** 게다가 매시간 같은 자리에서 반복되므로 뒤쪽 테이블은 아예 한 번도 정리되지 않고, 커지면 실패는 더 잘 나므로 스스로를 강화합니다. 현실적인 방아쇠는 깨진 쿼리가 아니라 호출자가 거는 2분 마감이고, 가장 큰 테이블인 1년치 감사 이벤트가 9개 중 **세 번째**에 있었습니다. 그 뒤에는 인증 없는 트래픽으로 채워지는 `authorization_requests`가 있었습니다 — 주석이 스스로 그렇게 적어둔 바로 그 테이블입니다. 이제 모든 문장을 시도하고 실패를 모아 함께 보고하며, 로그가 **어느 문장이 실패했는지** 밝힙니다. 마감이 소진된 경우만 예외로 중단하고 남은 문장 수를 함께 남깁니다.
- 같은 수정에서 정리 순서를 **기다릴 수 없는 것부터**로 바꿨습니다. 외부 트래픽으로 채워지는 테이블, Session에 묶인 테이블, 그리고 긴 이력 보존 순입니다. 시간이 모자라 남는 쪽이 한 시간 더 기다려도 되는 쪽이 되도록.
- **LDAP 동기화가 자기 결과를 기록하지 못해도 성공을 반환하던 문제**를 수정했습니다. 동기화는 끝난 방식을 두 곳에 남깁니다 — 콘솔이 읽는 Provider 레코드(`last_sync_*`)와 감사 이벤트 `LDAP_FEDERATION_SYNC`. **두 쓰기 모두 오류를 버리고 있었습니다.** 그래서 기록에 실패한 실행은 호출자에게 성공을 반환하고, 콘솔은 `RUNNING`인 채 **이전 실행의 숫자**를 계속 보여주며, 감사에는 그 실행에 대한 항목이 **하나도 남지 않습니다** — 방금 계정을 비활성화하고 모든 곳에서 로그아웃시킨 실행이라도. v0.5.0 릴리즈 노트가 "`DISABLE` 정책의 영향을 받았는지"를 확인하라며 지목하는 것이 바로 그 감사 이벤트입니다. 이제 실패가 동기화 요약(`record_error`)에 실려 나오고, 예약 실행과 수동 실행 양쪽 로그가 **결과가 기록되지 않았다는 것과 그래서 콘솔·감사 어디에도 나타나지 않는다는 것**을 함께 남깁니다.
- 수동 동기화 응답은 "진행 상황은 목록의 동기화 상태에서 확인할 수 있습니다"라고 안내합니다. 기록이 실패한 경우 그 안내가 가리키는 곳이 유일하게 말해주지 않는 곳이므로, 그때는 로그 한 줄이 남는 전부라는 점을 코드 주석에 적었습니다.

### 사용성

- **같은 문제가 남아 있던 나머지 관리 쓰기 경로**도 정리했습니다. Realm·Client·Role·Client Role·사용자 생성과 Realm 수정, 비밀번호 변경·재설정이 모두 실패 원인을 가리지 않고 그대로 돌려보내고 있었습니다. 저장소가 **caller가 보낸 것에 대한 오류**(이름 누락, HTTPS가 아닌 issuer_url, 잘못된 현재 비밀번호 등 10건)를 문장으로 구분해 반환하고, 핸들러는 그 문장만 통과시킵니다. 그 밖의 실패는 데이터베이스 문구 없이 서버 오류입니다. Client Secret digest 손상처럼 **내부 불변식이 깨진 경우는 일부러 문장을 붙이지 않아** 응답에 실리지 않습니다.
- **Role 매핑 저장이 실패하면 데이터베이스 오류 문구를 그대로 응답에 싣던 문제**를 수정했습니다. 쓰기가 실패했을 때 `ERROR: ... (SQLSTATE P0001)`가 **`400 잘못된 요청`으로** 돌아왔습니다 — 서버 쪽에서 실패한 일을 두고 관리자에게 "당신이 보낸 것이 잘못됐다"고 말한 셈이라, 방금 입력한 값을 뒤지게 만듭니다. 이름 충돌 경로에는 이미 같은 처리가 되어 있었고 이 경로만 빠져 있었습니다. 이제 **Realm에 속하지 않는 Role을 지정한 경우만 400**이고 문장으로 설명하며, 그 밖의 실패는 데이터베이스 문구 없이 서버 오류로 답합니다.
- **Redirect URI가 하나도 없는 로그인 Client도 같은 이유로 거절**합니다. 인가 endpoint는 요청의 `redirect_uri`를 등록된 목록과 대조하므로, 목록이 비어 있으면 무엇도 일치하지 않아 모든 요청이 "등록되지 않은 주소"로 거절됩니다. 만들 때도 편집할 때도 검사합니다.
- **로그인에 절대 성공할 수 없는 Client 설정이 그대로 저장되던 문제**를 수정했습니다. 인가 endpoint는 Client의 허용 Scope에 `openid`가 있어야 진행하는데, 허용 Scope를 직접 지정하면서 `openid`를 빼면 **저장은 되고 화면에도 정상으로 보이며** 이후 모든 인가 요청이 `invalid_scope`로 거절됩니다. 비어 있을 때만 기본값을 채워 넣고 있어서, 목록을 손으로 적거나 편집에서 지우는 경우가 그대로 통과했습니다. 관리자는 이 사실을 RP 쪽 오류로 알게 되는데 RP는 **어떤 설정이 잘못됐는지 말해줄 수 없습니다.** 이제 방금 그 항목을 보고 있는 자리에서 거절합니다. `client_credentials`만 쓰는 Client는 인가 endpoint를 지나지 않으므로 그대로입니다.
- **서명 키 화면이 키의 사용 기간을 브라우저 시계로 계산하던 문제**를 수정했습니다. 대시보드는 데이터베이스 시계로 `180일`이 넘은 키를 세는데 목록 화면은 `Date.now()`로 계산해서, **같은 키를 두고 두 화면이 다른 판정**을 내릴 수 있었습니다 — 시계가 앞선 관리자에게는 대시보드가 세지 않는 키가 회전 권고 색으로 보입니다. 같은 파일의 주석이 "두 화면이 한 키를 두고 어긋나는 것이 둘 다 침묵하는 것보다 나쁘다"고 적으면서 기준 일수만 공유하고 나이는 브라우저에 맡겨두고 있었습니다. 이제 나이도 서버가 데이터베이스 시계로 계산해서 내려보냅니다. 계정 잠금 표시에서 이미 같은 이유로 고친 것과 같은 종류입니다.
- **LDAP 사용자 인증 테스트가 "확인할 수 없음"과 "거절당함"을 구분하지 않던 문제**를 수정했습니다. 디렉터리에 닿지 못했을 때도, Bind 계정이 더 이상 통하지 않을 때도, 저장된 자격 증명을 열지 못했을 때도 전부 **`401 LDAP 사용자 인증에 실패했습니다`**로 답했습니다. 이 화면은 관리자가 **무엇이 잘못됐는지 알아보려고** 쓰는 도구인데, 그 답을 보고 나면 문제가 아니었던 비밀번호를 재설정하러 갑니다. 이제 물어보지 못한 경우는 사유와 함께 `502`로 답하고, 디렉터리가 실제로 거절한 경우만 `401`입니다. 바로 옆의 연결 테스트는 이미 실제 사유를 그대로 돌려주고 있었습니다.
- **Role 삭제가 거절될 때 "동일한 항목이 이미 존재합니다"라고 답하던 문제**를 수정했습니다. 거절 사유가 두 가지인데 구분 없이 하나의 충돌로 돌려보내고 있었고, 충돌의 기본 문구는 삭제에 대해 **아무 의미도 없는 문장**입니다. Realm과 함께 만들어지는 기본 Role(`user`·`realm-admin`·`offline_access`)을 지우려는 관리자도, 이미 남이 지운 Role을 오래된 화면에서 지우려는 관리자도 같은 답을 받았습니다. 이제 기본 Role은 그 사실을 문장으로 말하고, 없는 Role은 충돌이 아니라 **찾을 수 없음**으로 답합니다 — 없는 것을 두고 이미 존재한다고 하면 있지도 않은 중복을 찾으러 가게 됩니다.
- **Web Origin에 기본 포트를 적어 등록하면 조용히 아무것도 허용하지 않던 문제**를 수정했습니다. 브라우저는 스킴의 기본 포트를 `Origin` 헤더에서 **생략**하므로, `https://app.example.com:443`으로 등록하면 저장은 되고 화면에도 보이지만 **어떤 요청과도 일치하지 않습니다** — 그 애플리케이션의 CORS 요청이 전부 거절되는데 이유를 알려주는 곳이 없습니다. 이제 기본 포트를 정규화해서 떨어뜨립니다. 두 표기는 같은 Origin이므로 허용 범위가 넓어지지는 않으며, 같은 Origin을 두 가지로 적으면 하나로 합쳐집니다. **이미 그렇게 등록된 Client는 저장을 한 번 다시 하면 정규화됩니다.**
- Redirect URI가 **정확히 일치할 때만** 허용된다는 규칙에 테스트를 붙였습니다. 이 규칙이 인가 코드가 다른 곳으로 전달되는 것을 막는 전부인데 확인하는 테스트가 하나도 없었고, 나중에 편의를 위해 접두사나 와일드카드나 끝의 슬래시를 너그럽게 처리하는 변경은 **편의처럼 보이면서 모든 테스트를 통과**합니다.
- **관리자 비밀번호 재설정 화면이 세션을 종료하지 못한 경우에도 종료했다고 알리던 문제**를 수정했습니다. 서버는 이 경우를 위해 204 대신 **200에 `other_sessions_ended: false`와 사유**를 실어 보내고 감사 기록도 `PARTIAL`로 남기는데, 화면은 응답 본문을 보지 않고 성공이면 무조건 **"비밀번호를 재설정하고 세션을 종료했습니다"** 초록 알림을 띄웠습니다. 침해됐다고 판단해 재설정하는 관리자야말로 **세션이 살아남았다는 사실을 들어야 하는 사람**입니다. 본인 비밀번호 변경 화면은 이미 이 응답을 구분하고 있었고 관리자 화면만 빠져 있었습니다.
- **아무도 답하지 않은 승인 요청이 목록에서 사라지던 문제**를 수정했습니다. 목록은 최근 500건으로 제한되고 "대기 중"은 그것이 도착한 **뒤에** 콘솔이 세는데, 최신순으로만 정렬하니 결재된 요청 500건이 쌓이면 아직 기다리는 요청이 페이지 밖으로 밀려났습니다 — **결재자 화면에는 "0 대기"로 보이고 넘길 페이지도 없습니다.** 이제 대기 중인 요청을 먼저 정렬하므로 제한에 걸려 사라지지 않습니다. 결재된 이력은 그 뒤로 최신순으로 그대로 옵니다.

- **같은 Role을 여러 번 요청할 수 있어 결재자에게 같은 줄이 반복해 보이던 문제**를 수정했습니다. 아무 일도 일어나지 않은 것처럼 보여 한 번 더 누르는 것이 이 상황이 생기는 평범한 경로입니다. 이미 대기 중이면 그렇게 답합니다.
- **이미 가진 Role의 요청을 승인하면 "approval target is no longer valid"로 실패하던 문제**를 수정했습니다. 관리자가 먼저 직접 부여했거나 중복 요청 하나가 먼저 결재된 경우인데, 대상은 멀쩡하고 **결재자가 원한 상태는 이미 성립해 있습니다.** 대상이 아직 있는지와 행이 쓰였는지를 따로 묻도록 바꿔서, 진짜로 사라진 대상(예: 결재 대기 중 삭제된 Role)만 그 메시지로 실패합니다.

- **이미 쓰이는 이름으로 무언가를 만들면 PostgreSQL 제약 위반 메시지가 그대로 콘솔에 표시되던 문제**를 수정했습니다. `duplicate key value violates unique constraint "clients_realm_id_client_id_key" (SQLSTATE 23505)` — 사용자·Client·Role·Realm 네 경로 모두 그랬습니다. 이름 충돌은 관리자가 가장 흔하게 저지르는 실수이므로, **제품에서 가장 자주 나는 오류가 내부 제약 이름으로 답하고 있었던 셈입니다.** 이제 어느 이름이 왜 안 되는지 문장으로 답하고 상태 코드도 `409`입니다.
- **어느 값이 충돌했는지는 제약이 결정하므로, 메시지도 발생한 제약에서 고릅니다.** `users` 테이블 하나에만 유일 제약이 셋이어서 호출부가 아는 필드 하나로 뭉뚱그리면, 이메일 충돌에 "사용자 이름이 이미 사용 중"이라고 답해 아무 문제 없던 필드를 고치러 보내게 됩니다. 이메일 중복·대소문자만 다른 사용자 이름·Issuer URL 중복이 각각 자기 이름으로 보고됩니다. 제약 이름 목록이 실제 스키마에 존재하는지 테스트가 확인하므로, 마이그레이션이 제약 이름을 바꾸면 조용히 뭉뚱그린 메시지로 되돌아가는 대신 테스트가 실패합니다.
- **수정·갱신 경로도 같이 처리합니다.** 사용자의 이메일을 다른 사람이 쓰는 값으로 바꾸면 `500 요청을 처리하지 못했습니다`가 돌아왔고, Realm의 Issuer URL을 다른 Realm이 쓰는 값으로 바꾸면 제약 위반이 그대로 나왔습니다. Client 수정과 LDAP 공급자 생성·수정도 포함했습니다.
- **Realm 이름 규칙을 서버가 검사합니다.** 콘솔 폼은 "소문자, 숫자, 하이픈만 사용합니다"라고 안내하면서 실제로는 브라우저의 `pattern` 속성만 그것을 강제했고, API로 직접 만들면 데이터베이스 CHECK 제약 위반이 그대로 돌아왔습니다. 같은 파일의 정책 숫자들은 이미 "제약 위반 대신 읽을 수 있는 메시지"로 처리하고 그 이유를 주석에 적어두고 있었습니다 — 이름만 빠져 있었습니다.
- 이 경로들이 응답에 싣는 문장은 저장소가 **직접 작성한 것만** 통과합니다. 데이터베이스가 돌려준 문자열이 이 길로 응답에 실리는 일은 없으며, 테스트가 `SQLSTATE`·`constraint`·`violates` 같은 흔적이 응답에 나타나지 않는지 확인합니다.
- OpenAPI 문서가 두 비밀번호 endpoint의 응답을 두 형태 모두 기술합니다. `/api/v1/me/password`는 요청 본문 스키마도 함께 갖췄습니다 — 지금까지 본문이 문서에 없었고 응답도 실제와 달랐습니다.
- **없는 경로와 허용되지 않는 메서드가 서비스의 오류 형식을 지키지 않던 문제**를 수정했습니다. 이 서비스의 모든 거절은 `error`·`message`·`trace_id`를 담은 JSON이고 OpenAPI 문서도 모든 operation의 `4XX`에 그 형식을 약속하는데, 두 경로만 라우터 기본값이 그대로 나갔습니다 — 없는 API 경로는 **`404 page not found` 평문**, 다른 메서드로만 존재하는 경로는 **본문이 아예 없는 405**였습니다. 후자는 CORS preflight가 프로토콜 endpoint 전체에 wildcard로 등록되어 있어 알 수 없는 하위 경로가 패턴에는 맞고 메서드에서 걸리기 때문입니다. 경로를 잘못 적었거나 다음 버전에서 없어진 endpoint를 부른 호출자가 바로 이 응답을 받는데, 그들의 오류 처리는 문서에 적힌 형식을 읽도록 쓰여 있습니다.
- 콘솔의 SPA 경로는 그대로입니다. 그쪽은 catch-all 라우트에 잡히므로 위 처리에 도달하지 않으며, 테스트가 함께 확인합니다.
- **콘솔의 두 동작이 실패해도 아무 말을 하지 않던 문제**를 수정했습니다. Client의 Role 삭제는 실패하면 행이 그대로 남고, 개인 API 키 회전은 실패하면 새 Secret을 보여주는 창이 뜨지 않습니다 — 둘 다 **클릭이 먹히지 않은 것과 구분되지 않습니다.** 같은 화면의 다른 모든 요청은 오류를 화면에 띄우고 있었으므로 설계가 아니라 누락입니다. 이제 서버가 보낸 문장을 알림으로 보여줍니다. 회전은 특히, 실패를 모르면 **기존 키가 아직 유효하다는 사실도 모른 채** 폐기됐다고 믿게 됩니다.
- **콘솔의 상태 문구가 본문 대비 기준(WCAG AA, 4.5:1)에 미치지 못하던 문제**를 수정했습니다. 팔레트의 경고색은 흰 배경에서 **3.49**, 성공색은 **3.91**이었고, 강조색은 흰 배경에서는 4.55로 통과하지만 **콘솔의 실제 바탕인 페이지 배경(#f6f7fb) 위에서는 4.25**였습니다. 이 색들은 배경이 아니라 **글자색으로** 쓰입니다 — 대시보드의 준비 상태 문구, 서명 키의 회전 권고 일수, 비밀번호 조건 체크리스트, 승인 요약의 경고, 상태 Chip이 전부 여기에 해당합니다. 즉 운영자가 화면에서 가장 먼저 읽는 판정 문구들이 기준 미달이었습니다. 색조는 유지한 채 기준을 넘도록 조정했고(경고 5.43, 성공 5.69, 강조 4.84), **팔레트를 손볼 때 다시 내려가면 테스트가 실패합니다.** 흰 배경만 재면 강조색의 경우를 놓치므로 카드 배경과 페이지 배경을 모두 계산합니다.
- 같은 검사에서 **브랜드 색이 테마 밖에 두 벌 더 박혀 있던 것**을 정리했습니다. 사이드바 로고와 대시보드 카드가 예전 값을 직접 들고 있어, 팔레트만 고치면 **같은 화면에 서로 다른 파랑이 남습니다.**
- **키보드로 본문에 닿으려면 매번 메뉴 12개를 지나야 하던 문제**를 수정했습니다. 화면을 옮길 때마다 사이드바 전체와 헤더 조작부를 Tab으로 통과해야 본문 첫 요소에 닿았습니다. 첫 Tab에서 나타나는 **"본문으로 건너뛰기"** 링크를 넣었습니다.
- **화면을 옮겨도 포커스가 따라가지 않던 문제**를 수정했습니다. 콘솔은 SPA라 라우팅이 화면만 갈아끼우므로, 제목은 바뀌어도 포커스는 **방금 누른 메뉴 항목에 그대로 남습니다.** 스크린 리더 사용자는 새 화면이 열린 사실을 알 수 없고, 키보드 사용자는 이전 위치에서 Tab을 이어가게 됩니다. 이제 이동 후 본문으로 포커스를 옮깁니다. **처음 열었을 때는 옮기지 않습니다** — 사용자가 요청한 이동이 아니기 때문입니다.
- **Cmd/Ctrl+K 명령 팔레트에 이름이 없던 문제**를 수정했습니다. MUI는 `DialogTitle`이 없어도 `aria-labelledby`를 붙이기 때문에, 제목을 렌더하지 않으면 **존재하지 않는 id를 가리킵니다.** 속성만 세면 이름이 있는 것처럼 보이지만 실제로는 이름 없는 대화 상자로 읽힙니다. 검색 입력도 `placeholder`만 있었는데 **placeholder는 접근 가능한 이름이 아니고 입력을 시작하면 사라집니다.** 콘솔에서 키보드로 가장 많이 쓰는 경로가 정작 이름이 없었습니다.

### 운영

- **서버 로그 목록의 정렬이 같은 시각의 레코드들 사이에서 임의였던 문제**를 수정했습니다. 로그는 몰려서 들어오고 시각이 겹치는 일이 계속 있는데 `occurred_at` 하나로만 정렬하고 있어서, 같은 순간의 레코드는 **어떤 순서로도 나올 수 있었습니다** — 실제로 확인해 보면 `record-01, record-02, record-00` 순으로 돌아옵니다. "최신순"이라는 약속이 그 구간에서는 사실이 아니고, `offset`으로 넘겨 가며 읽으면 원리상 같은 레코드를 두 번 보고 다른 하나를 못 보게 됩니다. 감사 이벤트 목록은 이미 식별자를 두 번째 정렬 키로 쓰고 있었고 서버 로그만 빠져 있었습니다. 여기서는 식별자가 `bigserial`이라 **같은 시각의 레코드가 도착한 순서대로** 정렬되기도 합니다.
- **Introspection이 판정하지 못한 Token과 죽은 Token을 구분할 수 없던 문제**를 수정했습니다. 둘 다 `active=false`로 답하고, **그 답 자체는 그대로 두었습니다** — 거절하는 쪽이 안전한 방향이고, 5xx를 받은 Resource Server가 오히려 검사를 건너뛸 수 있기 때문입니다. 문제는 서비스가 내보내는 **모든 신호에서 두 경우가 같은 호출**이었다는 점입니다. 실제로 측정하면 정상 호출과 조회가 깨진 호출이 **HTTP 200, 요청 카운터의 같은 시계열, `status=200` 접근 로그 한 줄**로 똑같이 남고, 조회 오류는 발생한 자리에서 버려졌습니다. Introspection에만 국한된 장애는 **모든 Token이 죽은 것과 겉모습이 같아서**, 모든 Resource Server가 모든 요청을 거절하는 동안 서비스는 멀쩡해 보입니다. Token 발급과 로그인에서 이미 고친 것과 같은 모양입니다.
- `resso_introspection_errors_total`(실패한 조회 단계별 `stage` 레이블)을 추가하고 로그에 사유를 남깁니다. Session이나 계정이 **사라진 경우는 실패가 아니라 진짜 답**이므로 세지 않습니다. README 지표 목록과 권장 경보에 함께 실었습니다.
- **로그인은 됐는데 인가 코드 발급에 실패하면 아무 기록도 남지 않던 문제**를 수정했습니다. 그 시점에 Session은 이미 만들어졌고 쿠키도 브라우저에 나간 뒤입니다 — 실제로 측정하면 **살아 있는 Session 1개, 쿠키 2개, 감사 기록 0건, 지표 증가 없음**입니다. 누군가 로그인해서 Session을 받아 갔는데 **누가 로그인했다는 기록이 어디에도 없고**, 운영자에게 지켜보라고 안내한 카운터도 움직이지 않아 **쓰기를 받지 못하는 데이터베이스가 한산한 1분처럼** 보였습니다. 이제 로그·감사(`LOGIN_SUCCESS` / `PARTIAL`, 사유 포함)·지표(`result="error"`)에 모두 남습니다.
- **Session은 일부러 그대로 둡니다.** 사람은 실제로 인증했고 쿠키도 갖고 있으므로, 애플리케이션에서 다시 시도하면 그 Session을 찾아 다시 묻지 않고 진행합니다. 응답 문구도 "로그인은 되었지만 인가 코드를 생성하지 못했다"로 바꿔 무엇이 된 것이고 무엇이 안 된 것인지 알 수 있게 했습니다.
- 이미 사용된 로그인 요청이라 방금 만든 Session을 되돌리는 경로에서, **되돌리기 자체가 실패하면** 로그에 남깁니다. 바로 다음 줄에서 쿠키를 지우므로 실패하면 주인이 볼 수 없는 Session이 남습니다.
- **`POSTGRES_DSN`에 넣은 연결 풀 설정이 조용히 무시되던 문제**를 수정했습니다. pgx는 연결 문자열의 `pool_max_conns`·`pool_min_conns`를 읽어 들이는데, 그 뒤 코드가 무조건 덮어썼습니다 — pgx 문서대로 풀을 조정한 운영자는 **오류도 효과도 얻지 못했습니다.** 이제 20/2는 지정하지 않았을 때의 기본값이고, 지정하면 그 값이 적용됩니다. 이 서비스는 비밀번호 해싱이나 디렉터리 조회 동안 연결을 붙들지 않아 20이면 대부분 충분하지만, 큰 설치가 다르게 말할 자격은 있습니다. `docs/operations.md`에 설명을 추가했습니다.

- **이미지를 되돌렸을 때 데이터베이스가 자기보다 앞서 있다는 사실을 서비스가 말해줍니다.** 컨테이너를 이전 이미지로 되돌리는 것은 문서에 적힌 업그레이드 절차의 5단계인데, 되돌린 바이너리는 자기가 아는 Migration이 전부 적용된 것을 보고 **아무 일도 없는 듯 기동합니다.** 그게 안전한지는 새 Migration이 무엇을 했느냐에 달렸고 각 릴리즈 노트가 그걸 적지만, 서비스는 이 사실을 그냥 볼 수 있으면서도 침묵했습니다 — 운영자의 유일한 보호막이 사고 중에 문서를 읽을 것을 기억하는 일이었던 셈입니다. 기동 로그 경고와 `admin diagnose`의 `migrations_ahead_of_binary`로 나타납니다.
- **기동을 막지는 않습니다.** 롤백은 이미 무언가 잘못됐을 때 하는 조치이고, 뜨지 않는 서비스는 그 순간 빠져나갈 길마저 없앱니다.
- README의 운영 지표 목록에 `resso_uptime_seconds`가 빠져 있던 것을 채웠습니다. `/metrics`는 늘 내보내고 있었습니다. 이 목록은 운영자가 Prometheus를 겨누는 근거이므로, **양방향으로** 일치하는지 테스트가 확인합니다 — 서비스가 내보내는데 목록에 없는 것과, 목록에 있는데 아무것도 내보내지 않는 것 둘 다 실패입니다. 후자는 설정된 것처럼 보이면서 영영 비어 있는 대시보드 패널이 됩니다.
- **서버와 데이터베이스의 시각 차이를 관리 대시보드에 표시합니다.** 서비스의 모든 수명은 한쪽에서 계산되어 다른 쪽에서 판정되므로, 두 시계가 어긋나면 모든 창이 그만큼 밀립니다 — 서로 무관해 보이는 증상 여러 개로 나타나 진단이 어려운 종류의 고장입니다. ReSSO는 오프라인 운영을 전제로 하는데 그런 환경에서는 NTP를 쓸 수 없는 경우가 많아 아무것도 이를 알려주지 않았습니다. 측정 왕복 오차와 함께 표시하며, 기준선은 위의 회전 유예(30초)입니다. `docs/operations.md`에 설명을 추가했습니다.
- **로그인 요청 제한을 문서화했습니다.** 계정 잠금 앞에 잠금과 별개인 두 제한이 있는데(출발지 주소 5분 100건, 계정 실패 5분 30건) 운영 문서에 아무 설명이 없었습니다. 특히 사용자가 이유 없이 `429`를 받을 때 가장 흔한 원인 — 사무실 전체가 하나의 NAT 주소를 쓰거나, Reverse Proxy가 `TRUSTED_PROXY_CIDRS`에 없어서 **모든 사용자가 Proxy 주소 하나로 합쳐지는** 경우 — 을 함께 적었습니다. 후자는 설치 전체가 5분에 100건이라는 예산 하나를 나눠 쓰게 됩니다.
- **오프라인 반입 스크립트가 아카이브를 검증하지 않던 문제**를 수정했습니다. Release는 아카이브와 함께 `release-sha256.txt`를 게시하는데, 정작 README가 실행하라고 안내하는 `scripts/offline-load.sh`는 그걸 보지 않고 그대로 `docker load`했습니다. README의 `sha256sum` 한 줄도 **다이제스트를 출력할 뿐 무엇과도 대조하지 않았고**, 게시되는 체크섬 파일을 언급하지도 않았습니다. 이동식 매체로 폐쇄망에 들어오는 아카이브에서 체크섬은 운영자가 가진 사실상 유일한 무결성 확인 수단입니다.
- 이제 아카이브 옆의 `release-sha256.txt`(또는 `<아카이브>.sha256`)를 찾아 대조하고, 없으면 **반입을 거절**합니다. 확인 없이 진행해야 하면 `--no-verify`가 있습니다. 대조는 `sha256sum -c` 대신 다이제스트를 직접 비교합니다 — `-c`는 체크섬 파일에 적힌 **파일 이름**으로 맞추므로 옮기는 중에 이름이 바뀌었으면 엉뚱한 메시지로 실패합니다.
- 압축 무결성도 `docker load` 전에 확인합니다. 잘린 아카이브는 `docker load`도 잡아내지만(그건 확인했습니다), 그 전에 한 줄로 끝나고 **체크섬이 잡을 수 없는 경우** — 손상된 뒤에 다이제스트를 다시 계산한 파일 — 를 덮습니다.
- **운영 문서가 세션 유휴 만료의 최댓값을 24시간이라고 적고 있던 문제**를 수정했습니다. 실제 제약은 `0` 또는 300–2592000초(30일)이며, 같은 문서의 Realm 정책 표에는 올바르게 적혀 있었습니다. **한 문서 안의 두 표가 같은 설정을 두고 서로 다른 값을 말하고 있었습니다.**
- Realm 정책의 상·하한이 **세 곳**에 적혀 있습니다 — 테이블의 CHECK 제약, 그것을 미리 읽을 수 있는 문장으로 바꿔주는 검증기, 그리고 값을 고르는 사람이 읽는 운영 문서. 셋이 서로 일치하는지 테스트가 확인합니다. 검증기가 제약보다 넓으면 제약 위반 메시지가 그대로 사용자에게 가는데, 그 검증기는 애초에 그걸 막으려고 존재합니다.
- **운영 문서가 존재하지 않는 감사 이벤트 이름을 안내하던 문제**를 수정했습니다. 비밀번호 재설정이 `USER_PASSWORD_RESET`으로 기록된다고 적혀 있었지만 실제 이름은 `PASSWORD_RESET`입니다. 그 이름으로 거르면 빈 목록이 나오고, **조사 중에 빈 목록은 "그런 일이 없었다"로 읽힙니다** — 실제로는 존재하지 않는 것을 물어본 것인데도.
- 운영 문서의 감사 신호 표에 **`result=PARTIAL`**을 추가했습니다. 오늘 도입한 결과 값인데 정작 운영자가 보는 문서에는 없었습니다. `result=FAILURE`만 걸어둔 알림이 이 항목을 놓친다는 점과, 해당 세션이 살아 있으므로 직접 종료해야 한다는 점을 함께 적었습니다.
- `LDAP_FEDERATION_SYNC` 항목에 **이벤트가 아예 없는 경우**의 의미를 덧붙였습니다. 실행이 끝났는데도 이벤트가 없다면 결과를 기록하지 못한 것이며, 그때는 공급자의 동기화 상태도 이전 실행에 머물러 있으므로 서버 로그를 봐야 합니다.

### 개발

- **문서가 안내하는 유지보수 명령이 실제로 존재하는 명령인지 테스트가 확인합니다.** 사고 중에 운영자가 타이핑하는 것이 이 줄들이고, 없는 명령은 이미 나쁜 하루를 보내고 있는 사람이 가장 나쁜 순간에 발견합니다. 실제로 있었던 일입니다 — 바로 앞 항목을 쓰면서 제가 `admin` 접두사를 빠뜨린 이름으로 적었고, 바이너리가 받는 이름은 `admin diagnose`입니다. 이 커밋에서 함께 바로잡았습니다. 릴리즈 노트에 잘못된 명령을 예시로라도 적지 않는 이유가 여기 있습니다 — 인용된 명령과 지시는 읽는 쪽에서 구분되지 않습니다. 검사는 한 방향입니다 — 바이너리에 있지만 문서가 언급하지 않는 명령은 문제로 보지 않습니다.
- **Discovery 문서가 광고하는 것과 endpoint가 실제로 하는 일이 일치하는지 테스트가 확인합니다.** RP는 아무에게도 묻지 않고 이 문서만 보고 스스로를 설정하므로, 어긋나면 연동하는 사람이 가장 나쁜 순간에 발견합니다. 각 항목을 양방향으로 시험합니다 — 문서가 제공한다고 한 것은 통해야 하고, 제공한다고 하지 않은 것은 다른 무언가로 조용히 처리되지 않고 거절되어야 합니다. `response_type`·`response_mode`·`code_challenge_method`·`request`/`request_uri`·`grant_type`을 다룹니다. 특히 `plain` PKCE는 검증자를 인가 요청에 그대로 실어 보내므로 요청을 본 사람이면 누구나 코드를 교환할 수 있는데, 문서는 `S256`만 제공한다고 밝힙니다.
- **`make test`가 `go test` 실패를 삼키던 문제**를 수정했습니다. 로그를 남기려고 출력을 `tee`로 넘기고 그 파이프라인의 상태를 `$?`로 읽는데, POSIX 셸에서 그 값은 **`tee`의 것**이라 언제나 0입니다. 그래서 실패한 `go test`가 `FAIL`을 찍고도 레시피는 다음 줄로 넘어갔고, 빌드가 멈추는 유일한 경우는 **그 뒤의 무언가가 따로 실패할 때**였습니다. 실제로 확인했습니다 — 일부러 실패하는 테스트를 넣고 `make test`를 돌리니 Go가 `FAIL`을 찍은 뒤 vet과 프론트엔드까지 진행했고, 프론트엔드가 자기 이유로 실패한 덕분에 우연히 멈췄습니다. 바로 그 줄 위의 주석이 "겉보기에 깨끗한 실행"을 경계하는 내용인데, 아래 줄이 그걸 가장 확실하게 만들어내고 있었습니다. 이제 레시피가 `pipefail` 아래에서 실행되며, 같은 실험에서 Go 줄에서 즉시 멈추고 프론트엔드는 실행되지 않습니다.
- **CI가 간헐적으로 실패하던 원인**을 찾아 고쳤습니다. 통합 테스트 서비스 스크립트의 TLS 준비 확인이 두 가지를 동시에 잘못하고 있었고, **두 오류가 서로를 가리고 있었습니다.**
  - `openssl ... | grep -q "Verify return code: 0"`는 `set -o pipefail` 아래에서 **일치했는데도 실패로 보고**할 수 있습니다. `grep -q`가 일치한 줄에서 즉시 빠져나가면 아직 쓰고 있던 openssl이 SIGPIPE를 받고, 파이프라인 상태가 141이 됩니다. openssl이 그전에 다 썼는지는 기기에 따라 달라서 여기선 통과하고 부하 걸린 러너에서 실패했습니다 — 실패 20밀리초 뒤에 찍힌 진단이 인증서가 내내 멀쩡했음을 보여줍니다.
  - 그런데 `Verify return code: 0`만으로는 **인증서가 검증됐다는 뜻이 아닙니다.** 검사할 peer 인증서가 아예 없으면 실패한 게 없으므로 openssl이 그렇게 출력합니다. Docker는 컨테이너가 뜨는 즉시 포트를 게시하므로 slapd가 듣기 전까지 연결은 수락됐다 끊기고, 그 빈 교환이 이 문자열을 만족시킵니다. 첫 번째 오류가 이 오탐을 가려주고 있었고(거짓 일치가 141이 되어 계속 기다림), 그것만 고치자 준비 확인이 디렉터리가 뜨기 전에 통과했습니다. 실제로 그 상태로 서비스를 올려 LDAPS 테스트를 돌려 확인했습니다 — 기동이 5.7초 만에 끝나고 테스트가 실패했습니다.
  - 이제 인증서가 제시되었고(`subject=`) 그것이 검증되었을 때만 준비된 것으로 봅니다. 기동은 27초가 걸리고 직후 LDAPS 테스트가 통과합니다.
- **Smoke test가 이번 릴리즈에서 고친 동작들을 확인하도록 확장했습니다.** 릴리즈 검증이 실행하는 것은 Go 테스트가 아니라 이 스크립트인데, 오늘 바꾼 것들 중 어느 것도 여기서 확인되지 않았습니다. 네 가지를 추가했습니다 — 계정 비활성화가 세션을 끝내고 **재활성화해도 돌려주지 않는지**, 정지된 Realm이 이미 발급된 Token에 대해서도 Discovery·Introspection·userinfo를 거절하는지, 자신이 로그인한 Realm 정지가 거절되는지, 이미 쓰이는 이름이 `409`와 문장으로 답하고 데이터베이스 문구가 새지 않는지.
- 추가한 Smoke test 검사들이 실제로 검사하는지 확인했습니다. 세션 종료 수정을 되돌린 이미지를 빌드해 돌리니 `re-enabling an account brought its old session back`으로 실패합니다. 나머지 세 블록은 기대값을 하나씩 뒤집어 각각 자기 메시지로 실패하는 것을 확인했습니다.
- **운영 문서의 감사 신호 표에 적힌 이벤트 이름이 실제로 서비스가 기록하는 이름인지 테스트가 확인합니다.** 서비스가 기록하지만 표에 없는 이벤트는 문제로 보지 않습니다 — 표는 목록이 아니라 "볼 만한 신호"의 선별이기 때문입니다.
- **릴리즈가 자기가 게시한 아카이브를 다시 반입해봅니다.** 포장까지는 검증했지만 그 역방향은 한 번도 확인한 적이 없어서, 포장 방식이든 반입 스크립트든 바뀌면 **운영자가 들여올 수 없는 릴리즈**가 나갈 수 있었습니다 — 하필 아무도 고쳐줄 수 없는 곳에서 벌어지는 단계입니다. 이제 로컬 이미지를 지운 뒤 게시되는 두 파일만으로, README가 안내하는 그 명령으로 다시 불러오고 이미지가 생겼는지 확인합니다. 손상된 아카이브가 거절되는지도 함께 확인합니다.
- 호출자가 없는 `AllowLoginAttempt`를 제거했습니다. "원래 동작을 원하는 호출자를 위한 호환 래퍼"라고 적혀 있었지만 그런 호출자는 없습니다. 다만 이 래퍼만은 잘못된 설정에서 거부하고 있었고, 그 판단이 옳았습니다.
- 위 Realm 정책 대조 중 문서 쪽 검사는 **초 단위로 범위를 적은 행만** 봅니다. 잠금 시간·비밀번호 길이·실패 횟수는 값을 고르는 사람이 생각하는 단위(`30초 ~ 24시간`)로 적혀 있고, 테스트를 만족시키려고 그걸 바꾸면 문서가 더 나빠집니다. 검사 범위를 넓히는 대신 그 사실을 테스트 주석에 적었습니다.
### Upgrade notes

마이그레이션은 없습니다. 스키마가 그대로이므로 v0.5.0으로 되돌리는 데 별도 절차가 필요하지 않습니다.

#### 영향을 받았는지 확인하는 방법

**기본 포트를 적어 등록한 Web Origin이 있는지** — 브라우저가 보내지 않는 형태이므로 그 Client의 CORS 요청은 지금까지 전부 거절되고 있었습니다. 아래에 나오면 콘솔에서 해당 Client를 한 번 저장하면 정규화됩니다.

```sql
SELECT c.client_id, o AS web_origin
  FROM clients c, unnest(c.web_origins) AS o
 WHERE o LIKE 'https://%:443' OR o LIKE 'http://%:80';
```

**서명 키를 만료 시각이 지난 뒤에도 쓰고 있었는지** — 이번 버전부터 `retire_at`이 지나면 즉시 효력을 잃습니다. 회전 직후 2시간이 지나기 전이라면 아무 영향이 없습니다.

```sql
SELECT realm_id, kid, status, retire_at
  FROM signing_keys
 WHERE status = 'PASSIVE' AND retire_at < now();
```

여기 나온 키로 서명된 Token은 이제 검증되지 않습니다. 보존 정리가 곧 `RETIRED`로 바꿀 행들이며, 정상적인 상태입니다.

#### 동작이 바뀌는 변경

- **로그인에 쓸 수 없는 Client는 이제 저장 자체가 거절됩니다.** v0.5.0에서는 허용 Scope에 `openid`가 없거나 Redirect URI가 하나도 없는 Client를 만들 수 있었고, 그런 Client는 만들어진 뒤 **모든 인가 요청이 실패**했습니다. 이제 만들 때도 수정할 때도 거절하므로, **그런 Client가 남아 있으면 이름만 바꾸려 해도 저장되지 않습니다** — 먼저 Scope나 Redirect URI를 채워야 합니다. 어차피 로그인이 되지 않던 Client이므로 고칠 대상이지만, 오류 문구가 방금 건드린 항목과 무관해 보일 수 있어 적어 둡니다.

```sql
SELECT c.client_id,
       NOT ('openid' = ANY(c.default_scopes)) AS missing_openid,
       cardinality(c.redirect_uris) = 0       AS missing_redirect_uri
  FROM clients c
 WHERE 'authorization_code' = ANY(c.grant_types)
   AND (NOT ('openid' = ANY(c.default_scopes)) OR cardinality(c.redirect_uris) = 0);
```

- **Revocation endpoint가 503으로 답할 수 있습니다.** 폐기하지 못한 경우이며, RFC 7009 §2.2.1대로 호출자는 Token이 아직 살아 있다고 가정하고 재시도해야 합니다. 200만 정상으로 처리하는 RP 라이브러리는 이제 오류를 보게 됩니다 — **그것이 정확한 상태입니다.**
- **204로 답하던 다섯 endpoint가 200에 본문을 실어 답할 수 있습니다.** 비밀번호 변경·재설정, 내 세션 종료, 관리자 세션 강제 종료, LDAP 공급자 삭제입니다. 요청은 요구받은 일을 했고 그에 딸린 동작이 실패한 경우이며, 본문이 무엇이 남았는지 말합니다. **204를 단정하는 클라이언트는 200도 받아들이도록 고쳐야 합니다.**
- **LDAP 공급자 변경은 원래 200에 항목을 실어 답했고**, 이 경우에만 `users_signed_out`과 안내 문구 두 항목이 더해집니다. 상태 코드는 그대로이므로 기존 클라이언트는 영향받지 않습니다. 부분 실패로 끝날 수 있는 여섯 operation 모두 OpenAPI 문서에 기술되어 있습니다.
- **`result=PARTIAL`이 나올 수 있는 감사 이벤트가 늘었습니다** — `LOGOUT`, `PASSWORD_CHANGE`, `PASSWORD_RESET`, `SESSION_REVOKE`, `ADMIN_FORCE_LOGOUT`, `LDAP_FEDERATION_UPDATE`, `LDAP_FEDERATION_DELETE`, `LOGIN_SUCCESS`. **`result=FAILURE`로만 알림을 걸어두면 전부 놓칩니다.**
- **LDAP 공급자를 비활성화하거나 연결 해제하면 Back-Channel Logout이 나갑니다.** 지금까지 나가지 않았으므로, 해당 RP는 받아본 적 없는 로그아웃 통지를 받게 됩니다.
- **지표가 하나 늘었습니다** — `resso_introspection_errors_total`. Resource Server의 질의에 답을 내지 못하는 상태를 나타내며, 응답은 `active=false`이므로 이 계열만이 "Token이 전부 죽은 것"과 구분해 줍니다. README와 권장 경보에 있습니다.
- **재기동이 더 이상 Bootstrap 관리자의 권한을 다시 부여하지 않습니다.** 재기동으로 관리자 접근을 되살리던 운영에 의존하고 있었다면 `admin recover`로 바꿔야 합니다.


## v0.5.0

이 릴리즈는 보안 수정 여덟 건을 담고 있습니다. 특히 **LDAP `DISABLE` 정책이 조직 전체를 비활성화할 수 있던 문제**와 **`mcp:read` 범위만으로 사용자 디렉터리를 읽을 수 있던 문제**는 운영 중인 설치라면 바로 확인하시길 권합니다. 동작이 바뀌는 변경과 마이그레이션은 마지막 [Upgrade notes](#upgrade-notes)에 모아두었습니다.

### 보안

- **`mcp:read` 범위만으로 사용자 디렉터리 전체를 읽을 수 있던 문제**를 수정했습니다. 이 범위는 모든 계정이 자기 API 키에 붙일 수 있는데, `resso_search_users`는 같은 Realm의 전 계정을 — 플랫폼 관리자 여부, 로그인 실패 횟수, 비밀번호 변경 시각까지 — 반환했고 `resso_list_clients`는 전 Client를 반환했습니다. 같은 레코드를 주는 REST 경로는 `admin:read`를 가진 관리자를 요구하므로 MCP가 그 요구를 우회하는 통로였습니다. 두 도구 모두 이제 관리자 + `admin:read`를 요구하며, 호출할 수 없는 키에는 `tools/list`에 나타나지 않습니다.
- **`id_token_hint`이 무시되던 문제**를 수정했습니다. RP가 조용한 갱신(silent renewal)에서 "이 계정을 갱신하는 중"이라고 알려주는 값인데, 브라우저에 다른 사용자가 로그인해 있으면 **그 사람의 코드가 돌아갔습니다.** RP는 같은 사용자를 갱신했다고 믿은 채 다른 계정으로 바뀝니다. 이제 지정된 계정과 현재 Session이 다르면 코드를 발급하지 않고, `prompt=none`이면 `login_required`를 반환합니다. 만료된 ID Token도 힌트로 받아들입니다 — 만료됐기 때문에 갱신하는 것이므로 거절하면 이 파라미터의 용도 자체가 사라집니다. 서명과 발급자는 검증합니다.
- **`request`·`request_uri`를 무시하지 않고 거절합니다.** Discovery는 미지원이라고 밝히고 있었지만 인가 endpoint는 조용히 무시하고 서명되지 않은 query string을 사용했습니다. 서명된 요청 객체는 그 안의 파라미터를 보호하려고 존재하므로, 무시하는 것은 RP가 요청한 보호를 조용히 무력화하는 것입니다. 사양이 정의한 `request_not_supported`·`request_uri_not_supported`로 응답합니다.
- **`max_age`가 무시되던 문제**를 수정했습니다. RP가 결제나 자격증명 변경처럼 민감한 동작 앞에서 "방금 본인 확인을 다시 하라"고 요구하는 파라미터인데, 받아들이고 아무 일도 하지 않았습니다. RP 입장에서는 재인증이 일어난 것과 구분되지 않는 상태로 기존 Session에서 만든 코드가 돌아왔습니다. 이제 마지막 인증이 지정한 시간보다 오래되었으면 SSO Session이 있어도 로그인 화면으로 보내고, `prompt=none`이면 `login_required`를 반환합니다. 숫자가 아닌 값은 무시하지 않고 `invalid_request`로 거절합니다 — 무시하면 RP에게는 재인증이 성공한 것처럼 보이기 때문입니다.
- **인가 코드 재사용을 유출로 처리합니다.** Refresh Token은 두 번 제시되면 계열 전체가 폐기되었지만 인가 코드는 거절만 됐습니다. 코드는 브라우저 리다이렉트로 실려 나가는 값이라 두 번 도착했다는 것은 새어 나갔다는 뜻이고, 둘 중 어느 쪽이 정당한 RP인지는 사후에 알 수 없습니다. 소비된 코드를 다시 제시하면 그 코드가 만들어낼 수 있었던 Refresh Token을 폐기합니다. 발급 대상 세션과 Client로 범위를 좁혀 다른 RP의 세션은 유지됩니다. 이미 발급된 Access Token은 만료까지 유효합니다.
- `TOKEN_REVOKED` 감사 이벤트가 실제로 무엇이 폐기되었는지 기록합니다. RFC 7009은 일치하는 Token이 없어도 200을 반환하므로 이전에는 "RP가 폐기를 호출했다"까지만 남았고, 사고 대응에서 정작 묻는 "그 Token이 죽었는가"는 알 수 없었습니다. 상세에 `refresh_token`(`family_id` 포함)·`access_token`(`jti` 포함)·`none`을 구분해 남깁니다.
- 인가 코드 재사용이 `AUTHORIZATION_CODE_REUSED` 감사 이벤트로 남습니다. 이전에는 낡은 요청과 구분되지 않아 유출을 알 수 있는 유일한 신호가 버려졌습니다. 계정·Client·세션을 함께 기록합니다.
- **잠긴 계정이 응답 시간으로 드러나던 문제**를 수정했습니다. 로그인 실패 메시지는 계정 존재 여부를 감추기 위해 모든 경우에 동일한데, 잠긴 계정은 비밀번호 해시를 아예 건너뛰고 거절해서 일반적인 거절의 절반 이하 시간에 응답했습니다(측정값 약 27ms 대 50–75ms). 알 수 없는 사용자에 대해 더미 검증으로 이미 막아둔 것과 같은 종류의 누출입니다. 이제 잠긴 계정에서도 비밀번호를 검증하므로 세 경로의 응답 시간이 구분되지 않습니다.
- **잠긴 사용자가 이유를 알 수 있습니다.** 비밀번호를 맞게 입력했는데도 거절당하면 잠금이지 비밀번호 문제가 아닌데, 같은 "아이디 또는 비밀번호가 올바르지 않습니다"만 보였습니다. 사용자는 비밀번호를 잊었다고 판단해 변형을 계속 시도하고 결국 다른 문제로 문의합니다. 올바른 비밀번호를 제시한 경우에만 잠금 사실과 남은 시간을 알려주며, 이때는 이미 계정 존재를 아는 상대이므로 열거에 도움이 되지 않습니다. 로그인 화면은 `Retry-After`로 남은 시간을 세어 보여주고 그동안 양식을 잠급니다. 비밀번호가 틀린 경우의 메시지는 그대로입니다.
- **승인 워크플로가 자기 요청을 승인할 수 있던 문제**를 수정했습니다. 이 기능은 아무도 자기 자신에게 Role을 부여하지 못하게 하려고 존재하고, 그 유일한 근거는 결재자가 요청자의 관리자라는 점인데 `manager_id`가 전혀 검증되지 않았습니다. 자기 자신을 지정하면 본인이 본인 요청의 결재자가 되고, 다른 Realm의 사용자를 지정하면 아무 자격 없는 외부인이 결재할 수 있었습니다. 결재자 검사가 식별자만 비교하고 Realm을 보지 않으며 검토함이 전 Realm을 가로질러 조회하기 때문입니다. `manager_id`를 저장하는 시점과 결재하는 시점 양쪽에서 막습니다. 이미 그런 조합이 저장된 데이터베이스에서도 결재는 거부됩니다. 관리자는 Role을 직접 부여할 수 있으므로 자기 승인 규칙에서 제외됩니다.

### 안정성

- **만료된 로그인 대기 요청이 이틀까지 남아 있던 문제**를 수정했습니다. 인가 endpoint는 로그인 화면이 필요할 때마다 행을 쓰고, 그 경로에 닿는 데는 Client 식별자와 Redirect URI만 있으면 됩니다 — 둘 다 RP의 로그인 링크에 그대로 보입니다. 즉 누구나 요청당 한 건씩 쓰게 만들 수 있는데, 행은 5분 뒤 모든 조회에서 보이지 않게 되면서도 하루 더 보관되었고 정리는 하루에 한 번만 돌았습니다. 이제 만료 즉시 정리 대상이 되고, 정리 주기도 한 시간으로 줄였습니다. 보관 기간이 긴 감사·로그 정리는 첫 통과 이후 거의 아무것도 지우지 않으므로 부담이 늘지 않습니다.
- 발급하지 못한 Token 요청을 지표에 집계합니다. `resso_tokens_issued_total`은 성공만 세므로, Signing Key를 열 수 없어 모든 Token 교환이 실패하는 상황에서도 시계열은 그냥 조용해집니다 — 로그인 지표와 같은 사각입니다. 실패는 발급이 아니므로 같은 카운터에 넣지 않고 `resso_token_errors_total`을 추가했습니다.
- 완료하지 못한 로그인 시도를 지표에 집계합니다. `resso_login_attempts_total`에는 성공·실패·Rate limit만 있어서, LDAP 디렉터리 장애처럼 **모든 시도가 그 이전에 실패하는 상황에서는 시계열이 그냥 조용해집니다.** 운영 가이드는 `failure` 급증을 보라고 안내하는데, 전면 장애가 가장 평온해 보이는 셈이었습니다. `result="error"`가 추가됩니다.
- **인증 없이 지표 시계열을 무한히 만들 수 있던 문제**를 수정했습니다. `resso_http_requests_total`의 `method` 라벨을 요청 줄에서 온 값 그대로 기록했는데, HTTP 메서드는 RFC 7230 토큰이면 무엇이든 유효합니다. 요청마다 새 메서드를 보내면 시계열이 하나씩 늘어나고, 레지스트리는 시계열을 회수하지 않으며 매 스크레이프마다 전부 출력합니다 — 메모리와 `/metrics` 응답이 함께 무한정 커집니다. 자격증명이 필요 없는 경로입니다. 이제 서비스가 실제로 다루는 메서드 외에는 `other`로 합칩니다. 비정상 메서드가 들어온다는 사실 자체는 계속 셉니다.
- **중단된 LDAP 동기화가 콘솔을 영구히 잠그던 문제**를 수정했습니다. "동기화 중인가"에 대한 답이 두 개였습니다 — 두 번째 실행을 거부하는 서버 가드는 `RUNNING` 상태를 마지막 보고 시각으로 한정했지만, 목록에 실리는 값은 원본 컬럼 그대로였습니다. 프로세스가 죽으면(최대 30분 걸리는 디렉터리 순회 도중 컨테이너 종료 등) 그 컬럼은 영원히 `RUNNING`으로 남습니다. 콘솔은 동기화 버튼을 계속 비활성화하고 5초마다 폴링을 반복하며 빠져나갈 방법이 없었는데, 정작 서버는 그동안 새 실행을 받아줬을 것입니다. 이제 목록이 서버가 조정한 `sync_running`을 실어 나르고 콘솔이 그 값을 쓰므로, 두 답이 어긋날 수 없습니다.
- **긴 Session에서 Back-Channel Logout이 조용히 끊기던 문제**를 수정했습니다. 어떤 Client가 그 Session에 참여했는지는 따로 기록되지 않고 남아 있는 Refresh Token과 인가 코드에서 유추하는데, 인가 코드는 만료 하루 뒤에 삭제됩니다. Refresh Token을 쓰지 않는 Client는 코드만 남기므로 그 시점부터 통지 대상에서 사라집니다 — Realm의 Session 수명은 30일까지 설정할 수 있습니다. 사용자가 로그아웃해도 해당 RP는 계속 로그인 상태로 남았습니다. 이제 Session이 살아 있는 동안 Client마다 코드 한 건을 남기고, Session이 끝나면 함께 정리합니다.
- **LDAP 동기화가 조직 전체를 잠글 수 있던 문제**를 수정했습니다. `DISABLE` 정책은 이번 동기화에서 보이지 않은 계정을 비활성화하고 세션을 종료하는데, 디렉터리가 사용자를 하나도 반환하지 않아도 그대로 실행됐습니다. 결과가 비어 있는 것은 실제로 전원이 떠난 경우와 `Users DN` 오타·이름이 바뀐 Base·Bind 계정 권한 상실이 구분되지 않으므로, 검색 설정 실수 하나가 다음 예약 동기화에서 해당 Provider의 모든 계정을 비활성화했습니다. 이제 디렉터리가 아무것도 반환하지 않았는데 활성 연동 계정이 남아 있으면 비활성화를 실행하지 않고 동기화를 실패로 기록합니다.
- **실패한 Token 갱신이 도난으로 오인되던 문제**를 수정했습니다. 갱신은 회전 후 발급의 두 단계인데, 두 번째가 실패하면(요청 사이의 계정 비활성화, 세션 종료, 서명 키 복호화 실패) RP는 서버가 이미 회전 처리한 Token을 쥔 채 남았습니다. 유예 창 30초가 지난 뒤의 재시도는 재사용 공격과 구분되지 않아 계열이 폐기되고, 일어나지 않은 도난이 `REFRESH_TOKEN_REUSE`로 감사 로그에 기록됐습니다. 계정 검사를 회전 앞으로 옮겼고, 그래도 실패하는 회전은 되돌립니다. 동시 갱신이 이미 후속 Token 위에 쌓았다면 그 교환은 성공한 것이므로 건드리지 않습니다.
- 관리자가 올바르지 않은 관리자(본인, 또는 다른 Realm 사용자)를 지정하면 무엇이 잘못됐는지 알려주는 400을 반환합니다.
- 계정 잠금 여부와 API 키 사용 가능 여부도 서버가 판단해 내려보냅니다. 콘솔이 브라우저 시계로 `locked_until`·`expires_at`을 비교했는데, 이 판단은 **동작을 가립니다** — 잠금 해제 버튼은 잠긴 계정에만 나타나고 키 회전·폐기는 살아 있는 키에만 열립니다. 관리자 PC 시계가 앞서 있으면 아무도 로그인할 수 없는 계정이 정상으로 보이고 해제할 방법이 없었으며, 아직 동작하는 키가 만료로 보여 폐기할 수 없었습니다.
- 세션 목록이 그 세션을 서버가 아직 받아주는지를 함께 알려줍니다. 콘솔은 `revoked_at`과 `expires_at`만 보고 활성 여부를 판단했는데, 유휴 만료는 `expires_at`이 몇 시간 남았어도 세션을 거부합니다. 그래서 이미 죽은 세션이 관리자의 `SSO 세션` 목록과 사용자의 `내 세션` 화면에 **활성으로** 표시됐습니다.
- 종료 시 버퍼에 남은 로그를 기록하고 끝냅니다. 이전에는 재시작 직전 수 초 분량이 유실되었습니다. 종료는 동시에 진행되므로 로그 미러가 끝난 뒤에도 기록이 도착할 수 있는데(백그라운드 작업 정리, 마지막 종료 로그), 이 경우 프로세스가 패닉으로 죽지 않고 해당 기록만 유실로 계산합니다.
- **종료 중이던 Back-Channel Logout 통지가 유실되던 문제**를 수정했습니다. 전달은 종료 신호와 함께 취소되는 컨텍스트에서 파생되어, SIGTERM이 도착하면 진행 중이던 요청이 그 자리에서 버려졌습니다 — 사용자는 로그아웃했고 ReSSO는 Session을 끝냈는데 RP는 통지를 받지 못해 계속 로그인 상태로 둡니다. 이런 전달에 시간을 주려고 있는 `Wait`가 정작 이미 포기한 작업을 기다리고 있었습니다. 이제 진행 중인 시도는 자체 제한 시간(10초, `Wait` 예산과 동일) 안에서 끝까지 진행하고, 새 재시도는 시작하지 않습니다.

### 기능

- Realm별 세션 유휴 만료를 추가했습니다. 이전에는 절대 수명(기본 8시간)만 있어 공용 PC에 열어둔 세션이 활동과 무관하게 끝까지 유효했습니다. 설정하면 그 시간 동안 사용되지 않은 세션이 만료되며, 브라우저 세션·Token 발급·Refresh 회전·MCP 인증 네 경로 모두에 동일하게 적용됩니다. 기본값 `0`은 사용 안 함이므로 업그레이드해도 동작이 바뀌지 않습니다.
- 사용자의 `내 세션` 화면과 관리자의 Realm 설정에서 적용 중인 유휴 만료 시간을 확인할 수 있습니다.
- 유휴 여부를 판단할 때 OIDC 사용도 사용으로 셉니다. 사용 시각은 관리 콘솔 요청에서만 갱신되어, RP를 통해서만 접속하는 사용자(하루 종일 그 애플리케이션에서 일하며 Token만 갱신하는 경우)의 Session은 로그인 시점부터 계속 유휴로 보였고 작업 중에 끊겼습니다. 이제 Token 갱신, Token 발급, MCP·Introspection의 Session 확인이 모두 사용으로 기록됩니다. 실제로 유휴한 Session이 만료되는 동작은 그대로입니다.

### 운영

- 서버 로그가 데이터베이스에 기록되지 못하면 지표로 드러납니다. 큐가 가득 차 버려지거나 쓰기가 실패해도 아무 신호가 없어, 관리 화면의 로그에 구멍이 생겨도 이유를 알 수 없었습니다. `resso_system_log_records_total`이 기록·유실·실패를 구분해 보고합니다.
- Back-Channel Logout 전달을 재시도합니다. 한 번 시도하고 실패하면 통지가 유실되어, RP가 재시작 중이면 사용자가 종료한 세션이 그쪽에 남았습니다. 이제 연결 실패·5xx·429처럼 저절로 해소될 수 있는 경우에만 2초·8초 간격으로 재시도하며, RP가 명시적으로 거절한 4xx는 반복하지 않습니다. 종료 중에는 즉시 중단합니다.

### 접근성

- 브라우저 탭이 보고 있는 화면의 이름을 갖습니다. 단일 페이지 앱은 새로고침 없이 경로가 바뀌므로 제목이 스크린 리더에 "페이지가 바뀌었다"를 알리는 유일한 신호인데, 모든 화면이 `ReSSO` 하나였습니다. 사용자·세션·감사 로그를 탭 세 개로 열어둔 관리자가 구분할 수 없고 브라우저 기록도 전부 같은 이름이던 문제도 함께 해소됩니다. 화면이 이미 표시하는 제목을 그대로 쓰므로 탭이 화면과 다른 말을 할 수 없습니다.
- 콘솔의 모든 표에 이름을 붙였습니다. 이름 없는 표는 스크린 리더에 그냥 "표"로 읽히는데, 한 화면에 여러 개가 놓인 곳이 있습니다 — 감사 이벤트와 서버 로그, 개인 API 키와 세션과 접근 요청. 표 단위로 이동하는 사용자에게는 구분할 단서가 없었습니다. 12개 중 2개에만 이름이 있어서 나머지 10개를 맞췄습니다.

### 개발

- 통합 테스트에 필요한 서비스를 스크립트 하나로 띄웁니다. PostgreSQL과 디렉터리 두 개(평문·TLS)가 없으면 60여 개가 건너뛰어지는데, 건너뛴 테스트도 `go test`는 `ok`로 보고하므로 로컬에서는 초록이고 CI에서 실패합니다. `make test`가 몇 개를 건너뛰었고 무엇을 실행하면 되는지 알려주며, CI도 같은 스크립트를 쓰므로 로컬에서 통과한 것과 CI가 검증하는 것이 갈라지지 않습니다.

- LDAPS 인증서 검증이 실제로 강제되는지 확인합니다. Bind 자격증명과 모든 사용자의 비밀번호가 이 연결로 지나가므로 무엇을 서버 인증서로 받아들이는지가 보호의 전부인데, 아무거나 믿는 구성은 다른 모든 면에서 똑같아 보입니다 — 연결되고, 검색되고, 인증됩니다. 모르는 CA가 서명한 인증서는 거부되고, 설정한 CA로는 같은 서버에 연결되며, 파싱할 수 없는 CA는 무시되지 않고 설정 오류로 거부되는 것까지 확인합니다. 검증을 끄면 첫 검사가 실패합니다.
- 디렉터리 그룹이 Realm Role로 매핑되는 경로를 실제 디렉터리로 검증합니다. 특히 **그룹에서 빠지면 Role도 회수되는지**를 확인합니다 — 회수되지 않으면 팀에서 옮겨진 뒤에도 접근 권한이 남습니다. 회수 로직을 무력화하면 이 검사가 실패합니다.
- 연동 계정의 로그인과 Edit Mode 동작을 실제 디렉터리로 검증합니다. `READ_ONLY`에서 편집과 비밀번호 변경이 거부되는지, `WRITABLE`에서 변경이 **디렉터리에 실제로 반영되는지**(로컬에만 남아 우연히 유지되는 것과 구분되는 유일한 방법입니다), 그리고 바뀐 비밀번호로 다시 로그인되는지까지 확인합니다.
- LDAP 동기화와 `DISABLE` 정책을 실제 디렉터리로 끝까지 검증합니다. 보이지 않는 계정을 비활성화하고 세션까지 끊는, 이 서비스가 스스로 하는 가장 파급이 큰 동작인데 seam이 없어 동기화 자체를 돌려본 적이 없었습니다. 이제 사용자를 가져오고, 한 명이 디렉터리에서 사라지면 **그 한 명만** 비활성화되며, 디렉터리가 아무것도 반환하지 않으면 아무도 비활성화하지 않고 실패로 기록되는 것까지 확인합니다. 가드를 제거하면 마지막 검사가 실패합니다.
- LDAP 디렉터리와 대화하는 코드를 실제 서버로 검증합니다. `internal/federation`은 외부 시스템에 나가는 유일한 경로인데 Go 테스트가 한 줄도 닿지 않았습니다 — 연결·검색·엔트리 변환·바인드 인증, 그리고 고객 디렉터리에 실제로 쓰는 속성 변경과 비밀번호 변경까지. 잘못된 Base, 거부되어야 할 Bind, 비워야 하는 선택 속성처럼 픽스처로는 흉내 낼 수 없는 실패들이 대상입니다. CI가 같은 이미지를 서비스로 띄우므로 이 테스트들은 실제로 실행됩니다. 커버리지 0%에서 70%로 올랐습니다.

- 동시성 안전성을 실제 경합으로 검증하는 테스트를 추가했습니다. Refresh Token 회전과 LDAP 동기화 점유는 순차 호출로만 검증되어 있었는데, 둘 다 락 경합이 핵심인 경로입니다. 병렬 탭이 동시에 refresh해도 모두 성공하고 같은 Token Family를 유지하는지, 동시 동기화 요청 중 정확히 하나만 점유하는지 확인합니다.
- 데이터가 있는 구버전 스키마에서 업그레이드하는 경로를 테스트로 고정했습니다. 마이그레이션은 지금까지 빈 데이터베이스에서만 검증되었는데, 실제로 깨지는 쪽은 기존 데이터가 있는 업그레이드입니다. 새로 추가되는 컬럼이 기존 동작을 바꾸지 않는지도 함께 검증합니다.
- OpenAPI 문서가 오류 응답의 형태를 기술합니다. 콘솔은 이 문서를 API 계약으로 안내하고 서비스는 "일관된 JSON 오류 형식"을 표방하는데, 정작 4XX는 `Request error`라는 설명 한 줄뿐이라 연동하는 쪽이 형태를 알려면 오류를 일부러 일으켜 봐야 했습니다. 이제 모든 endpoint가 `error`·`message`·`trace_id` 스키마를 참조하고, 분기가 필요한 주요 코드 목록과 `Retry-After` 헤더를 함께 기술합니다. 참조하는 스키마가 실제로 정의되어 있는지도 테스트로 확인합니다 — 끊어진 `$ref` 하나가 문서 전체의 클라이언트 생성을 막습니다.
- Smoke test가 MCP 도구의 권한 경계를 확인합니다. 이전에는 handshake만 검증해서, `mcp:read`만 가진 키가 사용자 디렉터리를 읽던 결함을 프로젝트의 종단 검증이 잡아내지 못했습니다. 이제 `admin:read`가 있는 키는 도구를 쓸 수 있고 없는 키에는 목록에도 나타나지 않는지 함께 확인합니다.
- 프론트엔드 테스트가 매 실행마다 파일 하나를 조용히 건너뛰던 문제를 수정했습니다. 파일마다 jsdom을 세우는 데 30초 넘게 걸려 15개가 경쟁하면 하나는 워커 시작에 실패했는데, 요약에는 `14 passed (14)`로 표시되어 온전해 보였습니다. 순수 함수만 검사하는 파일은 jsdom을 쓰지 않도록 해 15개 전부 실행되고 더 빨라졌습니다.

### 문서

- 서명 키 회전 권고 기준일을 서버가 응답에 실어 보냅니다. 대시보드는 서버 값을 쓰는데 서명 키 화면은 같은 숫자를 자체 상수로 들고 있었고, "대시보드와 일치한다"는 주석이 붙어 있었을 뿐 그것을 강제하는 것은 없었습니다. 한쪽만 바뀌면 두 화면이 같은 키를 두고 다른 말을 하게 됩니다.
- Realm의 네 가지 수명 설정(Access Token, Refresh Token, SSO Session, 유휴 만료)이 각각 무엇을 재는지 운영 가이드에 정리했습니다. 특히 Refresh Token 수명은 **최초 발급 시점부터 Token 계열 전체의 총 수명**이며 회전해도 연장되지 않는데, 이 점이 어디에도 적혀 있지 않아 회전할 때마다 갱신된다고 읽힐 수 있었습니다. Realm 설정 화면의 해당 필드에도 안내를 넣었고, 동작이 조용히 바뀌지 않도록 테스트로 고정했습니다.

### Upgrade notes

#### 영향을 받았는지 확인하는 방법

위 두 건은 "확인하시길 권합니다"로 끝내기에는 확인 방법이 자명하지 않습니다.

**LDAP `DISABLE`로 계정이 잘못 비활성화되었는지** — 남아 있는 증거는 계정 자체이므로 보존 기간과 무관하게 확인할 수 있습니다.

```sql
SELECT f.name AS provider, u.username, u.federation_synced_at
  FROM users u JOIN user_federations f ON f.id = u.federation_id
 WHERE u.enabled = false
 ORDER BY u.federation_synced_at DESC;
```

여기 나온 계정이 디렉터리에는 여전히 존재한다면 잘못 비활성화된 것입니다. 감사 이벤트로도 확인할 수 있는데, `LDAP_FEDERATION_SYNC`에서 `read`가 `0`인데 `disabled`가 0보다 큰 실행이 이 문제의 서명입니다. 다만 이 집계는 **v0.4.1부터** 감사에 남습니다. v0.4.0에서는 `LDAP_FEDERATION_SYNC_STARTED`만 기록되므로 서버 로그(30일 보존)를 봐야 합니다.

**`mcp:read` 키로 디렉터리가 읽혔는지** — **확인할 수 없습니다.** MCP endpoint는 v0.5.0까지 아무것도 기록하지 않았으므로 과거 호출의 흔적이 없습니다. 개인 API 키 목록의 `마지막 사용` 시각으로 그 키가 쓰였다는 것까지는 알 수 있지만 무엇을 했는지는 알 수 없습니다. 용도를 설명할 수 없는 `mcp:read` 키가 있다면 회전하거나 폐기하는 편이 안전합니다. 이후의 호출은 `MCP_TOOL_CALL` 감사 이벤트로 남습니다.

- **동작이 바뀌는 변경이 있습니다.** `mcp:read`만 가진 기존 API 키는 MCP에서 `resso_service_status`만 사용할 수 있게 됩니다. `resso_search_users`와 `resso_list_clients`를 계속 쓰려면 관리자 계정에서 `admin:read` 범위를 함께 가진 키를 발급하세요. 에이전트가 실패하는 도구를 들고 있지 않도록 `tools/list`에서도 함께 사라집니다.
- 마이그레이션 `005_session_idle_timeout.sql`이 `realms.idle_timeout_seconds`(기본 `0` = 사용 안 함)와 `sso_sessions(last_access)` 인덱스를 추가합니다. 기본값이 사용 안 함이므로 업그레이드만으로 세션 동작이 바뀌지는 않습니다.
- **v0.4.1로 롤백할 수 있습니다.** 마이그레이션 005는 기본값이 있는 컬럼과 인덱스만 추가하고, v0.4.1은 컬럼을 이름으로 지정해 읽고 쓰므로 마이그레이션된 데이터베이스에서 그대로 동작합니다. 실제 릴리즈 이미지 두 개로 확인했습니다 — 업그레이드 후 기존 사용자 로그인·기존 Client의 코드 교환·기존 API 키가 모두 유지되고, 되돌린 뒤에도 조회와 쓰기가 정상이며 Realm을 수정해도 새 컬럼 값은 보존됩니다. 다만 되돌린 동안에는 `유휴 만료`가 적용되지 않습니다. 설정값은 남아 있으므로 다시 올리면 그 시점부터 다시 적용됩니다.
- `manager_id`가 검증됩니다. 본인 또는 다른 Realm의 사용자를 관리자로 지정하는 요청은 400으로 거부됩니다. 기존 데이터는 그대로 두지만, 그런 조합으로는 승인 결재를 할 수 없습니다. 해당 계정이 있다면 관리자를 다시 지정하세요.
- 감사 이벤트에 `AUTHORIZATION_CODE_REUSED`가 추가됩니다. 이벤트 종류 목록은 데이터에서 도출되므로 관리 화면 필터에 자동으로 나타납니다.
- 지표에 `resso_system_log_records_total`이 추가됩니다.

## v0.4.1

- v0.4.0에서 LDAP 동기화를 백그라운드로 옮기며 **동기화 결과가 감사 이벤트에서 빠졌던 회귀**를 수정했습니다. 시작만 기록되고 성공·실패와 건수는 서버 로그(30일 보존)에만 남았습니다. `DISABLE` 정책은 계정을 비활성화하고 세션을 종료하므로 감사 대상이어야 합니다. 이제 수동·예약 실행 모두 완료 시 `LDAP_FEDERATION_SYNC` 이벤트를 기록합니다. 예약 동기화는 이전에도 결과를 감사에 남기지 않았으므로 함께 해소됩니다.
- LDAP User Federation 문서를 백그라운드 동기화 동작에 맞게 갱신했습니다.
- 통합 검색(Ctrl+K)에서 사용자나 Client를 선택하면 개인 프로필 화면으로 튕겨나가던 문제를 수정했습니다. 존재하지 않는 경로(`/admin/realms/{id}/users`, `.../clients`)를 반환해 catch-all 라우트에 걸렸습니다. 이제 실제 경로로 이동하며 Realm과 검색어를 함께 전달해 선택한 항목이 걸러진 상태로 열립니다. 결과 정렬도 고정했습니다.
- OpenAPI 문서가 실제 API 전체를 기술합니다. 등록된 라우트 25개가 문서에 없어, 콘솔이 "OpenAPI 3.1 계약"으로 안내하는 문서로 클라이언트를 생성하면 로그인·로그아웃을 포함한 절반가량이 빠졌습니다. 라우터를 순회해 누락을 실패로 처리하는 테스트를 추가해 다시 어긋나지 않게 했습니다.
- 목록의 행을 키보드로 열 수 있습니다. Client 설정, 감사 이벤트 상세, 로그 상세, Role 관리는 마우스 클릭으로만 열려 각 화면의 주 동작을 키보드로 수행할 수 없었습니다. 행 안의 버튼을 조작할 때는 행이 함께 열리지 않습니다.
- 아이콘 버튼에 접근 가능한 이름을 부여했습니다. API 키 회전·폐기와 세션 강제 로그아웃 버튼은 Tooltip만 있고 이름이 없어 스크린 리더에 "버튼"으로만 읽혔습니다. MUI Tooltip은 `aria-label`을 제공하지 않습니다.
- 서명 키가 오래되었을 때 알려줍니다. 서명 키는 자동으로 회전되지 않는데 회전 시점을 안내하는 곳이 없어, Bootstrap 때 만든 키를 몇 년째 쓰면서도 대시보드는 "정상"으로 표시했습니다. 이제 180일을 넘기면 대시보드 준비 상태에 표시되고 서명 키 화면에서 사용 기간과 함께 안내합니다. 실패가 아닌 권고이며 키는 계속 정상 동작합니다.

### Upgrade notes

- 호환성이 깨지는 변경은 없습니다. 데이터베이스 마이그레이션도 없습니다.
- LDAP 동기화 완료 시 `LDAP_FEDERATION_SYNC` 감사 이벤트가 다시 기록됩니다. v0.4.0에서는 `LDAP_FEDERATION_SYNC_STARTED`만 남았으므로, 그 사이 기간의 동기화 결과는 서버 로그에서 확인해야 합니다.
- 관리 대시보드 응답에 `aging_signing_keys`와 `signing_key_advisory_days`가 추가되었습니다. 추가 필드입니다.
- 통합 검색이 반환하는 `path` 값이 실제 콘솔 경로로 바뀝니다. 이전 값은 존재하지 않는 경로였습니다.
- 공식 오프라인 Docker 아카이브는 `linux/amd64` 전용입니다.

## v0.4.0

### 사용성

- 승인 화면이 요청자 이름과 **부여될 Role**을 표시합니다. 이전에는 요청자 UUID 앞자리와 `ROLE_ASSIGNMENT`라는 종류만 보였기 때문에, 결재자가 누구에게 어떤 권한을 주는지 모른 채 승인해야 했습니다. 관리자 승인함과 팀장 검토함 모두에 적용되며, 결정 대화상자가 승인 결과를 문장으로 다시 확인시킵니다.
- 감사 이벤트에 이벤트 유형·결과·행위자 필터와 페이지네이션을 추가했습니다. 이전에는 최신 500건을 정렬 없이 보여줄 뿐이라 더 오래된 기록에 접근할 방법이 없었고, 잘렸다는 사실도 알 수 없었습니다. 유형 목록은 실제로 기록된 값에서 가져옵니다.
- 감사 이벤트와 서버 로그를 Trace ID로 연결했습니다. 상세 화면에서 같은 Trace의 로그로 바로 이동하고, Trace ID를 복사할 수 있습니다.
- 서버 로그 검색이 입력마다 요청을 보내던 문제를 수정했습니다(300ms 디바운스). 500건 상한에 도달하면 잘렸음을 안내합니다.
- Role 목록이 할당된 사용자 수와 기본 Role 여부를 표시하고, 삭제 시 회수 범위를 명시한 확인 단계를 거칩니다. 이전에는 확인 없이 즉시 삭제되었습니다.
- 검색 입력의 `aria-label`이 MUI 루트 요소에 붙어 실제 `input`에는 적용되지 않던 접근성 결함을 수정했습니다.
- 세션이 끊긴 뒤에도 콘솔이 로그인 상태로 남아 있던 문제를 수정했습니다. 만료·관리자 강제 폐기·재시작 등으로 세션이 사라지면 모든 화면이 인라인 401로 실패할 뿐 로그인으로 돌아갈 방법을 알려주지 않았습니다. 이제 서버가 인증을 거부하면 로그인 화면으로 이동하고 이유를 안내합니다.
- 로그아웃이 신원 캐시를 지우지 못하던 문제를 수정했습니다. `setQueryData(key, undefined)`는 캐시를 비우지 않고, 실패한 refetch는 이전 데이터를 유지하므로 로그아웃 후에도 콘솔이 로그인 상태로 남았습니다.
- API·MCP 화면의 복사 버튼이 평문 HTTP에서 조용히 실패하던 문제를 수정했습니다(이전 릴리즈에서 놓친 마지막 사용처). 연결 예시도 고정된 예시 주소 대신 접속 중인 주소로 생성됩니다.
- LDAP 전체 동기화가 HTTP 요청 안에서 끝날 때까지 실행되던 문제를 수정했습니다. 사용자 수가 많은 디렉터리에서는 서버 `WriteTimeout`(60초)을 넘겨 관리자에게는 실패로 보이지만 동기화는 계속 진행되었고, 결과를 확인할 방법이 없어 다시 눌러 중복 실행을 만들 수 있었습니다. 이제 즉시 `202`로 응답하고 진행 상황을 공급자 상태로 확인하며, UI가 진행 중에는 자동으로 갱신합니다.
- 서버에 연결하지 못했을 때 영문 `Failed to fetch`가 그대로 노출되던 문제를 수정했습니다. 오프라인 배포에서 재시작 중 가장 흔한 실패인데 원인도 대처 방법도 알 수 없었습니다.
- 오류 안내에 상황별 대처 방법과 Trace ID 복사, 재시도 버튼을 추가했습니다. 재시도는 스스로 해소될 수 있는 실패(연결 불가·제한·서버 오류)에만 제공하고, 권한 부족이나 충돌에는 무의미한 재시도 대신 다음 행동을 안내합니다.
- 목록 정렬을 추가했습니다. 사용자와 감사 이벤트는 서버에서, Client와 Role은 화면에서 정렬합니다. 정렬 컬럼은 화이트리스트로 제한해 요청 문자열이 SQL에 들어가지 않으며, 동일 값에서도 페이지가 흔들리지 않도록 보조 정렬 키를 둡니다.
- 감사 이벤트와 서버 로그의 시각을 초 단위로 표시합니다. 이전에는 분까지만 표시되어 같은 분에 일어난 사건의 순서를 화면에서 구분할 수 없었습니다.
- Client 목록과 SSO 세션 목록에 검색을 추가했습니다. "이 사용자가 연 세션"을 찾으려면 Realm 전체를 스크롤해야 했습니다.
- 동일 공급자의 동시 동기화를 차단합니다. 이전에는 전체 디렉터리를 훑는 작업이 겹쳐 같은 사용자에 대한 쓰기가 교차될 수 있었습니다. 진행 중이면 `409`로 거절하고, 중단된 실행은 30분 후 자동 해제되어 공급자가 영구히 잠기지 않습니다. 예약 동기화가 수동 실행과 겹치면 실패가 아닌 건너뜀으로 기록합니다.

### Upgrade notes

- **`POST /api/admin/v1/realms/{realmID}/user-federations/{federationID}/sync`의 응답이 바뀝니다.** 이전에는 동기화가 끝날 때까지 기다린 뒤 `200`과 집계(`read`, `added`, `updated`, `failed`, `disabled`)를 반환했습니다. 이제 즉시 `202`와 `{status, message}`를 반환하고, 결과는 공급자 목록의 `last_sync_*` 필드에서 확인합니다. 이 endpoint를 호출하는 자동화가 있다면 수정이 필요합니다. 진행 중 재호출은 `409`입니다.
- 관리 API에 정렬 파라미터가 추가되었습니다(`sort`, `order`). 지정하지 않으면 기존 정렬을 유지하므로 기존 호출은 그대로 동작합니다.
- 감사 이벤트 조회에 `event_type`, `result`, `actor`, `trace_id`, `order` 필터가 추가되고 응답이 `{items, total}` 형태가 되었습니다. 이전에는 `{items}`만 반환했으므로 이 endpoint를 사용하는 자동화는 확인이 필요합니다.
- Realm Role 목록 응답에 `assigned_users`, `builtin` 필드가, 승인 요청 목록 응답에 요청자·검토자·대상 Role 이름이 추가되었습니다. 모두 추가 필드입니다.
- 새 endpoint: `GET /api/admin/v1/audit/event-types`.
- 데이터베이스 마이그레이션은 없습니다.
- 공식 오프라인 Docker 아카이브는 `linux/amd64` 전용입니다.

## v0.3.0

### 보안

- Client Secret 검증을 Argon2에서 Digest Keyring 기반 HMAC으로 전환. Client Secret은 ReSSO가 생성하는 256bit 난수라 Password Stretching이 불필요한 반면, 인증 없이 호출 가능한 Token·Introspection·Revocation endpoint에서 요청 1건마다 64MiB와 CPU 1코어를 소비하는 경로였습니다. 기존 Argon2 Hash는 그대로 동작하며 최초 인증 성공 시 자동 승격됩니다.
- OIDC Client 인증 실패에 대한 발신지·Client 식별자별 Rate limit 추가. 성공 경로에는 비용을 추가하지 않도록 실패만 계수하며 `429`와 `Retry-After`로 응답합니다.
- Argon2 동시 실행 수를 CPU 수에 비례해 제한. 인증 요청 폭주 시 메모리 사용량이 요청 수에 비례해 증가하던 문제를 해소합니다.
- Back-Channel Logout URI에 Redirect URI와 동일한 전송 규칙(HTTPS 또는 Loopback HTTP)을 적용. 이전에는 검증 없이 저장되었습니다.

### 기능

- OIDC Back-Channel Logout 1.0 구현. 이전 버전은 Client에 Back-Channel Logout URI를 저장하고 관리 화면에서 편집까지 가능했지만 실제로 통지하지 않았습니다. 이제 Session 종료 시 해당 Session에 참여한 Client에만 서명된 `logout_token`을 전송합니다.
- Prometheus `/metrics` endpoint 추가. 요청률·지연, Token 발급, 로그인 결과, Client 인증 실패, Back-Channel Logout 전달, LDAP 동기화 결과를 노출하며 `admin:read` 인증을 요구합니다.
- Access Token Introspection을 같은 Realm의 모든 Confidential Client에 허용. 이전에는 발급 Client만 조회할 수 있어 Resource Server 패턴이 불가능했습니다. Refresh Token Introspection은 보유 Client로 제한됩니다.
- ID Token에 `at_hash` Claim 추가. 이 Claim을 요구하는 엄격한 RP가 ID Token을 거부하던 문제를 해소합니다.
- RP-Initiated Logout에서 `id_token_hint` 대신 `client_id` 파라미터 허용.
- Discovery에 `backchannel_logout_supported`, `backchannel_logout_session_supported`, `frontchannel_logout_supported`, Request Object 지원 여부 메타데이터 추가.

### 안정성과 성능

- 로그인 검증이 Argon2 연산 내내 PostgreSQL 커넥션을 점유하던 문제 수정. 동시 로그인이 커넥션 풀을 고갈시켜 Token 발급과 관리 API까지 지연시켰습니다. 실패 계수는 Row Lock 대신 원자적 UPDATE로 처리하고, 해싱 도중 계정이 잠기면 성공 처리를 거부합니다.
- Refresh Token 회전에 30초 유예 창 도입. 병렬 탭이나 네트워크 재시도로 같은 Token이 두 번 제시되면 전체 Token Family가 폐기되어 정상 사용자가 강제 로그아웃되었습니다. 유예 창 밖의 재사용은 이전과 동일하게 침해로 처리합니다.
- Realm별 서명 키 캐시 추가. Token 발급마다 조회·복호화·PKCS#8 파싱을, 검증마다 조회·JWK 파싱을 반복하던 경로를 제거합니다. 회전 시 즉시 무효화되며, 이전 키는 2시간 동안 PASSIVE로 유지되므로 다중 인스턴스에서도 안전합니다.
- 서버 로그를 건별 INSERT에서 배치 `COPY`로 전환. HTTP 요청마다 로그가 1건 생성되므로 이전에는 로그 기록이 데이터베이스의 최다 쓰기 작업이었습니다.
- 사용자 검색용 선택적 Trigram 색인 추가. 관리 화면은 선행 Wildcard로 검색하므로 B-tree 색인을 사용할 수 없었고 입력마다 전체 스캔이 두 번 발생했습니다. `pg_trgm`이 설치되어 있으면 기동 시 색인을 만들며, ReSSO가 확장을 직접 설치하지는 않습니다.
- 보존 정책 정리 작업을 기동 1분 후 1회 실행. 이전에는 첫 실행이 24시간 뒤라 매일 재시작하는 서비스에서 만료 데이터가 전혀 정리되지 않았습니다. 만료된 Refresh Token 정리도 추가했습니다.

### 사용성 — 사용자

- 로그인 제한(429)의 `Retry-After`를 화면에 표시하고 남은 시간을 카운트다운합니다. 이전에는 헤더가 있어도 사용자에게 전달되지 않아, 차단된 상태에서 계속 재시도하게 되었습니다.
- 로그인 연속 실패 시 잠금 정책을 안내합니다. 계정 존재 여부를 노출하지 않도록 서버 응답은 그대로 두고 브라우저에서만 계수합니다.
- 비밀번호 변경 화면이 Realm의 실제 최소 길이를 조건 목록으로 보여주고 충족 전까지 제출을 막습니다. 이전에는 8자로 하드코딩되어 있어 기본 정책(12자)과 어긋났고, 서버 오류로만 알 수 있었습니다.
- 복사 버튼이 평문 HTTP Origin에서도 동작합니다. `navigator.clipboard`가 없는 오프라인 배포에서 예외가 조용히 삼켜져 일회성 Secret 복사가 실패하던 문제를 수정했습니다. 실패 시에도 사용자에게 알립니다.
- 세션 목록이 User-Agent 원문 대신 "Chrome · Windows" 형태로 표시합니다.
- 현재 사용 중인 세션을 종료할 때 확인을 요구합니다.

### 사용성 — 관리자

- 잠긴 계정을 비밀번호 재설정 없이 해제하는 기능을 추가했습니다(`POST .../users/{userID}/unlock`). 이전에는 잠금 해제 수단이 비밀번호 재설정뿐이라 잘못 입력했을 뿐인 사용자에게 불필요한 자격증명 변경을 강요했습니다.
- Realm의 비밀번호 최소 길이·잠금 횟수·잠금 시간을 조회하고 변경할 수 있습니다. 이 정책은 로그인과 비밀번호 변경에 계속 적용되고 있었지만 데이터베이스에만 존재해 확인할 수도 바꿀 수도 없었습니다.
- 관리 화면의 Realm 선택이 URL(`?realm=<name>`)에 반영되어 화면을 공유·북마크할 수 있습니다. 이전에는 localStorage에만 있어 같은 링크가 수신자마다 다른 Realm을 열었습니다.
- Client Secret 회전에 확인 단계를 추가했습니다. 클릭 한 번으로 즉시 회전되어 운영 중인 연동이 예고 없이 끊겼습니다.
- 사용자 목록에 잠김·비활성 상태 필터와 행 단위 잠금 해제를 추가하고, 잠금 해제 시점을 함께 표시합니다.
- 대시보드의 준비 상태 항목과 지표 카드가 해당 화면으로 연결됩니다. 이전에는 "3개 실패"만 알려주고 원인을 찾는 것은 운영자 몫이었습니다.
- 저장 성공 알림을 사라지는 Toast로 통일했습니다. 편집을 계속해도 남아 있던 성공 Alert이 의미를 잃던 문제를 해소합니다.
- `LISTEN_ADDRESS`로 Listen 주소를 설정할 수 있습니다. 이전에는 코드에 `:8080`이 고정되어 Container 밖에서 포트를 바꿀 수 없었습니다.

### 개발

- `golangci-lint`, `govulncheck`, ESLint, Trivy 이미지 스캔을 CI에 추가하고 `make lint` 제공.
- 프론트엔드 테스트에 Testing Library 정리(cleanup)를 등록했습니다. Vitest가 `globals: false`로 동작해 자동 정리가 걸리지 않았고, 한 파일에 테스트를 추가하면 DOM이 누적되어 조회가 실패했습니다.
- 콘솔의 "비동기 데이터에서 상태를 파생"하는 9곳을 `useEffect` 대신 렌더 중 조정으로 바꿨습니다. 효과로 동기화하면 갱신 전 값이 한 프레임 보이고 렌더가 연쇄됩니다. `AuthProvider`·`ToastProvider`의 Hook을 별도 모듈로 분리해 Fast Refresh도 복구했습니다.
- 프론트엔드 린트를 `--max-warnings 0`으로 고정했습니다.
- `go.mod`의 `go` 지시자를 `1.26`으로 낮추고 `toolchain go1.26.7`을 분리. 이전에는 모든 기여자에게 정확한 Patch 버전을 강제했습니다.

### Upgrade notes

- Migration `004_operations.sql`이 Back-Channel Logout 대상 조회와 만료 Refresh Token 정리를 위한 인덱스를 추가합니다. 별도 조치는 필요 없습니다.
- Client Secret이 Digest Keyring으로 보호됩니다. 기존 Argon2 Secret은 계속 동작하며 최초 인증 성공 시 자동 승격되지만, **Digest Key를 제거하기 전에 모든 Confidential Client의 Secret을 회전**해야 합니다. Client Secret은 만료되지 않으므로 이 항목이 사실상 이전 Digest Key의 보관 기간을 결정합니다.
- Access Token Introspection이 같은 Realm의 모든 Confidential Client에 허용됩니다. 이전에는 발급 Client만 조회할 수 있었으므로, 이 동작에 의존해 접근을 제한하던 구성이 있다면 검토하세요. Refresh Token Introspection은 보유 Client로 계속 제한됩니다.
- Refresh Token 재사용 판정에 30초 유예 창이 생겼습니다. 유예 창 안의 재제시는 침해가 아닌 재시도로 처리되어 새 Token을 발급합니다.
- ID Token에 `at_hash`가 추가됩니다. Claim을 화이트리스트로 검증하는 RP가 있다면 확인하세요.
- `GET /metrics`는 `admin:read` 권한을 요구합니다. Prometheus에 개인 API Key를 Bearer token으로 설정하세요.
- 사용자 검색 색인은 선택 사항입니다. 데이터베이스 소유자 권한으로 `CREATE EXTENSION pg_trgm;`을 실행하면 다음 기동 시 색인이 생성됩니다. 실행하지 않으면 기존 동작을 유지합니다.
- 공식 오프라인 Docker 아카이브는 `linux/amd64` 전용입니다.

## v0.2.1

- Data Encryption Keyring과 Digest Keyring을 분리하고, Key ID가 포함된 암호문 Envelope 및 기존 v0.2.0 키 읽기 호환 추가
- 기존 Session, Authorization Code, Refresh Token, 개인 API Key를 유지하는 다중 Digest 조회와 무중단 단계형 키 회전 지원
- `admin diagnose`, `admin recover --password-stdin`, `crypto rewrap` 운영 복구 CLI 추가
- 계정별 로그인 제한은 실패 시에만 증가하고 성공 시 초기화하며 실제 남은 시간을 `Retry-After`로 반환
- Digest Key 활성 순서가 다른 인스턴스도 로그인 제한 버킷을 원자적으로 병합하고, 관리자 복구 시 계정 제한까지 초기화
- MCP OIDC 인증을 활성 사용자 Session에 정확히 결합해 `client_credentials` 주체 혼동과 폐기 Session 재사용 차단
- 이메일을 선택값으로 통일하고 변경·삭제 시 `email_verified`를 해제하며 관리자가 명시적으로 확인할 수 있는 UI 추가
- 비어 있지 않은 이메일의 서버 형식 검증, Keyring 설정 오류의 키 재료 비노출, 변경 상태 감사 metadata 보강
- Authorization Code 동시 소비, Refresh Token 재사용, Keyring 회전, Rate Limit, 관리자 복구를 실제 PostgreSQL에서 검증하는 통합 테스트 추가
- 빈 배열 Client 필드의 `NULL` 저장과 복구 감사 이벤트의 PostgreSQL 타입 추론 오류 수정

### Upgrade notes

- 기존 `ENCRYPTION_KEY` 단일 키 모드는 v0.2.0 형식의 암호문 쓰기를 유지하므로 먼저 모든 인스턴스를 v0.2.1로 안전하게 올릴 수 있습니다. 이후 새 Keyring에 기존 키를 포함하는 단계형 회전을 수행하세요.
- 암호문이 참조하는 Data Encryption Key ID는 변경하거나 다른 키 재료에 재사용하지 마세요. `crypto rewrap`은 Signing Private Key와 LDAP Bind Credential만 재암호화하며 Digest는 재작성하지 않습니다.
- `crypto rewrap`과 `admin diagnose`가 완료되면 이전 Data Encryption Key를 제거할 수 있습니다. 이전 Digest Key는 해당 키로 만든 모든 장기 API Key까지 회전·폐기한 후에만 제거하세요.
- 공식 오프라인 Docker 아카이브는 `linux/amd64` 전용입니다.

## v0.2.0

- LDAP/Active Directory User Federation: LDAPS/StartTLS, 연결·인증 테스트, JIT·전체·주기 동기화, Group→Role 매핑
- Realm Role과 Client Role CRUD, 사용자별 수동 할당·회수, LDAP 관리 Role 보존
- `realm-admin`을 실제 Realm 범위 관리 권한으로 연결하고 교차 Realm 접근 차단
- Client Web Origin 기반 OIDC CORS와 정확한 Origin 검증
- `profile`, `email`, `roles` Scope에 따른 Claim 최소화, 명시적인 `email_verified`, 최초 Session 기준 `auth_time`
- PostgreSQL 공용 로그인 Rate Limit과 신뢰 Proxy CIDR 기반 Client IP/TLS 판정
- 개인 API Key `api:read` 및 관리자 `admin:read` REST GET 인증
- 실제 운영 위험을 표시하는 관리자 준비 상태 대시보드와 사용자 목록 페이지네이션
- 이메일이 없는 사용자에게 합성 주소를 만들지 않고 여러 빈 이메일 계정을 허용
- PostgreSQL 통합 Smoke Test를 CI와 Release workflow에 추가

### Upgrade notes

- Migration `003_hardening.sql`이 이메일 검증 상태와 로그인 제한 테이블을 추가합니다.
- 기존 Client에는 `roles` 기본 Scope가 자동으로 추가되어 v0.1.x의 Role Claim 동작을 유지합니다.
- Reverse Proxy Header는 `TRUSTED_PROXY_CIDRS`에 등록된 발신지에서만 신뢰합니다. 운영 Proxy CIDR을 배포 전에 설정하세요.
